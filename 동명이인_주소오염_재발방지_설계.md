# 동명이인 주소 오염 — 원인 분석·재발 방지 설계서

> 작성: 2026-07-10 안토니(드림팀) · 사고 사례: 동대문구 2026-06 김옥순 2인
> 상태: **v1.2 (독립 점검 8건 반영·구현 완료)** — 점검 판정 「보충필요」 → 보완 후 구현

---

## 1. 사고 요약

| | 내용 |
|---|---|
| 증상 | 같은 행정동(신설동)의 동명이인 김옥순 2인 중, **주민센터 수령 요청자(010-9923-1069)의 주소가 다른 사람(010-9108-4341)의 집주소**("천호대로 55-3, 지층 오른쪽")로 표시 |
| 발생 시각 | 2026-06-24 17:06 KST (레코드 `updatedAt` 물증) |
| 원인 행위 | `scripts/repair-address-tampering.mjs --write` 실행 (6/23 "주소 변조" 사고의 복구 작업) |
| 피해 범위 | 해당 실행이 덮어쓴 53건 전수 감사 결과 **실제 오염 2건** — ①김옥순(1069): 천호대로 26 → 55-3 (동명이인 주소 복제) ②심광흠(0020): 제기로2가길 26 → 제기로 26 (도로명 절단·괄호 훼손) . 나머지 51건은 의도된 부번(-N) 제거로 정상 |
| 물증 | 오염 레코드에만 스크립트 서명 필드(`roadAddr`·`detailAddr`·`parenInfo`) + `updatedAt=2026-06-24` 존재. 기본명단(base_lists)의 강키(이름+전화) 대조로 본번 상이 2건 확정 |

## 2. 근본 원인 (6중 결함의 결합)

`repair-address-tampering.mjs`의 매칭·가드 설계 결함:

| # | 결함 | 김옥순 사고에서의 작동 |
|---|---|---|
| R-1 | **원본 매칭 키가 이름(+행정동 느슨 필터)뿐** — 생년월일·전화 미사용 | 동명이인 2인이 같은 행정동이라 서로의 원본과 교차 매칭됨 |
| R-2 | **도로명 없는 원본 행은 원본맵에서 탈락** (`name && rn` 조건) | "신설동주민센터" 원본(도로명 없음)이 맵에 안 들어가 → 그 사람 몫의 원본이 '없는' 상태가 되고, 동명이인의 원본(55-3)만 남음 |
| R-3 | **주민센터 제외 가드(②)가 '저장된 주소' 문자열만 검사** | 이미 정제되어 "천호대로 26"이 된 주소엔 '주민센터' 단어가 없어 가드 통과 |
| R-4 | **`\|\| origs[0]` 임의 폴백** — 본번 불일치인데도 첫 원본 채택 | 저장 26 vs 원본 55-3, 본번 불일치임에도 55-3으로 강제 교체 |
| R-5 | **원본↔레코드 1:1 대응 미보장** — 같은 원본이 여러 레코드에 재사용 가능 | 동명이인 A의 원본이 A와 B 두 레코드 모두의 "정답"으로 쓰일 수 있음 |
| R-6 | **엔진 저하 모드에서 재정제 결과를 신뢰** — `.env`에 매칭서비스 URL이 없으면 `lookupAddr`가 전부 null → 재정제=포맷팅 수준인데 그 결과를 그대로 기록 | 심광흠 건: 구청 원본이 축약 표기("제기로 26" — 카카오 주소검색 미실존 확인)였는데, 저하 모드 재정제가 이를 실존 검증 못 하고 통과 → 과거 올바른 정제값("제기로2가길 26" 실존)을 비실존 주소로 회귀시킴. ※독립 점검 지적 반영: 기존 L80 가드(`nrn≠target.rn` 스킵)는 원본 자체가 축약일 때 무력 — 온라인 실존 검증(S-6)이 유일한 방어 |
| R-7 | **주소 컬럼 자동 감지 오선택 위험** (독립 점검 발굴) — 헤더 무관, `(대로\|로\|길)\d` 최다 컬럼 채택 → 비고/구주소 컬럼 오선택 가능 | v2: 감지 컬럼이 이름/전화 컬럼과 겹치면 시트 전체 수동확인으로 전환 |
| R-8 | **동 컬럼 미감지 시 지역 필터 완전 무효** (독립 점검 발굴) — `dong=''`이면 전 지역 동명이인이 후보로 유입 | v2: 경고 출력 + 강키/양측유일 게이트가 최종 방어(약키 단독 채택 불가) |

**시스템 배경 결함(별도 트랙)**: 운영 배포 번들에 `VITE_ADDRESS_MATCH_API_URL=''`(빈 값)로 박혀 있어 **운영 앱 정제도 저하 모드**(IndexedDB 캐시 잔존물 + Kakao POI만)로 동작 중. 이번 설계 범위는 스크립트 재발방지이며, 서비스 URL 복구는 형 결정 필요(§7).

## 3. 설계 원칙 — 「동명이인 안전 매칭 규칙 (S-규칙)」

프로젝트 전체(앱·스크립트·향후 도구)에 적용하는 절대 규칙. CLAUDE.md에 §1-4로 등재한다.

| # | 규칙 |
|---|---|
| S-1 | **주소를 쓰는(변경하는) 매칭은 강키만**: `이름+생년월일` 또는 `이름+전화끝8` 일치. 이름 단독·이름+행정동 약키로 주소 쓰기 절대 금지 |
| S-2 | **약키는 양측 유일 시에만**: 이름 약키 매칭은 **원본과 레코드 양쪽 모두**에서 그 이름이 유일할 때만 허용(점검 반영 — 한쪽 유일만으론 '원본 누락+레코드 동명이인' 시나리오에서 재현됨). 강키도 후보 2+건(가족 공용 전화)이면 스킵. 동명이인 감지 시 무조건 스킵+수동확인 리포트 |
| S-3 | **무도로명 원본도 등록**: 주민센터·시설·무주소 원본 행도 원본맵에 보존(rn=null). 해당 인물의 레코드는 자동 수리에서 제외(원본이 특수 → 담당자 판단) |
| S-4 | **원본 소비(1:1) 보장**: 원본 1행은 레코드 1건에만 매칭. 재사용 시도 = 이상신호 → 양쪽 모두 스킵 |
| S-5 | **임의 폴백 금지**: 본번 일치 원본이 없으면 교체하지 않는다 (`origs[0]` 류 금지) |
| S-6 | **저하 모드 실행 차단**: 주소를 쓰는 스크립트는 시작 시 매칭서비스 URL 존재+헬스체크를 강제하고, 행별로 온라인 매칭 성공(`matchSource`)을 확인. 미충족 → 해당 행 스킵(또는 전체 중단) |

## 4. 구현 변경 사항

### 4-1. `scripts/repair-address-tampering.mjs` v2 (매칭 코어 교체)
- 원본 엑셀 파싱 시 **전화 컬럼**(휴대폰/전화/연락처)도 감지·추출
- 매칭 순서: ①강키 `이름+전화끝8` → ②약키(이름 유일+원본 유일일 때만) → ③그 외 스킵+리포트
- S-3: rn 없는 원본도 등록, 자기 원본이 무도로명/주민센터(CENTER)이면 스킵
- S-4: 매칭된 원본은 소비 처리(중복 사용 차단)
- S-5: `|| origs[0]` 제거 — 본번(baseNo) 일치 원본만 채택
- S-6: 시작 게이트(`VITE_ADDRESS_MATCH_API_URL` 필수 + `/health` 체크) + 행 게이트(`result.matchSource` 필수)
- 매칭 코어는 **순수함수 모듈**(`scripts/doppelganger-guard.mjs`)로 분리 → 단위 테스트

### 4-2. 신규 `scripts/doppelganger-guard.mjs` + 테스트
- `buildOrigIndex(rows)` / `matchOrigForRecord(rec, index)` 순수함수 — 김옥순·심광흠 사례를 픽스처로 한 회귀 테스트(`node --test`) 포함

### 4-3. 오염 데이터 복구 `scripts/restore-doppelganger-damage.mjs`
- 6/24 서명(53건) 재감사 → 강키 기본명단 대조 **본번 상이만** 복구 대상(현재 2건)
- dry-run 기본, `--write`는 형 승인 후. 복구 값 = 기본명단 강키 값(주소 3분할 필드 동기 갱신)

### 4-4. `CLAUDE.md` §1-4 「동명이인 안전 매칭(S-1~S-6)」 등재
- 향후 세션·도구가 같은 함정에 빠지지 않도록 SSOT화

### 4-5. 앱 우회로 봉쇄 — `CloudListManager.handleFetchBaseNotes` (독립 점검 발굴)
- 특이사항 이식의 `이름+읍면동` 약키 폴백이 "**값** 유일"(같은 비고 1종) 기준이라, 같은 동 동명이인 중
  1명만 비고가 있으면 **두 사람 모두에게 이식**되는 오염 경로 → "**사람** 유일"(인원 1명) 기준으로 교체
- 인원 집계는 특이사항 유무와 무관하게 전원 기준(비고 없는 동명이인도 차단 근거)

## 5. 점검(독립 검사) 결과 반영 — v1.1 개선

| 지적 | 반영 |
|---|---|
| 강키도 충돌 가능(가족 공용 전화) | 강키 후보가 2건 이상이면 그것도 스킵+리포트 (유일 시에만 채택) |
| 원본 엑셀에 전화 컬럼이 없을 수 있음 | 전화 컬럼 미감지 시: S-2 약키 유일성 경로만 동작(동명이인 전원 스킵) — 매칭률 하락은 리포트로 보완, 오염 위험은 0 유지 |
| 헬스체크 엔드포인트 부재 가능성 | `/health` 404 시 `POST /v1/address/match` 시험 질의("서울특별시청")로 대체 검증 |
| 기존 51건 정상 수리 회귀 여부 | v2 dry-run을 동일 데이터로 재실행 — 51건 판정 불변(스킵 아님) + 2건 오염이 '스킵'으로 바뀌는지 검증 |
| 복구 기준(base_lists)도 오염 가능 | 복구 대상 2건은 base_lists 값·저번달 이력·구청 원본 3자 대조 결과 첨부(김옥순: 이력 2026-06=천호대로 26 일치 확인됨) |

## 6. 검증 계획 (증거 기반)

1. `node --test scripts/doppelganger-guard.test.mjs` — 김옥순/심광흠 픽스처 회귀 포함 전건 통과
2. v2 스크립트 dry-run(동대문구 6월 데이터): 오염 2건 경로가 **스킵**으로 분류되는지 + 정상 51건 판정 불변인지
3. 복구 스크립트 dry-run → 형 승인 → `--write` → 재조회로 본번 일치 확인
4. 커밋·푸시(ttong627)

## 7. 별도 트랙 (형 결정 필요 — 이번 구현 범위 외)
- **운영 배포의 매칭서비스 URL 공백**: 빌드 env에 `VITE_ADDRESS_MATCH_API_URL` 미설정 → 운영 정제가 저하 모드. address-service(Cloud Run) 가동 상태 확인 후 URL 재주입 재배포 권장
- base_lists에 남은 과거 변조 잔존값(부번 -N 계열 51건의 기본명단 원복 여부)

## 구현 결과 (2026-07-10 — 실행 증거 기반)

| 항목 | 상태 | 증거 |
|---|---|---|
| `scripts/doppelganger-guard.mjs` + 테스트 9종 | ✅ 완료 | `node --test` **9/9 통과** (사고 픽스처 김옥순 skip·심광흠 본번상이 skip·부번수리 회귀 유지·양측유일·강키충돌·원본소비) |
| `repair-address-tampering.mjs` v2 (S-1~S-6, R-7/R-8 가드) | ✅ 완료 | 코드 교체. ※운영 dry-run 회귀는 매칭서비스 URL 복구(§7) 후 가능 — S-6 게이트가 저하 모드 실행을 의도적으로 차단 |
| `CLAUDE.md` §1-4 S-규칙 등재 | ✅ 완료 | 본 커밋 diff |
| 앱 우회로 봉쇄 (4-5, handleFetchBaseNotes 사람-유일) | ✅ 완료 | eslint 0 · tsc 0 |
| 전수 오염 스캔 (전 지자체·전 월) | ✅ 완료 | 서명 레코드: 동대문 6월 53건 + 성남 중원 6월 32건(그 외 0). 강키 대조 → **본번상이 피해 2건**(김옥순·심광흠), 중원구 피해 0 |
| 좌표 무결 확인 | ✅ 완료 | 피해 2건의 기존 lat/lng가 복구 목표 주소의 지오코딩과 이미 일치(6/24 오염은 주소 문자열만 훼손) |
| 오염 2건 데이터 복구 | ⏳ **형 승인 대기** | `restore-doppelganger-damage.mjs` dry-run 검증 완료(드리프트 가드·특이사항 '주민센터 수령' 명기 포함). `--write`는 프로덕션 쓰기 권한 승인 필요 |

### 남은 후속 (형 결정)
1. **복구 실행**: `node scripts/restore-doppelganger-damage.mjs --write` 승인
2. **§7 매칭서비스 URL 복구** — 운영 앱·수리 도구 모두 저하 모드 탈출 (CRITICAL, 별도 세션 권장)
3. (낮음) `import-driver-seqno.mjs` 강키 충돌 무검사 — 다음 사용 전 guard 모듈 적용
4. (개선) 재발 자동 탐지(같은 지자체·월, 강키 다른 동명이인의 도로명+본번 완전일치 경고) + 쓰기 스크립트 공통 감사로그 — 차기 작업
