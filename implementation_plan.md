# 🚀 Nexus Pipeline V4.0 스케일업 마스터플랜 (긴급 1~3순위 통합 적용)

형님, 명령하신 대로 "세계 최고의 프로그램"으로 도약하기 위한 핵심 1~3순위 기능의 완벽한 구현 계획을 수립했습니다. 코드를 수정하기 전, 아래 계획을 검토해 주시고 승인해 주시면 즉시 100% 전체 코드로 구축에 돌입하겠습니다.

---

## 📌 주요 제안 변경 사항

### 1️⃣ 집단 지성 오타 사전 (AI 학습 엔진 연동)
현재 `addressEngine.js`는 최초 1회만 Firestore에서 `typo_dict`를 읽어오고 쓰기 기능이 없습니다.
- **기능 추가:** 유저가 검수 화면(Grid)에서 주소를 직접 수정하여 엔터(Enter)를 치면, 수정 전의 '잘못된 주소'와 '교정된 주소'의 패턴을 추출해 즉시 Firestore `typo_dict` 컬렉션에 자동 저장(Auto-save)합니다.
- **실시간 반영:** 저장됨과 동시에 로컬 캐시 엔진의 오타 사전도 갱신되어, 바로 다음 행(Row)부터 동일한 오타를 만나면 AI가 자동으로 고쳐버립니다.

### 2️⃣ 스마트 대시보드 (관제 센터) 구축
현재 로그인하면 바로 파일 업로드 창(1단계)이 뜹니다. 이를 개선하여 진정한 엔터프라이즈 SaaS처럼 대시보드를 첫 화면(Step 0)으로 띄웁니다.
- **새로운 컴포넌트 (`Dashboard.jsx`):** 
  - 나의 누적 정제 건수
  - 내가 기여한 집단 지성 오타 수정 건수
  - 시스템 전체의 성공/에러 비율 차트 (UI 애니메이션 적용)
- **흐름 변경:** 로그인 -> 대시보드(통계 확인) -> `[새로운 정제 시작]` 버튼 클릭 -> 1단계 업로드 진행.

### 3️⃣ 작업 히스토리 및 Undo(실행 취소) 안전망 구축
유저의 실수를 방지하고 데이터를 완벽히 보호합니다.
- **Undo(실행 취소) 기능:** 그리드에서 데이터를 잘못 수정했을 때 `Ctrl + Z`를 누르거나 화면의 `[↩ 되돌리기]` 버튼을 누르면 이전 상태로 복구됩니다. (최대 20단계 상태 저장)
- **Session Recovery (임시 저장):** 브라우저가 강제로 종료되거나 F5를 눌러 새로고침 되어도, 세션 스토리지(Session Storage)에 `gridData`를 실시간 암호화 동기화하여 다시 접속 시 "이전 작업 내역을 복구하시겠습니까?"라고 물어보는 로직을 추가합니다.

---

## ⚠️ User Review Required (승인 대기)

> [!IMPORTANT]
> **대시보드 라이브러리 추가 여부:** 화려한 차트를 그리기 위해 `recharts` 패키지를 추가해도 되겠습니까? (명령어: `npm install recharts`)
> 만약 외부 패키지 설치를 원치 않으시면, TailwindCSS만으로 세련된 차트와 진행바를 하드코딩하여 만들겠습니다.

> [!TIP]
> **세션 복구 방식:** 브라우저 세션 스토리지를 사용할 계획입니다. 이는 탭을 닫기 전까지는 F5를 눌러도 데이터가 유지되는 가장 빠르고 안전한 방식입니다.

## 🛠️ 수정/생성될 파일 목록 (예상)

- **[NEW] `src/components/Dashboard.jsx`** : 로그인 직후 만나는 화려한 관제 센터 화면.
- **[MODIFY] `src/App.jsx`** : 대시보드 라우팅(Step 0 추가), Undo/Ctrl+Z 로직, Session Storage 연동 로직 추가.
- **[MODIFY] `src/engine/addressEngine.js`** : `addTypoRecord` 비동기 쓰기 함수 추가 및 실시간 사전 동기화.

---

## ✅ 검증 플랜
1. 대시보드 진입 시 내 정보와 통계가 뜨는지 확인.
2. 그리드에서 주소 오타를 고쳤을 때, Firestore에 즉시 적재되는지 테스트.
3. 데이터 수정 후 `Ctrl + Z`를 눌렀을 때 원래 값으로 복원되는지 테스트.
4. F5 새로고침 후 복구 알림이 뜨고 기존 데이터가 부활하는지 테스트.

**형님, 위 마스터플랜을 이대로 진행할까요? 라이브러리(Recharts) 도입 여부와 함께 "진행해" 라고 승인해 주시면 팀 전원이 코딩을 시작합니다!**
