# 기사배정·순번 개편 계획 (형 지시 2026-08-27)

## 형이 말씀하신 것 (그대로 옮김)
1. 기사배정 루트맵 → **지자체 선택** → 행정동과 기사를 띄워 배정
2. 행정동에 **기사 1명이면 그 동 전체를 그 기사에게 자동 배정**
3. 행정동에 **기사 2명 이상이면 브러쉬로 배정**
4. 저장하면 **기사별로 저장**
5. 지자체별 기사배정·순번은 **월 단위로 계속 저장**, 새 명단이 저장돼도 **전달 데이터를 매칭**
   · **명단은 1개월치만, 기사배정·좌표·순번은 3개월치 보관**(2026-08-27 추가 지시)
6. **기사 배정 패턴에 따라 인근 지역도 배정**
7. 저장된 지도를 **같은 소속사 담당자**가 확인
8. 자동 순번의 **교차를 줄이고 실제 도로주행 중심**으로
9. "지금 구조가 너무 어렵다" — 단계를 줄일 것

## 이미 있는 것 (다시 만들지 않는다)
| 요구 | 현재 상태 |
|---|---|
| 지자체·월 선택 | `RouteQuickModal`·`RouteSetupModal` |
| 행정동 × 기사 배정판 | `RouteSetupModal` 2단계 (`dongDriverMap`) |
| 브러쉬 배정 | `RouteMapModal` 브러쉬 모드(규칙 §18) |
| 자동 N등분·순번 | `computeAutoSplit`(R-A~R-N) · `roadAwareTSP`(DS-1~DS-18) |
| 월 단위 저장 | `route_sessions/{지자체}/months/{월}` (실측 10건 저장돼 있음) |
| 전월 승계 | `prevMonthCarryover.annotateCarryover` — 이전 기사·순번·NEW 배지 |
| 소속사별 행정동 | `org_presets/{지자체}.orgs[].dongs` |
| 기사 명부 반영 | ✅ 2026-08-27 완료(V7.19.1) |
| 소속사 선택 단계 제거 | ✅ 2026-08-27 완료(V7.19.2) |

## 실측으로 확인한 사실 (계획의 근거)
- 순번 교차 **153건**(인천 미추홀구 2026-07 · 672집)의 정체: **30m 미만 63 · 30~100m 65 · 100m↑ 25**.
  671개 구간 중 **376개(56%)가 길이 0m**(같은 건물). → 교차의 **84%는 표시 문제**, **16%가 알고리즘**.
- 같은 명단의 **좌표 이상치 0건** — 규칙 DS-15의 "교차 원인은 좌표 오류"는 이 건에 해당하지 않는다.
- 총 17.4km/672집(평균 26m) — 거리 자체는 나쁘지 않다.
- `users` 16명 중 소속(orgId) 보유 6명, 지자체 승인 10명. `route_sessions` 저장 10건.

## 단계 (각 단계마다 회귀·E2E·배포)

### Phase 1 — 완료 ✅
기사 명부 반영(비활성 제외·신규 추가) · 소속사 선택 단계 제거.

### Phase 2 — 행정동 배정 규칙 + 기사별 저장
- 행정동에 배정된 기사가 **1명이면** 그 동의 레코드 전체를 그 기사에게 자동 배정(지도 진입 시 즉시).
- **2명 이상이면** 그 동은 "브러쉬 필요"로 표시하고, 지도에서 브러쉬로 나눈다.
- 저장은 **기사별**로 남긴다(`route_sessions.drivers[]` + 레코드의 `기사`·`배송순번`).
- 위험: 기존 자동 N등분과 충돌 → 1명 동은 자동배정, 나머지만 N등분 대상으로.

### Phase 3 — 보관 분리 + 새 명단 자동 승계 ★설계의 핵심

형 지시(2026-08-27): *"명단은 1개월치만 저장이 되지만 기사배정과 좌표·순번은 3개월치가 저장이 되고,
새로운 명단이 저장되어도 안정적으로 매칭이 되도록 관리해줘."*

**왜 구조가 바뀌나**: 지금은 이름·주소(=개인정보)와 기사·순번·좌표가 `cloud_lists` 한 곳에 같이 있다.
명단을 1개월 뒤 지우면 **배정·순번도 같이 사라진다**. 그래서 둘을 갈라야 한다.

| 저장소 | 담는 것 | 보관 |
|---|---|---|
| `cloud_lists/{지자체}/months/{월}/records` | 이름·주소·연락처 등 **명단(개인정보)** | **1개월** |
| `route_assignments/{지자체}/months/{월}/records` (신설) | **매칭키 해시 + 기사 + 배송순번 + 좌표 + 행정동** | **3개월** |

- 매칭키는 **S-1~S-6 강키**(이름+생년월일 / 이름+전화끝8)를 **해시**로 저장한다 —
  매칭은 되면서 이 저장소만으로는 누구인지 알 수 없다(G-10 최소보관 취지).
  ※해시 재현이 가능해야 하므로 정규화 규칙(공백·하이픈 제거 등)을 순수함수로 고정하고 회귀로 잠근다.
- 새 명단이 저장되면 `route_assignments` 최근 3개월을 **자동 조회**해 기사·순번·좌표를 승계한다.
- 승계 못 한 건은 **미배정**으로 남기고 건수를 화면에 표시한다(임의 배정 금지 · S-5).
- 3개월 지난 배정은 정리 도구로 삭제(백업·audit — G-2).
- 좌표는 이미 `coord_cache` 에 영구 재사용 구조가 있으니 그대로 쓰고, 배정 저장소에는 **그 달의 좌표**만 둔다.

#### (구) Phase 3 — 새 명단이 들어와도 전달 배정 승계
- 월 명단 저장 시 전달 `route_sessions` + `delivery_history` 를 **자동 매칭**해 기사·순번을 승계.
- 매칭 키는 **S-1~S-6 강키 원칙**(이름+생년월일 / 이름+전화끝8)을 그대로 쓴다 — 동명이인 사고 재발 금지.
- 승계 못 한 건은 **미배정**으로 남기고 화면에 건수 표시(임의 배정 금지).

### Phase 4 — 인근 지역 패턴 배정
- 미배정 행정동을 **인접 동 + 기존 배정 패턴**으로 채운다(같은 기사 구역이 이어지도록).
- 인접 판정은 그 동 레코드 좌표의 중앙값 거리로(외부 경계 데이터 없이).
- 반드시 **제안 → 담당자 확인 → 적용** 순서(임의 배정 금지, M-1·R-0 취지).

### Phase 5 — 소속사 단위 열람
- `route_sessions` 문서에 `orgId` 기록(일부는 이미 있음) + 규칙에 **같은 소속사** 조건 추가.
- 지금은 `hasCityAccess`(지자체 승인)만 본다 → 소속사 축을 더한다.
- 규칙 회귀(에뮬레이터)로 "다른 소속사는 못 읽는다"를 잠근다.

### Phase 6 — 순번 교차
- **6-1 표시**: 같은 좌표·같은 건물 구간은 선을 묶어 한 점으로 → 화면 교차의 84% 제거.
- **6-2 알고리즘**: 도로 그룹 방문 순서를 개선해 되돌아가는 25건을 줄인다(외부 API 없이).
- 규칙 **DS-15 정정**: "좌표 오류가 유일한 원인"이라는 문장을 실측으로 고친다.

## 검증 (매 단계)
`node --test scripts/*.test.mjs` · `npm run test:rules` · `npx eslint .` · `tsc` · `vite build` · `npm run test:e2e`(운영 브라우저)
