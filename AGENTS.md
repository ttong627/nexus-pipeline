# NEXUS PIPELINE V5.0 — 프로젝트 마스터 규칙서

이 파일은 프로젝트의 모든 운영 규칙과 협업하는 **12인의 AI 최고 전문가 에이전트 팀**의 규범을 정의합니다.
코드 수정 및 협업 시 이 규칙들이 깨지지 않도록 반드시 준수해야 합니다.

---

## 👥 NEXUS PIPELINE 최고 전문가 전담 팀 (12인 정예 체제)

형님(보스)을 모시고 세계 최고의 프로그램을 완성하기 위해 가동 중인 12인의 드림팀 라인업입니다. 모든 개발 업무 및 보고는 관련 분야 에이전트들이 주축이 되어 유기적으로 협업합니다.

### 12인 전문가 프로필
*   **리더(사용자):** **"형님"** (모든 비즈니스 의사결정권자이자 세계 최고의 프로그램을 지휘하는 보스)
1.  **안토니 (Anthony):** 전체 아키텍처 설계와 프로젝트 방향성을 잡는 20년 차 기획/설계 팀장.
2.  **홀리 (Holly):** 글래스모피즘 등 압도적인 프리미엄 UI/UX 및 디자인 전담 박사.
3.  **타미 (Tommy):** 신기술 도입 및 무지연(Zero-Loading) 구현으로 생산성을 극대화하는 기술 전문가.
4.  **미아 (Mia):** 중복 없는 무결점 코드, 완벽한 에러 핸들링을 책임지는 코딩 천재.
5.  **브루마 (Bruma):** 풀스택 개발 및 최종 시스템 리소스 최적화 전담 전문가.
6.  **빌 (Bill):** 데이터베이스 스키마 설계 및 효율적인 데이터 활용을 전담하는 데이터 분석 전문가.
7.  **코코 (Coco):** 시스템 취약점 방어 및 철저한 보안 아키텍처를 설계하는 1위 해킹/보안 전문가.
8.  **미연 (Miyeon):** 기능 테스트, 사용성 검증, 엣지케이스 발굴을 전담하는 QA·UX 테스터.
9.  **에릭 (Eric) [🆕]:** 빌드 에러 해결사 (`build-error-resolver`). Vite/Firebase 빌드 에러 및 의존성 충돌 즉각 해결.
10. **도나 (Donna) [🆕]:** 문서 관리 전담 (`doc-updater`). `CLAUDE.md`, 가이드, 규칙서 등 모든 공식 지침 최신화 및 싱크 조율.
11. **재이 (Jay) [🆕]:** TDD 전도사 (`tdd-guide`). 테스트 코드 설계 가이드 및 RED-GREEN-IMPROVE 패턴 무결성 수호.
12. **베라 (Vera) [🆕]:** 검증 파이프라인 (`verify-agent`). 자동 빌드, 배포 전 최종 유효성 검사 및 6단계 보안 검증 파이프라인 관제.

---

## 기술 스택

- React 19.2 + Vite 8
- Firebase 12 (Firestore, Auth, Hosting)
- Tailwind CSS 4
- XLSX (SheetJS) — Web Worker에서 실행
- 도로명주소 API: business.juso.go.kr (3개 키 로테이션)
- 배포: `npm run build && firebase deploy --only hosting`

---

## 1. 주소 정제 규칙

| # | 규칙 | 상세 |
|---|------|------|
| A-1 | 이름 5자 초과 | 5자로 자르고 `(본명:XXX)` 를 특이사항에 추가 |
| A-2 | 오타 사전 적용 | Firestore `typo_dict` 우선 치환 (AI 자가학습). Enter키로 재정제 시 오타→정답 자동 등록 |
| A-3 | 유니코드 정규화 | NFC + 제어문자/NBSP/탭/개행 제거, 따옴표 제거 |
| A-4 | 미닫힌 괄호 제거 | `\([^)]*$` 패턴 삭제 |
| A-5 | 지역 접두어 제거 | 시/도/구/동 토큰 — 도로명·지번 만나면 제거 중단 |
| A-6 | 통·반 제거 | `제X통 제X반` 패턴 제거 |
| A-7 | 주민센터 검색어 생성 4단계 | ①행정동 컬럼 > ②분리형(XX동 주민센터) > ③부착형(청량리주민센터→청량리동) > ④시군구+주민센터 |
| A-8 | 건물명 전용 Fallback | 도로명·지번 없고 건물 유형어 있으면 `광역시도 + 시군구 + adminDong + 건물명` 순으로 재검색한다. 주소DB/JUSO/캐시 결과가 선택 지자체와 다르면 채택하지 않는다 |
| A-9 | 특수문자 분리 | `* / ☆ ★ \| ~ # § ※ **` 이후 내용 → 특이사항으로 이동 (Firestore `special_chars` 자동 확장). 명단에 적힌 내용은 삭제·임의 변경 금지, 주소 외 내용은 이동만 허용 |
| A-10 | 동호 형식 정규화 | `XXX동 YYY호 Z층` → `XXX-YYY호 Z층` |
| A-11 | 최종 주소 형식 | 기본은 `도로명주소(건물번호까지), (법정동, 건물명) 상세주소` 이며, 결과 화면의 `주소 형식` 버튼으로 `도로명주소(건물번호까지), 상세주소 (법정동, 건물명)` 형식과 상호 전환할 수 있다. 도로명주소 바로 뒤 첫 구분자는 반드시 `,` 를 사용한다. 이후 추가 구분이 필요하면 `/` 를 사용하고, `(법정동, 건물명)` 괄호 내부의 쉼표만 예외로 유지한다. 상세주소 외 명단 원문 내용은 삭제하지 않고 상세주소 또는 특이사항으로 보존 |
| A-12 | 확인필요 플래그 | 주소 공란 / 도로명 미발견+API 실패 / 변환 후 주소 3자 미만 / 타지역-지자체 벗어남 / 지번주소만 확인됨 / 주소 없음 |
| A-13 | API fallback 캐시 | 주소DB 미매칭 JUSO fallback 은 메모리(최대 1000건) + IndexedDB 2단계 캐시, 중복 요청 dedup (pendingRequests Map) 적용. 캐시 결과도 선택 지자체와 맞을 때만 사용한다 |
| A-14 | 병렬 처리 | asyncPool(20) — 500건 청크, 50건마다 메인스레드 양보 (화면 프리징 제로) |
| A-15 | 주민센터 참고주소 | 특이사항에 읍·면·동사무소, 주민센터, 행정복지센터, 복지센터가 있으면 주소 끝에 `[참고: 해당행정동 주민센터 주소]` 를 추가. 검색어는 `행정동 + 주민센터` 우선, 실패 시 행정복지센터/POI fallback 사용 |
| A-16 | API 검색어 범위 | 주소DB/JUSO 검색어는 원칙적으로 `도로명주소(건물번호까지)` 만 사용한다. 건물명, 지층/층/호, 괄호 동명은 표시 주소·상세주소로 보존하되 검색어에 붙여 정상 주소가 0건 처리되지 않게 한다 |
| A-17 | 공식 도로명주소 파서 우선 | 쉼표, 공백, 괄호 위치만 보고 주소 본문을 임의 분리하지 않는다. 먼저 공식 도로명주소 문법(`도로명 + 지하 여부 + 건물본번/부번`)으로 조회 범위를 확정하고, 그 밖의 문자열은 상세주소/특이사항으로 보존한다 |
| A-18 | 원문 구분기호 정리 | 원문 앞의 `:`, `;`, `/`, `\`, `|`, `ㆍ` 등 주소가 아닌 머리 기호는 공백 찌꺼기와 함께 삭제하고 조회어·상세주소에 넣지 않는다. 도로명주소 뒤 첫 구분자는 `,` 로 되돌리고, 그 다음 최상위 구분자만 `/` 로 바꾼다. 단, `(법정동, 건물명)` 괄호 안의 쉼표는 유지한다 |
| A-19 | 압축 부번+호수 보정 | `이문로12길 53-4303호` 처럼 `부번+호수`가 붙은 경우는 1차 조회 실패 위험이 크므로 `53-4 303호` 후보로 보정한다. 단, 자동 확정이 불안하면 확인필요로 남긴다 |
| A-20 | 주소 추정 변환 검수 | 오타 사전·학습 오타·도로명 오타·압축 부번+호수 보정처럼 시스템이 원문을 고쳐 매칭한 경우 정상 주소로 저장하되 `_주소추정`, `_추정사유`, `_원주소`를 남긴다. 담당자 확인을 위해 특이사항에 `[주소추정] 원문 → 보정값` 형태로 반드시 기록하고, 결과 화면의 `주소 추정 변환` 필터에서 따로 검수할 수 있어야 한다 |

### 1-1. 한국 도로명주소 공식 규칙 (JUSO 기준)

공식 출처: 주소정보누리집 도로명주소 소개 `introduction/RoadAddress`.
도로명주소 API와 루트맵은 아래 공식 주소 체계를 공통 기준으로 사용한다.
공식 원리와 충돌하는 임의 주소 분해 규칙을 추가하지 않는다.

| # | 규칙 | 프로젝트 적용 |
|---|------|---------------|
| J-1 | 도로명주소는 도로명, 기초번호, 건물번호, 상세주소로 건물 주소를 표기하는 제도 | 지번/건물명보다 공식 도로명주소 구조를 주소 정제와 루트맵 배정의 우선 기준으로 사용 |
| J-2 | 도로명은 주된 명사 + 도로별 구분기준 `대로`/`로`/`길` 로 구성 | 도로명 파싱 시 `대로`, `로`, `길` 을 주소 체계 토큰으로 취급 |
| J-3 | 도로명주소 기본 표기는 행정구역 + 도로명 + 건물번호 + 필요 시 상세주소 | `roadAddrPart1` 또는 정제된 도로명주소를 단지/도로 그룹 키의 우선 기준으로 사용 |
| J-4 | `대로`/`로`에서 분기되는 `길`은 분기 지점의 일련번호 또는 기초번호를 이용해 이름을 부여할 수 있음 | `OO로5길`, `OO로351번길` 의 숫자를 임의 층/동/건물번호로 오인하지 않음 |
| J-5 | 추가로 분기되는 길은 `OO로3가길`, `OO로3번나길` 처럼 가/나/다 순을 포함할 수 있음 | 도로명 내부의 `가`, `나`, `다` 를 상세주소 분리 기준으로 잘라내지 않음 |
| J-6 | 건물번호는 주된 출입구에 인접한 도로의 기초번호를 사용 | 건물명보다 도로명주소와 건물번호를 우선 신뢰하고, 건물명은 보조 폴백으로만 사용 |
| J-7 | 2개 도로에 출입구가 접하면 큰 도로의 출입구 기준으로 건물번호를 부여하는 것이 원칙 | 같은 건물이 여러 도로와 인접해 보여도 API가 반환한 공식 도로명주소를 우선 사용 |
| J-8 | 도로 시작점에서 진행방향 기준 왼쪽은 홀수, 오른쪽은 짝수 건물번호 | 루트맵에서 `대로`/`로` 본번 홀수는 좌측, 짝수는 우측 도로 측면 배정 단위로 분리 |
| J-9 | 기초번호는 도로 시작점부터 20m 간격으로 부여 | 같은 도로의 건물번호 차이는 거리/순번 추정 보조값으로만 사용하고 좌표를 대체하지 않음 |
| J-10 | 도로구간은 직진성·연속성을 고려해 서→동 또는 남→북 방향으로 설정 | 홀짝 좌우 판정은 화면 방위가 아니라 도로명주소 진행방향 기준으로 해석 |
| J-11 | 부번은 한 구간에 여러 건물이 있거나 종속구간이 있을 때 사용 | 좌우 판정은 `127-3` 의 부번이 아니라 본번 `127` 기준 |
| J-12 | 도로에 위치한 지하상가는 건물번호 앞에 `지하` 를 함께 표기 | `지하` 표기를 주소 오류나 층 정보로 삭제하지 않고 도로명주소 일부로 보존 |
| J-13 | 상세주소는 건물번호 뒤의 동·층·호 정보 | 단지/도로 그룹 키는 상세 동·층·호보다 도로명주소를 우선하고, 대형 단지 분할 때만 동 정보를 사용 |
| J-14 | 상세주소 층수는 지상은 일련번호, 지하는 `지하` 를 붙여 표기 | 층수 파싱 시 지하층 표기를 삭제하지 않고 배송 참고정보로 보존 |

### 1-2. 주소DB 설계 및 매칭 규칙 (온라인 전용)

전국 주소 매칭은 사용자 로컬 DB에 의존하지 않는다.
모든 사용자는 온라인 주소 매칭 API를 사용하고, 전국 기준주소는 서버 DB에서 일관된 버전으로 제공한다.

#### 1-2-1. 온라인 주소DB 아키텍처

| # | 규칙 | 상세 |
|---|------|------|
| AD-1 | 전국 주소 DB 저장 위치 | 전국 원천 주소 데이터는 Firestore 대량 문서나 사용자 IndexedDB 전체 적재가 아니라 `Cloud SQL PostgreSQL` 기준 DB로 구축 |
| AD-2 | 원천 파일 보관 | JUSO 전체분/변동분 원천 파일은 `Cloud Storage` 버전 폴더에 보관하고 원본은 수정하지 않음 |
| AD-3 | 조회 계층 | 웹앱은 전국 주소 DB를 직접 읽지 않고 `Cloud Run Address Matching API` 만 호출 |
| AD-4 | Firestore 역할 제한 | Firestore는 배송명단, 기사배정, 주소 DB 상태, fallback 캐시 메타, 좌표 캐시, 관리자 알림 등 운영 데이터에 사용 |
| AD-5 | 배치 적재 | 전국 전체분 적재·검증·버전 전환은 `Cloud Run Job` 배치로 수행 |
| AD-6 | 온라인 전용 | 사용자 브라우저에는 전국 주소 DB를 다운로드하지 않으며 최근 조회 캐시만 제한적으로 허용 |

#### 1-2-2. 원천 데이터 사용 규칙

| # | 규칙 | 상세 |
|---|------|------|
| AD-7 | 주소 단위 기준 | `주소DB` 를 도로명주소 주소 단위 기준 데이터로 사용 |
| AD-8 | 건물 단위 보강 | `건물DB` 를 건물명, 건물관리번호, 지번, 집합건물 동 단위 보강 데이터로 사용 |
| AD-9 | 표시주소 보강 | `도로명주소 한글` 을 사람이 보는 도로명주소 표시와 주소관리번호 연결에 사용 |
| AD-10 | 상세주소 보강 | `상세주소DB`, `상세주소 표시`, `상세주소 동 표시` 를 동·층·호 및 건물단위 상세주소 연결에 사용 |
| AD-11 | 원천/정규/캐시 분리 | 원천 적재 테이블, 정규 기준 테이블, 검색 인덱스, API fallback cache, 좌표 cache 를 분리 |
| AD-12 | 고유키 보존 | `도로명코드(12)+읍면동일련번호(2)`, `건물관리번호(25)`, `주소관리번호(26)` 를 조인과 추적 키로 보존 |

#### 1-2-3. 정규 주소DB 모델

| # | 기준 테이블 | 필수 역할 |
|---|-------------|-----------|
| AD-13 | `address_core` | 주소 단위 표준 도로명주소, 도로명, 본번/부번, 법정동, 우편번호, 건물명, 주소관리번호 보관 |
| AD-14 | `building_core` | 건물관리번호, 건물명, 도로명주소, 지번, 동 단위 건물 정보 보관 |
| AD-15 | `detail_core` | 주소관리번호/건물관리번호와 동·층·호 상세주소 연결 |
| AD-16 | `address_search_keys` | 정규화 도로명 키, 지번 키, 건물명 키, 전체 검색 키, 검색 가중치 인덱스 보관 |
| AD-17 | `address_fallback_cache` | DB 미매칭 후 JUSO API로 확정한 결과를 원본 검색어, 표준주소, 출처, 조회시각, confidence 와 함께 보관 |
| AD-18 | `address_geocode_cache` | 표준주소 또는 건물관리번호 기준 Kakao 좌표 결과와 실패 기록 보관 |

#### 1-2-4. 주소 매칭 실행 순서

```text
1. 입력 주소 정규화
2. Cloud Run Address Matching API 호출
3. Cloud SQL 전국 주소 DB 정확 매칭
4. 도로명/지번/건물명/상세주소 후보 점수화
5. 신뢰도 충분하면 표준 도로명주소 확정
6. DB 미매칭 또는 저신뢰 후보만 JUSO API fallback
7. JUSO 확정 결과는 fallback cache 에 저장
8. 표준주소 확정 후 Kakao 주소 API로 좌표 매칭
9. Kakao 주소 검색 실패 시 Kakao keyword 검색
10. 주소/좌표 결과를 운영 데이터에 반영
```

| # | 규칙 | 상세 |
|---|------|------|
| AD-19 | DB 우선 원칙 | JUSO API 직접 조회보다 전국 주소 DB 매칭을 항상 먼저 실행 |
| AD-20 | 정확 도로명 우선 | 시도·시군구·도로명·건물본번·건물부번 정확 매칭을 1순위로 사용 |
| AD-21 | 지번 매칭 | 도로명 미매칭 시 법정동·산여부·지번본번·지번부번 기반 지번 매칭 수행 |
| AD-22 | 건물명 매칭 | 건물명/아파트명 입력은 주소 후보와 건물DB 후보를 함께 점수화 |
| AD-23 | 상세주소 처리 | 동·층·호는 주소 본체 확정 뒤 상세주소 DB와 원문 상세주소를 결합 |
| AD-24 | JUSO fallback 제한 | 전국 DB 후보 없음, 후보 다수, confidence 낮음, 신규주소 의심, 오타 과다일 때만 JUSO API 사용 |
| AD-25 | Kakao 역할 제한 | Kakao는 표준주소 확정 후 좌표 매칭을 담당하며 주소 기준 DB의 진실원천이 아님 |
| AD-26 | 좌표 캐시 키 | 좌표는 `건물관리번호(25)` 우선, 없으면 정규화 표준 도로명주소 키로 캐시 |

#### 1-2-5. 주소 매칭 결과 계약

주소 매칭 결과는 기사배정과 좌표 매칭을 위해 아래 필드를 유지한다.

```text
standardRoadAddress
roadName
buildingMainNo
buildingSubNo
buildingName
buildingMgtNo
addressMgtNo
isApartment
dongName
detailAddress
zipNo
legalDong
matchSource
matchConfidence
apartmentGroupKey
buildingGroupKey
roadSideKey
lat
lng
coordSource
```

| # | 규칙 | 상세 |
|---|------|------|
| AD-27 | 단지 그룹 키 | 아파트 그룹 키는 표준 도로명주소와 건물번호 기준으로 생성하고 건물명은 보조값으로 사용 |
| AD-28 | 건물 그룹 키 | 건물 단위 통합과 상세주소 연결은 `건물관리번호(25)` 를 우선 사용 |
| AD-29 | 도로 측면 키 | 기사배정용 도로 측면 키는 J-8/J-10/J-11 기준의 도로명 + 건물본번 홀짝으로 생성 |
| AD-30 | 출처 표기 | 결과마다 `national_address_db`, `juso_fallback`, `manual`, `kakao_coord` 등 출처와 신뢰도를 구분 |

#### 1-2-5-1. 주소 표시와 매칭 메타데이터 분리

| # | 규칙 | 상세 |
|---|------|------|
| AD-30-1 | 표시 주소 표준화 | 명단 주소 열은 `도로명주소(건물번호까지), (법정동, 건물명) 상세주소` 를 기본 형식으로 사용하되 담당자 선택 시 `도로명주소(건물번호까지), 상세주소 (법정동, 건물명)` 로 전환할 수 있다. 도로명주소 뒤 첫 구분자는 `,`, 이후 최상위 구분자는 `/`, 괄호 내부 법정동·건물명 구분은 `,` 로 고정한다 |
| AD-30-2 | 괄호 정보 표시 | 주소DB 확정 건도 법정동·건물명을 주소 열 괄호에 표시하고, 같은 값은 메타데이터에도 보존 |
| AD-30-3 | 표준 메타 보존 | `standardRoadAddress`, `roadName`, `buildingMainNo`, `buildingSubNo`, `buildingName`, `buildingMgtNo`, `addressMgtNo`, `legalDong`, `detailAddress` 를 저장 |
| AD-30-4 | 기사배정 키 독립 | 단지 묶음, 건물 묶음, 도로 좌우 키는 화면 문자열 파싱보다 `_routeHints` 와 관리번호 메타데이터를 우선 사용 |
| AD-30-5 | fallback 보존 | 주소DB가 표준 표시를 확정하지 못하면 기존 주소 정제 규칙의 괄호/원문 상세정보 표현을 유지하고 확인필요 여부를 함께 판단 |
| AD-30-6 | 지자체 불일치 검출 | 명단 정제 시 선택한 `광역시도 + 시군구` 와 주소DB/JUSO 결과의 `sido + sigungu` 를 비교해 다르면 해당 후보를 주소 이식에 사용하지 않는다. 최종 확정 결과가 그래도 다르면 확인필요로 분리한다 |

#### 1-2-6. 갱신 및 알림 규칙

| # | 규칙 | 상세 |
|---|------|------|
| AD-31 | 버전 스냅샷 | 주소 DB는 기준월/기준일 snapshot 으로 빌드하고 이전 버전은 롤백 가능하게 보관 |
| AD-32 | 월 전체분 우선 | 1차 운영은 월 전체분 재빌드를 기준으로 하고, 일변동/월변동 자동 현행화는 검증 후 확장 |
| AD-33 | staging 검증 | 새 자료는 staging 적재 후 원천 건수, 필수 키, 조인 누락, 검색 샘플을 검증한 뒤 publish |
| AD-34 | 관리자 상태 | 관리자 메뉴에 기준월, 적재일, 주소/건물/상세주소 건수, fallback 비율, 좌표 실패율, 최근 오류를 표시 |
| AD-35 | 갱신 알림 | `Cloud Scheduler` 로 주소 DB 기준월 노후화와 최신자료 확인 알림을 담당자에게 발송 |
| AD-36 | 반복 실행 안전 | 적재 Job, 알림 Job, 변동분 적용 Job 은 재실행되어도 결과가 중복 오염되지 않도록 idempotent 하게 구현 |

---

## 2. DB 저장 규칙 (기본명단 — `base_lists`)

### 2-1. 3순위 매칭 시스템 (절대 변경 금지)

```
1순위: 이름 + 생년월일         → liveByBirth    (전체 레코드)
2순위: 이름 + 휴대폰           → liveByPhone    (생년월일 없는 레코드만)
3순위: 이름 + 유선전화         → liveByLandline (생년월일·휴대폰 모두 없는 레코드만)
```

### 2-2. 세부 규칙

| # | 규칙 | 상세 |
|---|------|------|
| B-1 | 저장 제외 조건 | 생년월일 + 휴대폰 + 유선전화 모두 없으면 저장 불가 |
| B-2 | 동명이인 중복 차단 | `birthKeyedNames` Set — 생년월일 있는 이름은 2순위·3순위 신규 추가 금지 |
| B-3 | 인덱스 키 정규화 | `digitKey = v.replace(/[^\d]/g, '')` — 숫자만으로 비교 (포맷 무관) |
| B-4 | 저장 포맷 | `normPhone = v.replace(/[^0-9-]/g, '')` — 대시 유지 |
| B-5 | 인플라이트 중복 | `_isInFlight` 플래그 — 같은 배치 내 신규→신규 중복 방지 |
| B-6 | 배치 커밋 단위 | **499건씩** (`writeBatch` 500 제한) |
| B-7 | 캐시 우회 | `getDocsFromServer` — 서버 최신 데이터 직접 조회 |
| B-8 | 스키마 혼재 지원 | 구버전(한국어 키: 이름/휴대폰) + 신버전(영문 키: name/mobile) 동시 지원 |
| B-9 | note 저장 | 기존 ◆ 이식 내용 제거 후 저장: `.replace(/\s*◆[^◆]*/g, '').trim()` |
| B-10 | Payload 필드 | `name, birthKey, dong, address, mobile, landline, note, driver, seqNo, sms` |
| B-11 | 감사 로그 | `audit_logs` 컬렉션에 모든 배치 저장 기록 (action, city, addCount, updateCount, adminEmail) |

---

## 3. 월별 클라우드 명단 저장 규칙 (`cloud_lists`)

| # | 규칙 | 상세 |
|---|------|------|
| C-1 | 저장 경로 | `cloud_lists/{city}/months/{YYYY-MM}/records` |
| C-2 | 중복 저장 방지 | 동월 데이터 존재 시 confirm 후 전체 삭제 → 재저장 |
| C-3 | 메타 문서 | city, monthId, totalCount, validCount, errorCount, needCheckCount, 수급자Count, 차상위Count, uploadedAt, uploadedBy |
| C-4 | 포수 기본값 | 빈값이면 1로 처리 |
| C-5 | 저장 대상 | 원본 명단 전건 저장. 오류 주소도 배송 과업 대상이므로 절대 누락 금지 |
| C-6 | 상태 분리 | `_에러` 행은 `확인필요=true`, `확인사유`, `작업상태`, `배송상태`, `좌표상태`를 함께 저장 |
| C-7 | 배송 판단 | 좌표 있는 오류 주소는 확인 경고를 유지한 채 배정 가능, 좌표 없는 오류 주소는 확인필요 묶음으로 관리 |
| C-8 | 오류 보존 | 주소 없음/타지역/지번만 확인/좌표없음 모두 이번달 명단·오류 명단·루트맵 확인 패널에서 추적 가능해야 함 |

---

## 4. 신규 데이터 이식 및 업데이트 규칙

| # | 규칙 | 상세 |
|---|------|------|
| D-1 | baseMap 키 생성 | `이름_생년월일` 또는 `이름_전화번호(숫자만)` |
| D-2 | 전체 이식 | `importFields === null` 이면 특이사항·기사·배송순번 전부 이식 |
| D-3 | 선택 이식 | `importFields` 가 Set이면 해당 필드만 이식 (note, driver, seqNo) |
| D-4 | ◆ 표시 | 기본명단 이식 특이사항에 `◆` 접두사 추가, 기존 `[기본]` 접두사 제거 |
| D-5 | 우선순위 | Excel 원본값 우선 — 빈값일 때만 기본명단에서 이식 |
| D-6 | 단건 실시간 업데이트 | 이름+생년월일 → 실패하면 이름+휴대폰 순서로 조회 후 upsert |

---

## 5. 컬럼 매핑 규칙

| 내부 키 | 감지 키워드 | 예외 |
|---------|-----------|------|
| `이름` | 이름, 성명, 대상자, 수령자명 | — |
| `주소` | 주소 | — |
| `수량(포수)` | 포수, 수량, 구입량, 포, 신청수량, 신청 수량 | **가구원** 포함 컬럼 제외 |
| `연락처` | 휴대, 연락, 전화, 유선, 핸드폰, 핸드, 모바일, 휴폰 | — |
| `행정동` | 행정동, 읍면동, 동명, 관할구역 | — |
| `비고` | 특이사항, 비고, 메모 | — |

추가 규칙:
- 헤더 행 자동 탐지: 상위 20행 중 키워드 최다 매칭 행 선택
- 병합 헤더: 최대 2행 서브헤더 통합 (데이터행 판단되면 중단)
- 시트 유형: `수급/의료/생계` → 기초수급자 / `차상위` → 차상위 / 나머지 → 기초수급자
- 혼합 시트: `구분/유형/계층/자격` 컬럼 데이터 값으로 자동 감지

---

## 6. 정렬 규칙

- **기본 정렬 (3단계 우선순위)**: 행정동 → 주소 → 이름 (모두 오름차순, 한국어 numeric)
- **컬럼 클릭 정렬**: 해당 컬럼 단일 정렬 (클릭 반복 시 asc ↔ desc 토글)

---

## 7. 특이사항 조합 규칙

조합 순서 (공백으로 join, 빈값 제외):

```
1. 주소 엔진 결과  (건물명 분리, 본명 초과, 특수문자 뒤 내용)
2. 전화번호 레이블 (레이블(번호) 형식 — 예: 딸(010-1234-5678))
3. 원본 비고 컬럼 값
4. 기본명단 이식   (◆이식내용)
```

---

## 8. 전화번호 처리 규칙

| 규칙 | 내용 |
|------|------|
| 번호+텍스트 분리 | `extractPhoneNote` — 9자리 미만이면 전화번호 아님 |
| 구분자 | 공백, /, \|, 쉼표, 세미콜론, 콜론, 괄호, 대시+텍스트 |
| 포맷 | 11자리→010-XXXX-XXXX, 10자리(02)→02-XXXX-XXXX, 9자리(02)→02-XXX-XXXX |
| 모바일 판별 | 010/011/016/017/018/019 시작 |
| 레이블 형식 | `레이블(번호)` — 예) `딸(010-1234-5678) 형님번호(010-0000-0000)` |
| 내부 중복 제거 | `parsePhoneNumbers` 에서 formatPhone 공용 함수 사용 (별도 정의 금지) |

---

## 9. 생년월일 정규화 규칙

| 입력 | 변환 결과 |
|------|----------|
| 8자리 숫자 (19750315) | 앞 2자리 버리고 `75.03.15` |
| 6자리 숫자 (750315) | `75.03.15` |
| `YY-MM-DD` / `YY/MM/DD` | `-` → `.` 치환 |
| 비정형 | 원문 반환 |

---

## 10. 문자수신 파싱 규칙

| 결과 | 패턴 |
|------|------|
| **N (거부 우선)** | 거부/거절/불가/불허/미동의/미수신/수신불가 |
| N | N, n, X, x, ×, 0, 아니오, 불, 없음 |
| Y | Y, y, O, o, ○, ㅇ, 1, 예, 동의, 수신, 가능, 허용, yes, YES |
| N | 패턴 미매칭 기본값 |

---

## 11. 중복 분석 규칙 (부가서비스)

| 유형 | 기준 | 보존 행 |
|------|------|---------|
| 강한 중복 | 이름+생년월일+전화 모두 일치 | 마지막 행 |
| 약한 중복 | 행정동+이름+전화 일치 | 특이사항 있는 행 우선, 없으면 마지막 행 |
| 특이사항 없음 | 중복 아니어도 특이사항 공란 | 삭제 추천 대상 |

---

## 12. 내보내기 파일명 규칙

```
{지자체명}-{X}월-기초{N},차상위{M},전체{T}-{MMDD}{HHMMSS}.xlsx
[확인필요]{위 형식}.xlsx
[행정동요약]{위 형식}.xlsx
```

---

## 13. Undo 규칙

- 최대 10단계 이전 상태 복원
- gridData 모든 변경은 반드시 `pushHistory` 경유

---

## 13-1. 작업 목적 기반 워크플로우 UI 규칙

명단 작업은 사용자가 처음부터 모든 기능을 보지 않도록 `workflowMode` 로 목적을 먼저 선택한다.
선택된 목적에 필요한 메뉴와 버튼만 우선 노출하고, 더 깊은 작업은 “추가 작업”으로 확장한다.

| mode | 사용자 표현 | 노출 단계 |
|------|-------------|-----------|
| `cleaningOnly` | 정제 명단 만들기 | 명단 업로드 → 주소 정제 → 오류 검토 → 저장/다운로드 |
| `geoOnly` | 위치까지 확인하기 | 명단 업로드 → 주소 정제 → 오류 검토 → 좌표 매칭 → 지도 확인 → 저장/다운로드 |
| `deliveryFull` | 배송 배정까지 하기 | 명단 업로드 → 주소 정제 → 오류 검토 → 좌표 매칭 → 기사 설정 → 기사 배정 → 배송순번 → 품질 점검 → 최종 저장 |

세부 규칙:
- Dashboard 첫 화면에는 작업 목적 카드 3종을 표시한다.
- 진행 중 작업 카드에서도 목적 변경이 가능해야 하며, 기존 정제/좌표/배정 데이터는 삭제하지 않는다.
- `cleaningOnly` 에서는 기사배정·배송순번 버튼을 기본 숨김 처리하고 “배송 작업 추가”로만 확장한다.
- 메뉴/버튼 표시 판단은 중앙 유틸 `src/utils/workflow.js` 기준을 따른다.
- 월별 명단 저장 시 `workflowMeta` 를 함께 저장해 정제 전용 명단과 배송 배정 명단을 구분한다.
- 디자인은 작업 몰입을 위해 저채도 차콜 배경, 에메랄드/시안/앰버/로즈 상태색을 사용하고 단색 계열로 화면 전체를 채우지 않는다.

---

## 14. 인증·권한 규칙

| 규칙 | 내용 |
|------|------|
| 관리자 이메일 | `ttong627@gmail.com`, `admin@logis-op.com`, `jsh6270@gmail.com` |
| 관리자 강제 보정 | admin 이메일이면 role=admin, tier=vvip 강제 설정 (onAuthStateChanged 시 자동 복구) |
| 일반 유저 강등 | 비admin이 role=admin인 경우 role=user, tier=basic으로 강등 |

---

## 15. 인트로 화면 규칙

- **표시 조건**: 신규 회원가입 + 지자체·성명 등록 완료 직후 1회만
- **이후 금지**: 같은 계정 재로그인 시 표시 안 함
- **구현**: `ProfileSetupModal` `onClose` 에서 `isNewUser === true` 였을 때만 `setShowIntro(true)`

---

## 16. 기사 배분 규칙 (루트맵)

### 제1원칙 (절대 원칙)
> **기사 구역은 완전히 분리된다. 지리적 겹침 절대 불가.**
> capacity 비례 포수 배분 · 이동거리 최소화 · 전건 배정 보장을 목표로 한다.

---

### R-0. 배타적 구역 (모든 규칙의 전제)

기사 구역은 완전히 분리된다. 동선 겹침 > 0이면 최종저장 불가.
구현: `handleSaveSession(isFinal=true)` 진입 시 `overlapCount > 0` 차단.

### R-A. 전건 배정 보장

970건 전원 배정 완료. 미배정 레코드 → 유효부담 가장 적은 기사에게 강제 배정.
구현: `routeWorker.computeAutoSplit` 마지막 보정 단계. 좌표가 없고 같은 동 투표도 없는 건은 지자체 벗어남이 아닌 한 현재 유효부담이 가장 낮은 기사에게 배정한다.

### R-B. 좌표 없으면 자동배정 잠금

좌표 미수신 건수 > 0이면 "자동 N등분" 버튼 비활성화.
좌표 매칭 실행 후 여전히 실패한 건 → 같은 동 최다배정 기사에게 자동 귀속.
구현: `hasRunGeocoding` state, `handleFetchMissingCoords` 완료 후 `true`.

### R-C. capacity 비례 포수 배분

기사별 목표 유효부담 = 전체유효부담 × (capacity / capacity 합계).
예) 3명 85%/100%/70% → 합계 255% → 373포/439포/307포.
구현: `splitContiguousLoads` 목표 유효부담 계산 (유효부담 기준).

### R-D. 절대 경계 (장벽 기반 구역 분리)

핀을 장벽 올바른 쪽에 배치하고 자동 배정 후 브러시로 경계를 마감한다.
J-8/J-10/J-11 기준으로 `대로`와 `로`는 건물번호 본번 홀수 측을 좌측, 짝수 측을 우측으로 보고 자동 배정 시 도로 측면 배정 단위를 분리한다.

| 장벽 유형 | 분류 |
|-----------|------|
| 고속도로 · 자동차전용도로 | 절대 경계 |
| 철도 · 지하철 지상구간 · 기찻길 | 절대 경계 |
| 하천 · 강 (교량 1개 이하) | 절대 경계 |
| 왕복 4차선 이상 대로 | 권장 경계 |

### R-E. 동일 주소 동일 기사

같은 주소의 복수 수급자는 반드시 같은 기사 배정.
구현: `handleAutoSplit` 후처리 — 주소별 최다배정 기사로 통일.

### R-F. 겹침 감지 → 해소 후 저장

단순히 150m 안에 다른 기사 배송지가 있다는 이유만으로 혼재로 보지 않는다. 정상적인 경계선 양쪽 배송지는 허용한다.
혼재는 아래 둘 중 하나로 판정한다.

1. 같은 아파트/동일주소/같은좌표 배정 단위가 여러 기사에게 갈라진 경우
2. 한 배정 단위 주변 180m 안에서 다른 기사 권역의 유효부담이 같은 기사 권역보다 압도적으로 큰 경우

구현: `overlapCount`, `buildAssignedRouteUnits`, `getMixedRouteUnitKeys`, `handleResolveOverlap`.
`handleResolveOverlap` 은 개별 핀을 옮기지 않고 아파트/동일주소 배정 단위 전체만 옮긴다.

### R-G. 이전달 구역 우선 승계

기사명 동일하면 이전달 좌표·기사·순번 자동 이식.
구현: `handleLoadPrevMonth` 버튼.

### R-H. 유효부담 환산

실제 배송 노동량을 반영한 가중 포수 계산.

| 조건 | 환산 |
|------|------|
| 20포↑ + 임대키워드(LH·SH·임대 등) | × 0.3 |
| 빌라·연립·다세대·단독 + 층수 | × (1 + min(층,5) × 0.1) |
| 문앞·거동불편·직접전달 등 | × 1.5 |
| 전화필수·골목·경비실 등 | × 1.2 |
| 기타 | × 1.0 |

구현: `getEffectiveLoad()` 함수.

### R-I. 아파트 단지 동일 기사 + 대형단지 분할

기본: 같은 아파트 도로명주소 전체 → 자동배정 시 하나의 배정 단위로 묶고 같은 기사 유지.
예외: 50포↑ 임대 단지는 담당자가 동(棟) 단위로 선택적 분할 가능.

구현:
- `handleAutoSplit` 전처리/후처리 — 아파트 도로명주소 그룹 동일 기사 유지
- 좌측 패널 "대형단지" 섹션 — 50포↑ 단지 목록 + 기사 드롭다운 + [분할] 버튼
- `handleAptDriverChange` — 단지 전체 일괄 기사 변경
- `openAptSplitModal` / `applyAptSplit` — 동 단위 분할 모달
- `_isApt=false` 여도 주소·건물명·표준도로명·routeHints 에 아파트/임대/LH/SH/주공/휴먼시아 등 단지 키워드가 있으면 아파트 후보로 감지한다.
- 50포↑ 아파트는 임대 키워드가 없어도 `대형단지`로 노출하고, 임대 키워드가 있으면 `임대감지`로 표시한다.
- 대형단지 분할 적용 시 클라우드 명단의 `기사`, `배송상태=대형단지분할배정`, `대형단지분할=true` 를 즉시 저장한다.
- `대형단지분할=true` 인 단지는 자동 보정 시 전체 단지로 다시 합치지 않고 동 단위 배정 묶음을 유지한다.

### R-J. 외곽 고립 배송지 묶음

주변 300m 이내 동일기사 레코드 < 3건인 고립 레코드:
1순위 → 해당 방향으로 구역이 이어지는 기사
2순위 → capacity 여유 있는 기사 (목표의 80% 미만)
3순위 → 포수 가장 적은 기사
같은 방향 외곽 2건↑ → 반드시 1명에게 묶음.

### R-K. 배송 밀집도 점수

기사별 배송순번 인접 레코드 간 평균 이동거리.
200m 미만 → 초록 / 200~400m → 노랑 / 400m↑ → 빨강.
구현: 기사 카드 하단 표시.

### R-L. 행정동 원자성

1행정동 = 1기사 원칙. 포수 과다 시에만 2기사 허용.
연속 권역 자동 배정 결과를 확인하고 필요 시 수동 조정.

### R-M. 아파트/단독 비율 균형

한 기사에게 아파트만 또는 단독만 몰리지 않도록 구성.
자동 배정 결과 확인 후 필요 시 수동 조정.

### R-N. 배송 연속성 점수

순번 인접 레코드 간 300m↑ 이동 횟수(역주행 횟수). 적을수록 좋음.
구현: 기사 카드 하단 표시.

### R-O. 배정 분석 안내

자동 배정은 결과만 적용하지 않고, 담당자가 판단할 수 있는 분석 안내를 함께 제공한다.

| 분석 항목 | 기준 |
|----------|------|
| 기사별 균등도 | 기사별 포수, 유효부담, capacity 목표부담, 목표 대비 편차율 표시 |
| 구역 섞임 위험 | 주요 도로/도로 측면이 여러 기사에게 갈라진 경우 경고 |
| 도로 경계 안내 | `대로`/`로` 본번 홀짝 좌우와 도로명 그룹을 기준으로 효율적 분할 근거 표시 |
| 핀 품질 안내 | 기사 핀이 없으면 좌표 분포/도로 연속 기준, 핀이 있으면 핀 순서 기준으로 나눈 사실을 안내 |
| 수동 보정 안내 | 편차 25% 초과, 주요 도로 다기사 분산, 핀 없음 등은 브러시/핀/대형단지 분할 보정 안내 |

구현:
- `routeWorker` 는 `clusterMap` 과 함께 `diagnostics` 를 반환한다.
- `RouteMapModal` 좌측 패널에 `배정 분석 안내`를 표시한다.
- 편차 기준은 유효부담 목표 대비 최대 절대 편차율이다.

---

### 배분 알고리즘 적용 순서

```
1. 도로주소 단위 연속 권역 균형 분할 (좌표 있는 레코드)
   - 아파트: 도로명주소 그룹을 하나의 배정 단위로 선묶음
   - 대로/로: 건물번호 홀수 좌측, 짝수 우측을 도로 측면 단위로 선묶음
   - 핀 있음: 모든 기사 핀의 주축 방향으로 기사 순서를 정하고 연속 권역을 분할
   - 핀 없음: 좌표 분포의 PCA 주축 기준 연속 구간을 capacity 비례 유효부담으로 분할
2. 좌표 없는 레코드 → 같은 행정동 최다배정 기사 자동 귀속
3. 지자체 벗어난 좌표는 자동 기사배정 대상에서 제외
4. R-I: 아파트 단지 동일 기사 통일 (전처리 배정 단위)
5. R-E: 동일 주소 동일 기사 통일 (전처리 배정 단위)
6. R-A: 미배정 전건 → 같은 권역/행정동 기준으로 보정
```

구현 위치: `handleAutoSplit`

---

## 17. 배송순번 독립 규칙

배송순번은 자동 기사배정의 일부가 아니라, 기사 배정이 끝난 뒤 별도 실행되는 독립 단계다.
자동 N등분은 `_driverId` 배정까지만 책임지고, 배송순번은 `RouteMapModal` 의 순번 엔진이 기사별로 다시 계산한다.

### DS-1. 실행 전제

| 규칙 | 내용 |
|------|------|
| DS-1-1 | 배송순번은 기사 배정 후 실행한다. `_driverId` 가 없는 레코드는 자동 순번 대상이 아니다 |
| DS-1-2 | 배송순번은 기사별로 독립 계산한다. 기사 A의 1번과 기사 B의 1번은 서로 충돌이 아니다 |
| DS-1-3 | 브러시 보정 직후 순번을 실행하면 `pendingPaintRef` 의 임시 배정을 먼저 반영한 뒤 순번을 계산한다 |
| DS-1-4 | 기존 순번이 있어도 일반 순번은 덮어쓸 수 있다. 관리자 AI 순번은 덮어쓰기 전 확인창을 띄운다 |

### DS-2. 일반 자동 배송순번 로직

구현 위치: `RouteMapModal.handleAutoSequence()`

```text
1. pendingPaintRef 임시 배정을 records 에 먼저 반영
2. 기사별로 `_driverId === driver.id` 인 레코드만 추출
3. 도로명 또는 아파트 정보가 하나라도 있으면 roadAwareTSP(driverRecs) 실행
4. 도로명/아파트 정보가 없고 좌표만 있으면 nearestNeighborTSP(coordRecs) fallback
5. 계산 결과를 해당 기사 안에서 1부터 순차 부여
6. analyzeSequenceQuality() 실행 후 순번 분석 패널 표시
```

### DS-3. roadAwareTSP 기준

| 단계 | 기준 |
|------|------|
| DS-3-1 | 도로명·아파트·동일주소를 배송순번 단위로 묶는다 |
| DS-3-2 | 같은 도로 안에서는 건물번호 흐름을 우선한다 |
| DS-3-3 | 시작 도로는 가장 북쪽 도로 그룹을 우선한다 |
| DS-3-4 | 다음 도로 그룹은 최근접 이웃 방식으로 선택한다 |
| DS-3-5 | 2-opt 로 교차 동선을 줄인다 |
| DS-3-6 | 각 도로 그룹은 이전 도착점에서 더 가까운 진입 방향(정방향/역방향)을 선택한다 |
| DS-3-7 | 좌표 없는 레코드는 거리 최적화에 직접 쓰지 않고 뒤쪽 확인 대상으로 붙인다 |

### DS-4. nearestNeighborTSP fallback 기준

| 단계 | 기준 |
|------|------|
| DS-4-1 | 도로명/아파트 정보가 부족한 기사 명단은 좌표 기반 최근접 이웃 순회로 계산한다 |
| DS-4-2 | 시작점은 가장 북쪽 좌표를 우선한다 |
| DS-4-3 | 매 단계에서 현재 지점과 가장 가까운 미방문 좌표를 다음 순번으로 선택한다 |
| DS-4-4 | 2-opt 교차 제거로 총 이동거리를 줄인다 |
| DS-4-5 | 좌표가 없는 레코드는 좌표 기반 fallback 순번 대상에서 제외하고 분석 경고로 남긴다 |

### DS-5. 수동 순번 편집

| 기능 | 규칙 |
|------|------|
| 클릭 순번 | 목록 행을 클릭하는 순서대로 현재 기사 안에서 1→2→3 순번을 부여한다 |
| 드래그 순번 | 같은 기사 안에서만 드래그 재정렬을 허용하고, 재정렬 후 1부터 다시 번호를 매긴다 |
| 직접 입력 | 목록의 배송순번 입력칸에서 수동 수정할 수 있다 |
| 기사 간 이동 | 순번 드래그는 기사 간 이동을 허용하지 않는다. 기사 변경은 배정 기능으로 처리한다 |
| 전체 초기화 | 배송순번 전체 초기화는 모든 `배송순번` 을 빈값으로 만든다 |

### DS-6. 관리자 AI 순번

| 규칙 | 내용 |
|------|------|
| DS-6-1 | 관리자 전용 기능이다 |
| DS-6-2 | 기사 배정이 1건 이상 있어야 실행한다 |
| DS-6-3 | 기존 배송순번이 있으면 덮어쓰기 확인창을 띄운다 |
| DS-6-4 | Kakao REST 키가 있으면 차량 경로 후보를 활용하고, 없으면 로컬 거리 계산으로 fallback 한다 |
| DS-6-5 | 실행 후 예상 정확도, 확인 필요 건수, Kakao/local 경로 계산 비율을 표시한다 |

### DS-7. 순번 품질 분석

구현 위치: `RouteMapModal.analyzeSequenceQuality()`

| 분석 항목 | 기준 |
|----------|------|
| 점프 | 인접 배송순번 간 300m 이상 이동이며, 같은 아파트/같은 도로측면이면 경고를 완화한다 |
| 도보 후보 | 인접 배송순번 간 120m 이하 이동 |
| 좌표 없음 | 순번은 있으나 좌표가 없어 거리 검증이 불가능한 레코드 |
| 도로 정보 없음 | 도로명 파싱이 불가능하고 아파트 후보도 아닌 레코드 |
| 도로 재방문 | 이미 지나간 도로가 순번 뒤쪽에서 다시 등장하는 경우 |
| 평균 이동거리 | 기사별 인접 순번 간 평균 이동거리 |
| 최대 이동거리 | 기사별 인접 순번 간 최대 이동거리 |
| 예상 정확도 | 점프·좌표없음·도로없음·도로재방문은 감점, 도보 후보는 소폭 가점 |

품질 기준:

```text
평균 이동거리 200m 미만  → 좋음
평균 이동거리 200~400m → 주의
평균 이동거리 400m 이상 → 위험
인접 순번 300m 이상 점프 → 확인 필요
```

### DS-8. 저장·이식·내보내기

| 구분 | 규칙 |
|------|------|
| 기본명단 | `seqNo` 로 저장하고, 전체 이식 또는 `seqNo` 선택 이식 때만 가져온다 |
| 월별 명단 | `cloud_lists/{city}/months/{YYYY-MM}/records` 에 `배송순번` 으로 저장한다 |
| delivery_history | 전월 비교·복원용으로 `seqNo` 를 함께 저장한다 |
| 이전달 승계 | 기사명이 같으면 이전달 기사·좌표·배송순번을 복원할 수 있다 |
| 기사 요청 반영 | 기사 공유 링크의 순번 요청은 담당자 유선 확인 후 해당 기사 배송순번만 공식 반영한다 |
| 내보내기 | 배송표는 `배송순번` 오름차순으로 정렬하고, 순번 없는 행은 원래 순서를 최대한 유지한다 |

---

## 18. 루트맵 UI 조작 규칙 (RouteMapModal)

### 자동 배정 버튼

"자동 N등분" 단일 버튼. 도로주소 단위 연속 권역 균형 분할 + R-A~R-I 후처리 일괄 실행.
좌표 미수신 > 0이고 좌표매칭 미실행 시 버튼 잠금 (R-B).

### 기사 핀 (거점 배치)

- 기사 카드의 [핀 꽂기] 버튼 클릭 → 지도 클릭 1회로 거점 위치 확정 (이후 클릭 무시)
- 핀 = 기사 구역의 좌우/상하 순서를 정하는 기준
- 핀 전체 있으면 핀 주축 순서대로 연속 권역을 분할하고, 없으면 좌표 분포 주축으로 연속 권역 균형 분할
- 핀 제거: [×] 버튼 클릭

### 연속 권역 균형 기준

```text
targetLoad = 전체유효부담 × (capacity / 전체capacity)

아파트 도로명주소 그룹, 대로/로 좌우 측면, 같은좌표 묶음을 배정 단위로 만든 뒤
지도 좌표의 PCA 주축으로 정렬하여 각 기사 targetLoad에 가까운 연속 구간을 배정한다.
가까운 점을 하나씩 따라가는 TSP식 순회 경로는 자동배정 경계 산정에 사용하지 않는다.
TSP식 순회는 밀집 지역에서 구역을 뱀처럼 왕복시켜 색이 섞일 수 있으므로 배송순번 계산에만 사용한다.
```

도로명주소로 묶을 수 없는 아파트는 다른 단지와 자동 통합하지 않는다.
`getEffectiveLoad()` 가중치(임대 0.3×, 계단 층수 보정, 문앞 1.5× 등) 그대로 반영.

### 페인트 브러시 모드 (2차 수동 보정)

- 기사 카드의 [브러시] 버튼 클릭 → 해당 기사 색상 페인트 모드 활성
- 지도 위에서 마우스 드래그 → 브러시 반경 내 모든 핀을 선택 기사로 일괄 재배정
- 반경 슬라이더로 브러시 크기 조절
- [브러시 종료] 버튼 또는 다른 기사 선택 시 모드 해제

### 대형단지 패널 (좌측 패널 하단)

50포↑ 아파트 단지 목록. 기사 드롭다운으로 단지 전체 일괄 변경.
[분할] 버튼 → 동 단위 분할 모달.

### 배정 관리

- **배정 전체 초기화**: 모든 `_driverId → null`. Undo 없으므로 신중히 사용
- **핀 우클릭**: 개별 핀 미배정 전환
- **최종저장 잠금**: 겹침 > 0건이면 차단 (R-0)

### 좌표 오류 지정 및 재매칭

- 사용자가 좌표가 잘못 찍혔다고 판단하면 상단 `좌표 오류 지정` 도구를 연다.
- 지도 핀 클릭, 목록 클릭, 또는 브러시 드래그로 잘못된 좌표를 선택한다.
- `삭제 적용`은 좌표를 제거하고 `좌표오류지정=true`, `좌표상태=좌표없음`, `배송상태=주소확인필요` 로 남긴다.
- `삭제 후 재매칭`은 기존 좌표를 `이전좌표` 로 보존한 뒤 도로명·지번·원본주소 기준으로 강제 재조회한다.
- 재매칭 성공 후 같은 지자체 안에서 행정동만 다르면 `배정행정동` 을 실제 확인 행정동으로 저장하고 `이관필요=true`, `배송상태=타동이관필요` 로 표시한다.
- 재매칭 후 지자체까지 다르면 자동 기사배정 대상에서 제외하고 `좌표검증상태=지자체벗어남`, `배송상태=타지자체확인필요` 로 담당자가 확인하게 한다.
- 기사 배정·행정동 필터·동별 기사 구성은 원본 `행정동` 보다 `배정행정동` 을 우선 사용한다.

---

## 19. 코드 보호 원칙

- 기존 작동 기능 임의 변경 금지
- 배치 499 제한 절대 초과 금지
- 3순위 매칭 시스템 구조 변경 시 전체 인덱스 재검토 필수
- `getDocsFromServer` → `getDocs` 로 교체 금지 (캐시 오염 위험)
- `pushHistory` 경유 없이 `setGridData` 직접 호출 금지 (Undo 파괴)
- 작업 완료 후 항상 `npm run build && firebase deploy --only hosting` 실행

## 20. 배포 버전 자동 증가 규칙

- Hosting 배포는 `firebase deploy --only hosting` 또는 `npm run deploy`를 사용한다.
- Firebase Hosting `predeploy`에서 `npm run version:bump` 후 `npm run build`를 자동 실행한다.
- `version:bump`는 `package.json`, `package-lock.json`, `src/version.js`의 버전과 빌드일을 함께 갱신한다.
- 기본 증가는 patch 버전이다. 필요 시 `VERSION_BUMP=minor npm run version:bump`, `VERSION_BUMP=major npm run version:bump`로 수동 승급한다.
- 화면 표시는 `src/version.js`의 `APP_VERSION`을 기준으로 하며, 배포된 화면과 패키지 버전이 달라지지 않게 유지한다.
