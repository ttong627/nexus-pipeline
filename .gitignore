# Secrets — never commit
.env
.env.*
!.env.example
# 서비스계정 키 (import 스크립트용) — 절대 커밋 금지
serviceAccountKey.json
**/serviceAccountKey.json
*serviceAccount*.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.xls
*.xlsx

# Firebase hosting cache (배포 시 자동 생성)
.firebase/

# AI/ML 학습 캐시 (로컬 전용)
unsloth_compiled_cache/

# Nationwide address source dumps (large local import files)
202604_*/
202605_*/
juso/
*.zip

# Local lint/report artifacts
lint-report.json

# 로컬 데이터 준비용(명단 원본·스크립트) — 저장소 제외
특이사항_기본명단/
