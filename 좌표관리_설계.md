# 좌표 관리 설계서 — 입구 좌표 / 동(棟) 좌표 2종 구분 관리

> 형 지시 2026-08-11:
> *"좌표 표시 DB와 연계 프로그램도 있어서 좌표는 명단의 모든 건물이 다 받아와야 하고
> 동 좌표까지 받아서 별도로 저장해줘야해. **입구 좌표와 동좌표 2개를 구분 관리** 해줘야해 아파트인경우는"*
>
> 작성: 2026-08-11 · 적대적 진단(실측) 기반 · 운영 DB `logis-op:asia-northeast3:nexus-address-pg`

---

## 0. 요약 — 세 줄

1. **출입구 좌표는 존재하지 않는다.** `entrance_core` 테이블 자체가 없다. 조회 코드(`findEntrance`)는 항상 `null`을 반환해 왔다.
2. **동(棟) 좌표는 살아 있다.** VWorld로 2,375건이 실제로 쌓여 있고 층수까지 있다. 다만 **입구 좌표와 같은 테이블에 섞여** 있어 형이 요구한 "2개 구분 관리"가 성립하지 않는다.
3. **주소 본체가 결손이다.** `building_core` 1,072만 건 중 **831만 건이 `address_core`에 없다**. 좌표를 어디에 매달지부터 이 결손에 걸린다.

---

## 1. 적대적 진단 — 기록을 믿지 않고 직접 셌다

`PROJECT_STATUS.md`에는 *"출입구 좌표 1,281만 건을 적재하고도 읽는 코드가 0건이었다"* 고 적혀 있다.
읽는 코드를 붙였는데도 전부 빈 값이 나와서, **적재 주장 자체를 검증**했다.

진단 도구: `services/address-service/scripts/diag-coords.mjs` (읽기 전용, Cloud Run Job으로 DB 내부에서 실행)

### 1-1. 실측 결과 (2026-08-11, version 202605)

| 항목 | 실측값 | 판정 |
|---|---|---|
| `entrance_core` | **테이블 없음** | 🔴 출입구 좌표 경로 전면 불가 |
| `building_ext` (엘베·층수) | **테이블 없음** | 🔴 부가정보 없음 |
| `entrance_load_runs` / `building_ext_runs` | **테이블 없음** | 🔴 **한 번도 적재된 적 없음** |
| `address_core` | 986,737 | 🟠 결손 |
| `building_core` | 10,722,592 | 🟢 |
| `address_building_links` | 6,420,025 | 🟢 |
| `detail_core` | 36,857,626 | 🟢 |
| **`address_core`에 없는 건물** | **8,311,373** | 🔴 77.5% 결손 |
| `address_geocode_cache` | 10,554 | 🟡 유일한 좌표 저장소 |
| ├ provider=kakao | 7,959 (좌표 7,947) | |
| ├ provider=vworld | 2,595 (좌표 2,595) | |
| ├ **동(棟) 좌표 `dong:*`** | **2,375** (dong_no·floors 전부 채워짐) | 🟢 작동 중 |
| └ 일반 주소 좌표 | 8,179 | |

동 좌표 실제 예시:
```
경기도 시흥시 장곡로53번길 10 | 동 207 | 17층 | vworld
경기도 시흥시 신천로 89-20   | 동 나  |  4층 | vworld
경기도 시흥시 매화2로32번길 4-1 | 동 2  |  5층 | vworld
```

### 1-2. 기록과 실제의 불일치

| 문서 주장 | 실제 | 왜 어긋났나 |
|---|---|---|
| "출입구 1,281만 건 적재" | 테이블 없음 | 적재기 코드·테스트는 만들었으나 **운영 DB에 실행한 적 없음**. `load-juso-entrc.mjs`는 자료 폴더를 인자로 받는 수동 실행기이고, 그 실행이 일어나지 않았다 |
| "읽는 코드가 0건이라 못 썼다" | 읽는 코드는 있고 **읽을 데이터가 없다** | 진단 순서가 반대였다. 읽기를 붙이기 전에 데이터 존재를 확인했어야 한다 |

> **교훈(설계에 반영)**: 문서의 "적재 완료" 주장은 근거가 아니다.
> 이후 모든 단계에 **행수 실측 게이트**를 넣는다(§6).

### 1-3. 파생 결함 — `address_core` 77.5% 결손

`exactRoadMatch`는 `address_core`를, `exactBuildingRoadMatch`는 `building_core`를 본다.
실제로 `안암로20길 4`는 **`address_core`에 없고** `building_core`로 매칭됐다.

이게 좌표 설계에 직접 영향을 준다:

- `findEntrance` 1차 경로는 `address_mgt_no`로 조인하는데, 이 키는 **`address_core`에만 있다**.
  → 831만 건에 대해 **1차 경로는 원천적으로 못 쓴다**.
- 따라서 좌표의 앵커(anchor)는 `address_mgt_no`가 아니라
  **`(road_code, underground_yn, building_main_no, building_sub_no)`** 여야 한다.
  이 키는 `address_core`·`building_core` 양쪽에 다 있다.

---

## 2. 요구사항 해석

형 지시를 네 가지로 분해한다.

| # | 요구 | 해석 |
|---|---|---|
| R1 | "명단의 **모든 건물**이 다 받아와야" | 명단에 등장하는 모든 고유 건물에 좌표가 있어야 한다. 일부만 있으면 배송순번이 그 구간에서 무너진다(DS-15 실측: 좌표 오류 379건이 기사 구역을 430km로 부풀렸다) |
| R2 | "동 좌표까지 받아서 **별도로 저장**" | 동(棟) 좌표를 입구 좌표와 **다른 자리**에 보관한다. 한 칸에 덮어쓰면 무엇이 들어있는지 알 수 없다 |
| R3 | "**입구 좌표와 동좌표 2개를 구분 관리**" | 같은 건물에 대해 두 좌표가 **동시에** 존재할 수 있어야 하고, 쓰는 쪽이 골라 쓸 수 있어야 한다 |
| R4 | "아파트인 경우는" | 동 좌표는 **아파트(단지형)에만** 해당한다. 단독·상가는 입구 좌표 하나뿐이다 |

### 2-1. 두 좌표는 왜 다른가 (섞으면 안 되는 이유)

```
아파트 단지
   ┌─────────────────────────────┐
   │  ①단지 정문(입구 좌표)         │ ← 차량이 진입하는 지점
   │       ↓ 단지 내부 도로          │
   │   [101동]  [102동]  [103동]   │ ← ②각 동 좌표(VWorld LT_C_SPBD)
   └─────────────────────────────┘
```

- **입구 좌표**로 순번을 짜면 단지가 한 점으로 뭉쳐 **단지 내부 동선이 사라진다**.
- **동 좌표**로 내비를 찍으면 차가 못 들어가는 지점을 목적지로 준다.
- 실측(2026-07-24): 은마아파트 18·23·28·30동이 **동간 약 280m** 떨어져 있다.
  단지 하나를 한 점으로 보면 그 280m가 통째로 사라진다.

→ **용도가 다르므로 둘 다 필요하고, 반드시 분리 보관한다.**

---

## 3. 설계

### 3-1. 저장 구조 — `building_coord` (신설, 버전독립)

```sql
-- sql/coords.sql  (★schema.sql 에 두지 말 것: 월 재적재 때 통째로 지워진다)
CREATE TABLE IF NOT EXISTS building_coord (
  -- 앵커: address_mgt_no 가 아니라 도로코드+본번-부번. address_core 77.5% 결손 때문.
  coord_key        text PRIMARY KEY,          -- {road_code}#{underground_yn}#{main}-{sub}
  road_code        text NOT NULL,
  underground_yn   text NOT NULL DEFAULT '0',
  building_main_no integer NOT NULL,
  building_sub_no  integer NOT NULL DEFAULT 0,
  road_address     text NOT NULL,             -- 사람이 읽는 확인용(디버깅·리포트)
  sigungu          text,
  legal_emd        text,
  building_name    text,
  is_apartment     boolean NOT NULL DEFAULT false,

  -- ① 입구 좌표 (차량 진입 지점) — 모든 건물
  entrance_lat     double precision,
  entrance_lng     double precision,
  entrance_source  text,                      -- juso_entrc | vworld | kakao | manual
  entrance_at      timestamptz,

  -- ② 대표 좌표 (건물 중심) — 지도 표시·거리계산 폴백
  center_lat       double precision,
  center_lng       double precision,
  center_source    text,
  center_at        timestamptz,

  quality          text NOT NULL DEFAULT 'unverified',  -- ok | outlier | unverified | none
  quality_note     text,
  updated_at       timestamptz NOT NULL DEFAULT now()
);

-- 동(棟) 좌표는 건물 1:N 이므로 별도 테이블 (R2·R3)
CREATE TABLE IF NOT EXISTS building_dong_coord (
  coord_key    text NOT NULL,                 -- building_coord.coord_key
  dong_no      text NOT NULL,                 -- '101' · '가' · 'B'  (정규화된 동 표기)
  lat          double precision NOT NULL,
  lng          double precision NOT NULL,
  floors       integer,                       -- 지상층수(VWorld gro_flo_co) — R-H 유효부담에 직접 사용
  build_name   text,                          -- VWorld buld_nm_dc 원문(검증 흔적)
  source       text NOT NULL DEFAULT 'vworld',
  matched      text,                          -- dong | complex | centroid  (매칭 강도)
  updated_at   timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (coord_key, dong_no)
);

CREATE INDEX IF NOT EXISTS building_coord_pending
  ON building_coord (updated_at) WHERE entrance_lat IS NULL;
CREATE INDEX IF NOT EXISTS building_dong_coord_key
  ON building_dong_coord (coord_key);
```

**설계 결정과 근거**

| 결정 | 근거 |
|---|---|
| 앵커 = `road_code#지하#본번-부번` | `address_mgt_no`는 `address_core`에만 있어 831만 건에서 못 쓴다(§1-3) |
| 입구/중심/동 **세 자리로 분리** | 형 지시 R2·R3. 한 칸에 덮어쓰면 "이게 입구인지 동인지" 영영 알 수 없다 |
| 동 좌표를 **별도 테이블** | 건물 1 : 동 N 관계. 컬럼으로 만들면 동이 늘 때마다 스키마가 바뀐다 |
| `source`를 좌표마다 기록 | 출처별 신뢰도가 다르다. 나중에 출처별로 품질을 재평가해야 한다 |
| **버전독립** (`sql/coords.sql`) | `schema.sql` 테이블은 월 재적재 때 `resetVersionData()`가 지운다(import-job.js:294) |
| `quality` 컬럼 | DS-15 좌표 이상치 검증 결과를 여기에 적어 재사용한다 |

### 3-2. 좌표 획득 파이프라인 — 4단계 폴백

```
                     ┌──────────────────────────────────────┐
  명단의 고유 건물 →   │  ① 캐시(building_coord) 조회          │ → 있으면 끝
                     └──────────────────────────────────────┘
                                    ↓ 없음
                     ┌──────────────────────────────────────┐
                     │  ② 행안부 출입구 자료(entrance_core)    │ → 입구 좌표(최상급)
                     └──────────────────────────────────────┘
                                    ↓ 없음 / 테이블 미적재
                     ┌──────────────────────────────────────┐
                     │  ③ VWorld 지오코더 + LT_C_SPBD         │ → 중심 좌표 + 동별 좌표·층수
                     └──────────────────────────────────────┘
                                    ↓ 실패
                     ┌──────────────────────────────────────┐
                     │  ④ Kakao 주소검색                      │ → 중심 좌표
                     └──────────────────────────────────────┘
                                    ↓ 전부 실패
                              quality='none' 로 기록 + 확인필요 리포트
```

- **아파트(`is_apartment`)일 때만** ③에서 동별 조회를 추가로 돈다(R4). 단독·상가에 동 조회를 돌리면 쿼터만 태운다.
- **좌표를 못 구해도 행은 만든다**(`quality='none'`). 그래야 "아직 안 해봤다"와 "해봤는데 없다"가 구분된다.

### 3-3. 쿼터 계산 (적대적 검토)

| 출처 | 일 한도 | 건당 호출 |
|---|---|---|
| VWorld | 40,000 | 지오코딩 1 + BBOX 1 = **2** (아파트는 동 수와 무관하게 BBOX 1회로 전 동 취득) |
| Kakao | 300,000 | 1 |

명단 1건당 고유 건물은 실측상 약 40~60%(같은 아파트에 여러 세대).
**7,413명 명단 → 고유 건물 약 3,000~4,500 → VWorld 호출 약 6,000~9,000회.**
→ 일 한도 내. 다만 **여러 지자체를 하루에 몰아 돌리면 한도에 걸린다** → §3-5 배치에 **일일 쿼터 카운터**를 둔다.

### 3-4. API

```
POST /v1/coords/resolve
  { records: [{ roadAddress, buildingName?, dongNo?, sigungu }], mode: 'cache'|'fill' }
  → { coords: [{ coordKey, entrance:{lat,lng,source}, center:{lat,lng,source},
                 dong:{no,lat,lng,floors}|null, quality }] }

GET  /v1/coords/status?sigungu=...
  → { total, withEntrance, withCenter, withDong, none, pendingSample[] }
```

- `mode:'cache'` = 조회만(빠름, 정제 화면용). `mode:'fill'` = 없으면 외부 API로 채움(배치용).
- **정제 경로에서는 절대 `fill`을 쓰지 않는다.** 정제 중 외부 API를 태우면 화면이 멈춘다(무지연 원칙).

### 3-5. 연계·정기 갱신

기존 `nexus-address-sync` Job(매일 04:23)에 단계를 추가한다.

```
① 스키마 보장 → ② 출입구 연계자료 적재(전체분→일변동) → ③ 학습주소 재확인
→ ④ 편입 표시 → ⑤ [신설] 좌표 미보유 건물 채우기(쿼터 상한 내) → ⑥ [신설] 좌표 이상치 검증
```

- ⑤는 **일일 상한**(기본 VWorld 20,000 / Kakao 50,000)을 두고 남은 건 다음 날로 넘긴다.
- ⑥은 `src/engine/coordValidator.js`의 `detectCoordOutliers`(중앙값 기준 25km)를 재사용해
  이상 좌표를 `quality='outlier'`로 표시하고 **좌표를 지우지 않는다**(원본 보존, 담당자 판단).

---

## 4. 적대적 검토 — 이 설계가 실패할 수 있는 지점

각 항목은 "이렇게 하면 조용히 망가진다"는 시나리오와 방어책이다.

| # | 실패 시나리오 | 왜 위험한가 | 방어 |
|---|---|---|---|
| F1 | 출입구 자료가 끝내 안 들어온다 | ②단계가 영원히 비어 입구 좌표가 안 생기는데, 파이프라인은 ③으로 넘어가 **중심 좌표를 입구 좌표인 척** 채운다 | `entrance_source`를 반드시 기록하고, `vworld/kakao` 출처는 **`center_*`에만** 쓴다. 입구 좌표 칸은 출입구 자료·수동 확인만 채울 수 있다 |
| F2 | 동 좌표를 입구 좌표 대신 쓴다 | 차가 못 들어가는 동 앞을 목적지로 준다 | 내비 링크(`buildNavigationLinks`)는 **입구 → 중심 → 동** 순으로만 고른다. 동 좌표는 **순번 계산·지도 표시 전용** |
| F3 | 같은 단지에 같은 동번호가 둘 | 인접 단지 오염(2026-07-27 실측: "306동이 320동 위치에") | `coord_key`가 도로코드+번지라 단지가 다르면 키가 다르다. VWorld 조회 시 단지명 필터(`pickDong`) 유지 |
| F4 | 좌표가 일부만 채워진 채 순번을 돌린다 | 좌표 없는 건이 뒤로 밀려 **기사 구역이 찢어진다**(R-B가 자동배정을 잠그는 이유) | `/v1/coords/status`로 **미보유 0 확인 후** 순번 실행. 미보유가 있으면 화면에 건수 배지 |
| F5 | 이상 좌표를 그대로 쓴다 | 기사 구역 430km 부풀림(DS-15 실측) | ⑥ 검증 단계에서 `quality='outlier'` 표시. 순번 엔진은 `outlier`를 좌표 없음으로 취급 |
| F6 | 월 재적재로 좌표가 증발 | 몇 달 쌓은 좌표가 한 번에 사라진다 | **버전독립 테이블**(`sql/coords.sql`) + `resetVersionData()` 목록에 넣지 않는다. 회귀 테스트로 못 박는다 |
| F7 | 쿼터 소진으로 배치가 조용히 멈춤 | 로그를 안 보면 좌표가 안 늘어나는 걸 모른다 | 일일 카운터 + 요약 로그에 `쿼터 소진으로 N건 이월` 명시. 0건 진행이 2일 연속이면 경고 |
| F8 | 좌표가 공유링크로 유출 | 좌표는 **개인의 집 위치**다 | 기존 KML PII 차단 규칙 유지. `startAddr/startLat/startLng` 외부 출력 금지(DS-9-5)와 동일 등급으로 취급 |
| F9 | "적재했다"는 기록만 믿고 다음 단계로 | 이번 사고가 정확히 그것이다 | 각 단계 종료 시 **행수를 세어 로그에 남기고**, 0이면 실패로 처리(§6) |
| F10 | 정제 중 좌표 API를 호출 | 화면이 멈춘다(무지연 원칙 위반) | 정제 경로는 `mode:'cache'`만 허용. `fill`은 배치·루트맵 전용 |

---

## 5. 단계별 실행 계획

각 단계는 **독립 배포 가능**하고, 끝에 실측 증거를 남긴다.

| 단계 | 내용 | 산출물 | 검증 기준 |
|---|---|---|---|
| **C-1** ✅ | 스키마 신설 | `sql/coords.sql`, 적용 Job | 두 테이블 생성 확인, 행수 0 확인 |
| **C-2** ✅ | 좌표 저장소 모듈 + 조회 API | `src/coords/coordStore.js`, `POST /v1/coords/resolve(cache)` | 순수 함수 회귀 20건+, 캐시 히트/미스 동작 |
| **C-3** ✅ | 채움 파이프라인(②③④ 폴백) | `mode:'fill'`, 쿼터 카운터 | **중심 좌표 1,543/1,543 (100%)** — 5-2 |
| **C-4** ✅ | 동 좌표 이관 | 기존 `address_geocode_cache`의 `dong:*` **2,375건**을 `building_dong_coord`로 이관 | 이관 전후 건수 일치, 샘플 10건 좌표 동일 |
| **C-5** 🟡 | 클라 연동 | 배치 조회 ✅ · 미보유 경고 ✅ · 동 좌표 채움 ⏳ | 아래 5-3 |
| **C-6** | 정기 배치 편입 | `sync-address-data.mjs` ⑤⑥ 추가 | Job 1회 실행 로그에 채움·검증 건수 |
| **C-7** | 출입구 자료 적재 | `load-juso-entrc.mjs` 실행 (**자료 파일 필요 — 형 확인 필요**) | `entrance_core` 행수 > 0, 조인 성공률 실측 |

> **C-7은 형의 확인이 필요하다.** 행안부 출입구 자료(`RNENTDATA`·`AlterD.JUSUEC`)는 계정 인증으로
> 내려받는 파일이다. 파일이 어디에 있는지(또는 받을 수 있는지) 알려주셔야 진행된다.
> **C-1~C-6은 그것 없이도 전부 진행 가능**하다 — 입구 좌표 칸이 비어 있을 뿐, 중심·동 좌표는 채워진다.

### 5-1. C-1·C-2·C-4 실측 증거 (2026-08-11, version 202605)

주장이 아니라 로그다. 재현: `gcloud run jobs execute nexus-address-verify --region asia-northeast3 --project logis-op --wait`

| 항목 | 실측값 | 의미 |
|---|---|---|
| `building_coord` / `building_dong_coord` | 생성됨 | C-1 완료 |
| 동 좌표 이관 | **2,373 / 2,375** (앵커 실패 2) | C-4 완료 — 원본은 지우지 않음 |
| 오염 격리(`matched='suspect'`) | 375건 강등 → **잔여 오염 0** | 같은 좌표가 건물명 다른 여러 건물에 붙은 건(F3) |
| 배송에 쓰이는 동 좌표(`matched='dong'`) | 1,998행 / 1,261건물 | |
| **앵커 왕복 일치** | **300 / 300 (100%)** | 조회(road_codes)와 이관(address_core·building_core)이 **같은 키**를 만든다 |
| 동 좌표 회수 | 5 / 5 | 실주소 조회에서 층수까지 정상 |
| 없는 주소 조회 | `quality='no_anchor'`, 좌표 없음 | **주소를 지어내지 않는다**(A-36) |
| 중심 좌표 보유 | **0** | C-3 채움 전이라 정상 |

> **왜 앵커 왕복을 셌나**: 이관과 조회는 서로 **다른 출처**로 `road_code`를 구한다
> (이관=`address_core`/`building_core`, 조회=`road_codes` — `address_core`는 77.5% 결손이라 못 쓴다).
> 어긋나면 조회는 **영원히 빈손이면서 에러도 안 난다**. A-35에서 겪은
> "키가 어긋나 학습분이 안 걸리던" 사고와 같은 종류라 배포 전에 셌다.

회귀로 못 박은 것(`scripts/db-guard.test.mjs` — Red-Green 확인 완료):
좌표 테이블 버전독립(F6) · 지오코딩이 입구 칸을 못 채움(F1) · 조회 경로에 외부 API 없음(F10).

---

### 5-2. C-3 현재 상태 (2026-08-11) — 코드 완료 · 실측 대기

**코드는 들어갔고, §6 완료 판정은 아직 못 세웠다.** 채움률·출처 분포는 운영 DB에
붙어야 나오는 숫자라 Cloud Run Job 실행이 필요하다(형 확인 후).

| 한 것 | 증거 |
|---|---|
| **동 좌표 오염 재발 차단** — `pickDong` 의 `\|\| byDong[0]` 폴백 제거 | Red-Green 실측: 폴백을 되살리면 오염차단 테스트 2건이 정확히 실패 |
| 채택 규칙 SSOT `acceptDongCandidate` (단일문자 동은 단지명 검증 필수) | `scripts/coord-fill.test.mjs` 26건 |
| `mode:'fill'` 개통 + 쿼터 카운터 + ②③④ 폴백 | address-service 217/217 · 루트 266/266 |
| 앵커 역함수 `parseCoordKey` (왕복 회귀) | 같은 건물에 행이 둘 생기는 것 차단 |
| F1·F7·F9·F10 회귀로 고정 | `scripts/db-guard.test.mjs` 12/12 |

#### 실측 결과 (2026-08-11, Job `nexus-address-fill`, 200건)

§6 판정 기준 대조. 주장이 아니라 Job 로그다.

| 판정 항목 | 실측 | 판정 |
|---|---|---|
| 행수 전/후 (중심 좌표 보유) | **0 → 200** | 🟢 |
| 채움률 | **100건 중 100건 (100%)** × 2회 | 🟢 |
| 출처 분포 | `vworld 200` · kakao 0 · entrance 0 · none 0 | 🟢 (입구는 C-7 대기) |
| 앵커 왕복 | **100/100 일치**, 키 어긋남 0 | 🟢 행 중복 없음 |
| 동 좌표 | **0건** | 🟡 아래 참조 |
| 이상치 `quality='outlier'` | **미측정** | ⬜ C-6 검증 단계에서 |
| 샘플 육안 | 부천 오정구 5건 전부 37.51~37.52 / 126.80 대역 | 🟢 |

**동 좌표가 0건인 이유(정상)**: 이 배치는 `building_coord` 를 대상으로 돈다. 그 테이블에는
**동 번호가 없다** — 동 번호는 명단에서 온다. 따라서 배치 경로는 **중심 좌표 전담**이고,
동 좌표는 명단 기반 `mode:'fill'` 호출(C-5)에서 채워진다.

**이 실행이 잡아낸 결함 2건**(둘 다 수정·재실측 완료):
1. **키 없는 출처가 쿼터를 차감** — 첫 실행은 키 미주입으로 채움률 0% 였는데 요약엔
   `vworld 100 사용`이 찍혔다. 호출은 0회였다(1.1초에 200콜 불가). → `availableSources`
   로 키 없는 출처는 주입하지 않고, 실행기가 출처 0개면 **시작 전에 중단**한다.
2. **맞출 동이 없는데 BBOX 호출** — 동 번호 없이 `isApartment` 만 보고 100회 태웠다.
   → `wantDong` 없으면 미호출. **실측: VWorld 200회·10.2초 → 100회·4.4초, 채움률 동일.**

#### 전량 채움 완료 (2026-08-11, 잔여 1,343건)

| 항목 | 실측 |
|---|---|
| 시도 / 좌표 확보 | 1,343 / **1,343 (100%)** |
| **중심 좌표 보유 (최종)** | **1,543 / 1,543 (100%)** · `quality='none'` 0 |
| 출처 분포 | **vworld 396 · kakao 947** |
| 쿼터 사용 | vworld 1,343 · kakao 947 (한도 20,000 / 50,000) |
| 소요 | 93.3초 |

> **④ Kakao 폴백이 실제로 명단을 구했다.** 설계 §3-2 의 4단 폴백이 없었다면 947건(70%)이
> 좌표 없이 남았을 것이다.

##### ✅ VWorld 실패율 70% 규명·해결 (2026-08-11)

**결론: VWorld 쪽 토큰버킷이다 — 버스트 약 370건 + 낮은 재충전. 초당 2건으로 제한하면 100%.**
`scripts/diag-vworld.mjs`(읽기전용)로 가설을 하나씩 죽였다.

| # | 조건 | 성공률 | 붕괴 지점 | 판정 |
|---|---|---|---|---|
| 1 | 40건 · 동시성 1·3·6 | **100%** (각 40/40) | 없음 | 동시성 무관 → **버스트 스로틀 아님** |
| 2 | 800건 · 초당 20 | 49.5% | 346회째 | 502 Bad Gateway 180 · network 224 |
| 3 | 800건 · 초당 3.9 (+재시도) | 57.5% | 379회째 | **속도 5배 낮춰도 붕괴점 그대로** → 속도 아님 |
| 4 | 800건 · `connection: close` | 58.9% | 368회째 | keep-alive 커넥션 가설 **기각**(되돌림) |
| 5 | 600건 · 초당 2.1 | **100%** | 없음 | 누적이 아니라 **지속 속도**가 관건 |
| 6 | **800건 · 초당 2.0** | **100%** | 없음 | ← 확정 |

- 실패 응답은 **502 Bad Gateway**("The server returned an invalid or incomplete response")
  로, 레이트리밋(429)이 아니라 **과부하 응답**이다 → 기다렸다 다시 하면 된다.
- **처방(적용됨)**: `geocodeRoad` 에 ①전역 속도제한 기본 **2/초**(`VWORLD_RATE_PER_SEC`)
  ②502·네트워크·타임아웃만 **지수 백오프 재시도** 3회(`VWORLD_RETRY_ATTEMPTS`).
  키 오류·좌표 없음은 재시도하지 않는다(같은 답 — 쿼터만 태운다).
- **덤으로 잡은 결함**: 속도제한기가 동시성 하에서 슬롯을 겹쳐 배정해 **설정 0.7/초가
  실효 2.1/초**로 나왔다(정확히 동시성 배수). 슬롯을 동기 예약하도록 수정 —
  이제 설정 2/초 → 실효 2.0/초. 회귀로 못 박음(`scripts/vworld-retry.test.mjs`).
- **속도 대가**: 800건 400초(≈6.7분). 명단 1,000건 동 좌표면 약 8분. 배치에는 충분하다.

> C-5 착수 가능. 동 좌표가 조용히 70% 비는 위험은 제거됐다.

<details><summary>규명 전 기록(원문 보존)</summary>

##### ⚠️ VWorld 실패율 70% — C-5 전에 반드시 규명할 것

`geocodeRoad` 가 1,343회 중 **947회 실패**했다. 로그의 실패 형태는 두 가지다:
- `Unexpected token '<', "<html><bod"...` — JSON 대신 **HTML 에러 페이지**가 왔다
- `fetch failed` — 연결 자체가 끊겼다

성공(396)과 실패가 **뒤섞여** 나온다 = 하드 한도 초과가 아니라 **버스트 스로틀링**으로 보인다
(동시성 3, 초당 약 14요청). 하드 한도였다면 어느 시점 이후 전부 실패했어야 한다.

**왜 C-5 전에 고쳐야 하나**: 동(棟) 좌표는 **VWorld 만** 줄 수 있다(Kakao 는 단지 1좌표뿐).
지금 상태로 C-5 에서 동 채움을 돌리면 **70% 가 조용히 동 좌표 없이 지나간다** — 그리고
`acceptDongCandidate` 가 정상 기각한 것과 **구분되지 않는다**. 둘 다 "동 좌표 없음"으로 보인다.

**해야 할 것(제안)**
1. `geocodeRoad` 실패를 **원인별로 계측**한다 — HTTP 상태·응답 앞 100자를 남긴다.
   지금은 `error.message` 만 찍혀 스로틀인지 키 문제인지 갈리지 않는다.
2. VWorld 전용 **동시성 1 + 백오프 재시도**를 넣는다(Kakao 는 3 유지).
3. 그 뒤에도 실패하면 `quality_note` 에 사유를 적어 **재시도 대상**으로 남긴다.

</details>

> 남은 대상: **0건**. 이 배치(중심 좌표)는 완료. 다음은 C-5(명단 기반 동 좌표).

**실측 절차** (형 승인 후 — 운영 DB 접속):
```bash
# ① 예행(외부 API·쓰기 없음) — 대상 건수와 앵커 왕복만 본다
node scripts/fill-coords.mjs --limit=100
# ② 실제 채움
node scripts/fill-coords.mjs --limit=100 --apply
```
Cloud Run Job 으로 돌릴 때는 `--command` 로 진입점을 지정한다(이 리포의 다른 Job 과 동일).

> ⚠️ **아직 안 고친 것 — 구 캐시 오염**: `pickDong` 수정은 **새 조회에만** 적용된다.
> `/v1/building/dong-coords` 는 `address_geocode_cache` 의 `dong:*` 행을 먼저 돌려주는데,
> C-4 격리(375건)는 `building_dong_coord` 쪽만 손댔다. 구 캐시의 오염 행은 그대로
> `match_type='dong'` 으로 서빙된다. 별도 정리가 필요하다(C-4-b 제안).

### 5-3. C-5 클라 연동 (2026-08-11) — 조회·경고 완료, 동 좌표 채움 남음

| 한 것 | 내용 | 증거 |
|---|---|---|
| **배치 조회로 전환** | `App.jsx` 백그라운드 좌표 루프가 저장소를 **먼저 한 번에** 묻는다(`mode:'cache'`). 기존엔 건당 650ms 쉬며 외부 지오코딩을 태웠다 — 이미 확보한 1,543건을 다시 사는 낭비였다 | 운영 API 실측: 실주소 3건 좌표 반환(Job 이 쓴 값과 일치), 없는 주소는 `no_anchor` |
| **선택 규칙 SSOT 일치** | `pickStoreCoord` 가 서버 `pickDeliveryCoord` 와 같은 규칙 — 내비=입구→중심(동 안 씀, F2) / 순번=동→입구→중심 | `scripts/coord-store-api.test.mjs` 13건 |
| **미보유 경고 노출** | `coordIssues.noCoord` 가 **계산만 되고 아무 데도 안 쓰이고 있었다**. 지도 분석 액션 맨 앞 + 이슈 카운트에 연결 | 미보유가 있으면 "큰 신호 없음"으로 안 뜬다(F4) |
| 응답 길이 방어 | 서버 응답 길이가 요청과 다르면 **그 응답을 통째로 버린다** | 인덱스가 밀리면 **다른 사람의 좌표**가 붙는다 |

#### 서비스 배포 + 동 좌표 채움 실측 (2026-08-11, 리비전 `00069-b77`)

`nexus-address-api` 재배포 완료(롤백 지점 `00066-znm`). 회귀 스모크 통과 —
match 0.1~0.28초 · `/v1/coords/status` total 1,543 / center 1,543 / 전무 0 · 학습사전 typo 23건.

**배포 직후 실측이 결함 3건을 더 드러냈다(전부 수정·회귀 고정):**

| # | 결함 | 실측 |
|---|---|---|
| 1 | **동 좌표가 영원히 안 채워짐** — 중심이 있으면 `cached` 로 건너뛰었다. 내비용 점과 동 좌표는 **용도가 다른 별개 값**인데 하나로 묶어 판정 | 명단 4건 → `skipped: 4` (동 0건) → 수정 후 `skipped: 0, attempted: 4` |
| 2 | 아는 중심이 있어도 지오코딩을 다시 삼 | `knownCenter` 재사용 → 호출 8회 → **4회** |
| 3 | **대단지 2차 BBOX 확장 누락** — `matchDongCoord` 가 2026-07-27 에 넣은 단계("306동이 320동 위치에" 사고의 수정)가 새 채움 경로에 없었다 | 수정 후 좁게+넓게 2회 호출 확인(4건 → 8콜) |
| + | 동만 채운 건이 요약에서 '못 구함'으로 잡힘 | `dongOnly` 분리 |

**동 채택 규칙 실증**(`scripts/probe-dong.mjs`, 읽기 전용) — 실재하는 동 번호로 **6/6 채택 성공**:

| 단지 | 요청 동 | 후보 수 | 결과 |
|---|---|---|---|
| 보성아파트(시흥 함송로 63) | 101 · 103 · 105 | 2 · 4 · 4 | **전부 채택** — 단지명으로 정확히 골라냄 |
| 월곶2차풍림아이원 | 202 · 203 · 205 | 1 · 1 · 1 | **전부 채택** |
| 〃 안전규칙 | `동 '가' \| 보성아파트` | 2 | **기각**(단일문자 동, 후보 2개) ✅ |

> 앞선 "동 0건"들은 **내가 넣은 동 번호가 그 단지에 실재하지 않아서**였다(무궁화아파트 101·102,
> 여월휴먼시아 105·106·108·109). 규칙이 정상 기각한 것이지 배관이 막힌 게 아니다.

##### ✅ 여월휴먼시아2단지 규명·해결 (2026-08-11) — 원인은 **단방향 이름 매칭**

가설 두 개(대표점 이탈 / 소스에 없음)를 **둘 다 실측으로 기각**하고 세 번째 원인을 찾았다.
도구 = `scripts/probe-address.mjs`(읽기 전용).

| 검사 | 실측 | 판정 |
|---|---|---|
| 저장중심(kakao) ↔ VWorld 지오코딩 | **216 m** 차이 | 있지만 좁은 BBOX(±250m) 안 |
| 저장 중심에서 BBOX | 여월휴먼시아 **22건**(좁게) · **66건**(넓게) | **소스에 있다** → 가설② 기각 |
| 동 104 좌표에서 BBOX | 여월휴먼시아 **23건** | 대표점 문제 아님 → 가설① 기각 |
| **VWorld 원본 필드** | `buld_nm='여월휴먼시아'` · `buld_nm_dc='204동'` · 13층 · **동번호 있는 것 22/23건** | 동 번호도 정상 |

**진짜 원인**: VWorld 는 단지명을 **줄여서** 준다 — `여월휴먼시아` vs 저장된 `여월휴먼시아2단지아파트`.
단지명 필터가 `vworld.includes(ours)` **한 방향**이라 `'여월휴먼시아'.includes('여월휴먼시아2단지아파트')`
= false → **BBOX 에 22건이 있는데도 전부 기각**됐다. 보성·월곶이 통과한 건 이름이 우연히 정확히 같아서였다.

**수정**: `sameComplex` — 어느 쪽이 더 길든 **서로 포함하면** 같은 단지로 본다.
짧은 쪽이 4글자 미만이면 역방향은 열지 않는다(`삼성`·`대우`가 아무 데나 붙는 것 차단).
남는 위험은 기존 "후보 둘 이상이면 기각" 규칙이 받는다.

**수정 후 실측**(신선 조회, `cached=false`):
`201동 → 37.51195` · `204동 → 37.51247` · `207동 → 37.51325` · `211동 → 37.51344` — **4/4 `matched='dong'`, 13층**.

> ⚠️ **부수 발견 2건(미조치)**
> 1. **동 파싱 경로별 불일치 → 통일 완료**: `toDongNo` 하나로 받는다(**값이면 정규화, 이름이면 추출**).
>    예전엔 `/v1/building/dong-coords` 가 `parseDongNo`(`'동'` 접미사 필요)를 값 자리에 써서
>    클라가 `'201'` 을 보내면 **빈 값 → 캐시키도 비고 동 매칭도 안 돌고 조용히 centroid**로 떨어졌다.
>    실측: `'201'` → 수정 전 `centroid` / 수정 후 `matched='dong' 37.51195 13층`.
>    기존 형식 `'201동'` 도 같은 좌표 — 퇴행 0. 회귀 5건(`coord-store.test.mjs`).
> 2. **새 오염 패턴 → C-4-c 로 정리 완료**(아래).

<details><summary>규명 전 기록(원문 보존)</summary>

##### ⏳ 미해결 — 여월휴먼시아2단지에서 VWorld 가 동을 안 준다

저장된 동(104·107·110·112·201~212)을 `/v1/building/dong-coords` 로 물으면 **2단계 확장을
거쳐도 전부 `matched='centroid'`**. 그 좌표 주변 BBOX 가 돌려주는 건 대한빌리지·장미하이빌·
한맥프라임빌 같은 **빌라들**이다(동 번호가 읽힌 건물 48개 중 해당 단지 없음).

- 저장된 동 좌표는 **구 캐시 이관분**(C-4)이라 지금 VWorld 응답과 출처가 다르다.
- 가설 2개(미검증): ①`여월로 65` 지오코딩 대표점이 단지에서 벗어나 있다
  ②VWorld `LT_C_SPBD` 에 그 단지가 없다.
- **코드 결함으로 볼 근거는 없다** — 같은 코드가 보성·월곶에서는 6/6 채택했다.
- 다음: 그 단지 좌표를 지도에서 육안 대조 → 대표점이 틀렸으면 주소·좌표 문제, 맞으면 데이터 소스 한계.

</details>

> **C-4-b 관련 정확한 현재 상태**: 캐시 격리(356행 `suspect`)는 **이미 적용**됐고,
> 클라는 `matched === 'dong'` 만 채택하므로 **오염 좌표는 지금도 안 쓰인다**.
> 서비스 재배포가 추가하는 것은 "suspect 를 미스로 보고 **고친 규칙으로 재계산**"하는
> 복구 동작이다. 즉 안전은 확보됐고, 재배포는 회복을 붙이는 일이다.

### 5-4. C-4-c 같은 좌표를 공유하는 동 격리 (2026-08-11) — 완료

C-4·C-4-b 는 "같은 좌표인데 **건물명이 다름**"만 잡았다. 여월 진단에서 **세 번째 패턴**이 나왔다:
**같은 건물명 · 동만 다른데 좌표가 하나**. 한 단지의 서로 다른 동이 한 점일 수 없다.
건물명이 같아 기존 규칙에 안 걸린다.

도구 `scripts/quarantine-dong-samecoord.mjs`(기본 dry-run). 두 테이블을 함께 본다 —
구 캐시가 이관 원본이라 거기를 안 고치면 재조회 때 같은 값이 다시 들어온다.

| 실측 | 값 |
|---|---|
| 오염 그룹 / 행 | **6그룹 / 18행** (양쪽 테이블 동일) |
| `building_dong_coord` 강등 | 18 |
| `address_geocode_cache` 강등 | 18 |
| 남은 신뢰 동 좌표 | 1,998 → **1,980** |
| **잔여 오염** | **0** (재실행 대상 0) |

오염 표본: 여월휴먼시아2단지 `104·107·110·112` / 시화5차평안마을건영 `502·503·505·509` /
대야역 두산위브 `113·114·115` / 시흥 금강펜테리움 `102·103·104` / 제일풍경채 센텀 · 파라곤 센트럴 각 2개.

**격리 후 회복 실측** — 지운 게 아니라 낮췄으므로 다음 조회에서 다시 계산된다:

| 단지 | 결과 |
|---|---|
| 여월휴먼시아2단지 | `104→37.51549(12층)` `107→37.51638(13층)` `110→37.5166(11층)` `112→37.51568(10층)` — **4/4 `matched='dong'`, 전부 다른 좌표·다른 층수** ✅ |
| 시화5차평안마을건영 | 502·503·505·509 → `matched='complex'` — VWorld 에 그 동이 없다. 클라는 `'dong'` 만 채택하므로 **틀린 좌표 대신 좌표 없음**이 된다 ✅ |

> 두 결과 모두 의도한 동작이다. 하나는 회복, 하나는 정직한 미보유.

## 6. 완료 판정 기준 (증거 없이는 완료가 아니다)

이번 사고의 근본원인은 "적재했다"는 **주장**이 검증 없이 문서에 남은 것이다.
각 단계는 아래를 로그로 남겨야 완료로 인정한다.

```
□ 테이블 존재 확인      to_regclass 결과
□ 행수 실측             단계 전/후 count(*)
□ 채움률                대상 N건 중 좌표 확보 M건 (M/N %)
□ 출처 분포             entrance / vworld / kakao / none 각 건수
□ 이상치                quality='outlier' 건수
□ 샘플 육안 확인        실주소 5건의 좌표를 지도에서 대조
```

**진단 도구는 상시 유지한다**: `scripts/diag-coords.mjs` — 언제든 실태를 다시 잴 수 있어야 한다.

---

## 7. 이 설계가 바꾸지 않는 것 (기존 규칙 보존)

- **A-36 주소 치환 금지** — 좌표를 못 구했다고 주소를 바꾸지 않는다. 좌표 없음은 좌표 문제이지 주소 문제가 아니다.
- **M-1 무손실** — 좌표가 없어도 대상자를 명단에서 빼지 않는다.
- **DS-15** — 좌표 이상치는 지우지 않고 표시만 한다.
- **R-E·R-I** — 같은 주소·같은 단지는 같은 기사. 동 좌표가 생겨도 이 원칙이 우선한다.
- **DS-9-5** — 기사 출발지 좌표는 외부 출력 금지. 신규 좌표도 같은 등급으로 취급한다.

---

## 부록 A. 진단 재현 방법

```bash
gcloud run jobs execute nexus-address-diag --region asia-northeast3 --project logis-op --account ttong627@gmail.com --wait
gcloud logging read 'resource.type=cloud_run_job AND resource.labels.job_name=nexus-address-diag' --project logis-op --limit 45 --format="value(textPayload)"
```

## 부록 B. 용어

| 용어 | 뜻 |
|---|---|
| 입구 좌표 | 차량이 실제로 진입하는 지점. 행안부 출입구 자료가 정본 |
| 중심 좌표 | 건물·단지 대표점. 지오코딩 결과 |
| 동(棟) 좌표 | 아파트 각 동의 위치. VWorld `LT_C_SPBD` 폴리곤 중심 |
| coord_key | `{road_code}#{지하여부}#{본번}-{부번}` — 좌표의 앵커 |
