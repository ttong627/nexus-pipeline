// SSOT: services/address-service/src/routing/deliveryDaySplit.js (배송 코어 승격 2026-08-03).
//   서버 Docker 빌드 컨텍스트에 포함시키려 실체를 서비스 쪽으로 옮겼다.
//   이 파일은 기존 참조 경로 호환을 위한 재수출 스텁 — 로직은 SSOT 에만 존재한다.
export * from '../../services/address-service/src/routing/deliveryDaySplit.js';
