# NEXUS PIPELINE V5.0 — 프로젝트 룰 (운영 전체 규칙)

이 파일은 프로젝트의 모든 운영 규칙을 정의합니다.
코드 수정 시 이 규칙들이 깨지지 않도록 반드시 준수해야 합니다.

---

## 기술 스택

- React 19.2 + Vite 8
- Firebase 12 (Firestore, Auth, Hosting)
- Tailwind CSS 4
- XLSX (SheetJS) — Web Worker에서 실행
- 도로명주소 API: business.juso.go.kr (3개 키 로테이션)
- 배포: `npm run build && firebase deploy --only hosting`

---

## 1. 주소 정제 규칙

| # | 규칙 | 상세 |
|---|------|------|
| A-1 | 이름 5자 초과 | 5자로 자르고 `(본명:XXX)` 를 특이사항에 추가 |
| A-2 | 오타 사전 적용 | Firestore `typo_dict` 우선 치환 (AI 자가학습). Enter키로 재정제 시 오타→정답 자동 등록 |
| A-3 | 유니코드 정규화 | NFC + 제어문자/NBSP/탭/개행 제거, 따옴표 제거 |
| A-4 | 미닫힌 괄호 제거 | `\([^)]*$` 패턴 삭제 |
| A-5 | 지역 접두어 제거 | 시/도/구 토큰 — 도로명·지번 만나면 제거 중단. **도로명(로/길/대로)이 없는 지번·건물명 주소면 읍/면/동(법정동) 토큰 보존**(`용두동 행복아파트`→용두동 유지, `광덕면 신흥리 123`→광덕면 유지). 도로명주소는 읍/면/동 제거(API가 `()` 법정동 복원) |
| A-6 | 통·반 제거 | `제X통 제X반` 패턴 제거 |
| A-7 | 주민센터 검색어 생성 5단계 | ①행정동 컬럼 > ②분리형(XX동 주민센터) > ③부착형(청량리주민센터→청량리동) > ④텍스트에서 동·읍·면 추출 > ⑤cityLabel 마지막 레벨 |
| A-8 | 건물명 전용 Fallback | 도로명·지번 없고 건물 유형어 있으면 `adminDong + 건물명` 으로 재검색 |
| A-9 | 특수문자 분리 | `* / ☆ ★ \| ~ # § ※ ** => ->` 이후 내용 → 특이사항으로 이동 (Firestore `special_chars` 자동 확장) |
| A-10 | 동호 형식 정규화 | 구현=**순수함수 `src/engine/dongHoFormat.js`**(회귀 `scripts/dong-ho-format.test.mjs`). **①순수 숫자 동(대단지 아파트)만** → 대시+호수 4자리 우측 공백 패딩 **+ 층을 호 뒤로**: `101동 203호`→`101- 203호`, `101동 3층 203호`→`101- 203호 3층`. **②층을 호 뒤로 보내는 규칙은 숫자 동이 앞에 붙을 때만**(형 지시 2026-07-30) — **비숫자 동(가동·A동·B동·`1-1동`)·동 없음은 층 위치를 원본 그대로 보존**: `가동 3층 101호`→`가동 3층 101호`, `B동 지하1층 104호` 그대로, `3층 101호` 그대로. **③동 대신 대시로 쓰인 숫자 동**(`101-203호`·`101-3층 203호`)도 숫자 동으로 인식해 같은 형식으로 저장(`101- 203호`·`101- 203호 3층`) — **동 3~4자리+호 3~4자리+`호` 표기 있을 때만**(지번 부번 `40-25호`·다가구 `1-2호`·호 없는 `101-203`은 미개입 = 주소 변조 차단). **동(棟) 없이 호수만 있는 건물(예: RYUJIN VILL 302호~1009호)** → 호수 4자리 우측정렬 패딩(3자리 앞 빈칸, 정렬 시 302<1008 자연정렬), 앞에 동 토큰이 있으면 제외. **한글/영문/대시 동** → `동` 그대로 유지: `가동 101호`, `B동 104호`, `1-1동 100호`. ※ 동(棟) 토큰(가동/나동/B동/101동/1-1동)은 `DETAIL_START_RE`/`DETAIL_MARKER_RE`의 `DONG_UNIT_SRC`로, 대시 동호(`101-203호`)는 `DONG_DASH_HO_SRC`로 **상세주소로 인식**(건물명 슬롯·괄호로 새지 않음 — `래미안 101-203호`가 건물명 `래미안`+상세 `101- 203호`로 분리, 데이터손실 차단). 절대 되돌리지 말 것 |
| A-11 | 최종 주소 형식 | **기본 = `도로명주소, 상세주소(동호수) (법정동, 건물명)` — 건물명이 맨 뒤**(형 지시 2026-07-30). 예 `권선로 472, 101- 203호 3층 (권선동, 래미안)`. 구현=`addressEngine.js` A-11 조합 + `App.jsx` `formatAddressForDisplayMode`(기본 모드 `detailBeforeParen`, `ADDRESS_DISPLAY_MODES`). 결과 화면 `주소 형식` 버튼으로 `도로명주소, (법정동, 건물명) 상세주소`와 상호 전환 가능(전환은 형이 화면에서 선택할 때만). 괄호 안에 법정동·건물명 `,` 구분, trailing 쉼표 제거 |
| A-12 | 확인필요 플래그 | 주소 공란 / 도로명 미발견+API 실패 / 변환 후 주소 3자 미만 / **도로명은 있으나 관할 시군구 DB에서 미확인(타지역 이전·오타 가능) → 담당자 확인요청** |
| A-13 | API 캐시 + 지역 컨텍스트 검색 | 메모리(최대 1000건) + IndexedDB 2단계 캐시, dedup (pendingRequests Map). cityLabel에서 시·군·구 토큰(districtTok) 추출 → `"동대문구 왕산로 72"` 형태로 전국 동명 오매칭 방지 |
| A-14 | 병렬 처리 | asyncPool(20) — 500건 청크, 50건마다 메인스레드 양보 (화면 프리징 제로) |
| A-15 | 구분자 점 치환 | 도로명 번호 뒤 `숫자. ` → `숫자, ` (예: `테헤란로 123. 456호` → `테헤란로 123, 456호`) |
| A-16 | 번지 표기 제거 | `123번지` → `123` (지번주소 정규화) |
| A-17 | 층 표기 변환 비활성화 | `3F→3층`, `B1→지하1층` 모두 적용 안 함 — 아파트 B동·F동 등 건물동 명칭과 혼동 오탐 발생 |
| A-18 | 제(第) 접두어 제거 | `제101동` → `101동`, `제205호` → `205호` |
| A-19 | 동호 붙여쓰기 분리 | `101동205호` → `101동 205호` (A-10에서 대시로 변환) |
| A-20 | 리(里) 지번 인식 | `신남리 123-5` 형태의 지번주소에서 `리`를 isJibun 패턴에 포함 |
| A-21 | 동사무소/읍사무소/면사무소 → 주민센터 | 주소 및 특이사항 입력값에서 구식 명칭을 `주민센터`로 정규화. DB 전체 일괄 변경 가능 (관리자 패널 → 운영현황 → DB 마이그레이션) |
| A-22 | 특이사항 주민센터 → 주소 앞에 붙이기 | `inputNote`에 `주민센터` 포함 시 해당 주민센터 주소를 API로 조회하여 `result.주소` 앞에 `{주민센터 도로명주소}, {기존 주소}` 형태로 추가 (중복 방지 내장) |
| A-23 | 번길/가길 도로명 공백 없음 | `사가정로 2길` → `사가정로2길`, `XX로 14번길` → `XX로14번길`. 로/대로/길 + 공백 + 숫자+한글길 계열은 붙여쓰기 (역방향 패턴: `\s+(\d+[가-힣]*길)` 제거) |
| A-24 | **법정동 최우선 (2026-07-21 개정 · 절대 되돌리지 말 것)** | 괄호 `()` 안 동명은 **반드시 법정동**. 우선순위 ①주소DB/Kakao가 확인한 **법정동**(`legalDong`/`emdNm`/Kakao `region_3depth_name`) → ②도로명주소에 포함된 읍/면 토큰 → ③`adminDong`(행정동 컬럼) **최후 폴백**. ※개정 전에는 ③이 ①보다 앞서 있어 도로명주소에 동 토큰이 없는 대부분의 경우 **행정동이 괄호에 들어갔다**(전농1동→`(전농동)`, 용신동→`(용신동)`). Kakao `region_3depth_h_name`(행정동)은 **절대 쓰지 않는다** |
| A-25 | 같은 도로명 `()` 건물명 **보완** | 배치 정제 완료 후 `unifyParenContent()` 후처리 — 공백 제거 후 도로명+번호(`황물로7길17`) 기준 그룹화 → **건물명이 빈 레코드에만** 최빈값 보완. **기존 건물명·동명은 각 레코드 그대로 보존**(같은 지번에 밀림장·은혜빌라 여러 빌라동 공존 가능 → `은혜빌라`↔`밀림장` 덮어쓰기 절대 금지, `답십리2동`·`답십리동` 행정동 덮어쓰기 금지). 괄호 없는 레코드에 건물명 추가 시 `, (건물명)` 쉼표 필수 |
| A-26 | `()` 내 동명 토큰 필터 | `buildingName` 구성 시 한글 시작 + 동/읍/면 접미어 토큰 제거 (`답십리1동`, `용두동` 등). 숫자 시작 건물 동호(`101동`)는 유지. 입력 주소 괄호 안 동명이 API 결과 `dongPart`와 달라도 중복 동명 표시 방지 |
| A-27 | 행정동 번호 접미어 제거 | 도로명주소 `()` 내 동명은 **법정동(번호 없음)** 표기. `답십리2동` → `답십리동`, `청량리3동` → `청량리동`. `행정동` 컬럼 값은 변경 안 함. `A-25 unifyParenContent`에서도 동일 정규화 적용 |
| A-28 | 중첩 괄호 잔여 `)` 제거 | 입력에 `삼화에코빌(6차)` 같은 중첩 괄호가 포함된 경우, non-greedy `/\(.*?\)/g` 추출 후 바깥 `)` 가 text에 잔류하여 `103- 501호 ) (장안동)` 형태의 오출력 발생. **괄호 추출 직후 text 내 남은 `)` 를 모두 제거** (`text = text.replace(/\)/g, '')`). 구현: `src/engine/addressEngine.js` 괄호 추출 다음 줄. 절대 되돌리지 말 것 |
| A-29 | 건물 동(棟) 괄호 침범 차단 | 괄호 `()` 는 **(법정동, 건물명)만** 담는다. 빌라/아파트 건물 동(가동·B동·101동·`푸르지오 101동` 등)이 `bdNm`/inline으로 buildingName에 섞이면 상세주소로 이동(A-10 일관 정규화). 규칙: 숫자동(101동)·영문동(B동)은 호수 무관 분리, 단일 한글동(가동)은 **호수 동반 시에만**(법정동 사동·본동 오인 방지). `푸르지오 101동`→상세 `101동`/건물명 `푸르지오`(앞 건물명 보존). 구현: `addressEngine.js` finalDetail 직전. 절대 되돌리지 말 것 |
| A-30 | 주민센터/도로명 지역 오매칭 차단 | 전국 동명(시흥시 군자동 vs 서울 광진구 군자동) 오매칭 방지. ① `getMunicipalityMatch`가 **시군구 단독 cityLabel("시흥시")** 도 시군구 토큰으로 비교(과거엔 시도 없으면 필터 OFF였음) ② Kakao 폴백 `kakaoDocToApiResult` 직행을 **`kakaoDocInMunicipality` 지역검증**으로 차단 ③ 주민센터 검색어에 **지자체 접두어 강제**(`시흥시 군자동 주민센터`). `extractSido/extractSigungu` 헬퍼 사용. ④ **시도 축약 정규화(`normalizeSido`+`SIDO_ALIAS`)** — cityLabel 정규명(`서울특별시`)과 Kakao/JUSO 결과 축약(`서울`)을 표준 약칭키로 일치시켜 **시도 비교 오차단 방지**(2026-07-16 주민센터 84건이 `서울특별시≠서울`로 게이트에 막혀 전부 확인필요로 빠지던 사고). **시군구 비교는 그대로 유지**(진짜 타지역 오매칭은 계속 차단). 절대 되돌리지 말 것 |

| A-31 | 법정동 빠짐 방지 + 법정동 컬럼 (2026-07-21 신설 · 절대 되돌리지 말 것) | **① 캐시 품질 게이트**: `lookupAddr` 캐시에 법정동이 없으면 캐시를 그대로 쓰지 않고 API를 1회 더 태운다(재조회 실패 시에만 기존 캐시 유지, 부정 캐시는 보존). **② Kakao 법정동 조회**: 전국 주소DB가 법정동을 못 주면 Kakao 주소검색 `region_3depth_name`(법정동)으로 보강 — 지역검증(A-30) 통과분만. **③ Kakao POI 폴백**에서도 지번주소(`address_name`)의 동 토큰을 법정동으로 채운다. **④ 법정동 컬럼**: `result.법정동`(=`legalDong`)을 정제결과 그리드·엑셀(최종명단·배송표)·`cloud_lists.법정동`·`base_lists.legalDong`에 저장·표시. 저장값 없는 기존 레코드는 괄호 첫 토큰에서 파생 표시. 엑셀 컬럼 버전 **v4** |

| A-32 | 법정동·건물명 빠짐 0 정밀화 (2026-07-21 · 절대 되돌리지 말 것) | **① 법정동 형태 확장** `LEGAL_DONG_RE`: `OO동`뿐 아니라 **`OO가`(매산로2가·을지로3가)**, **`OO읍 OO리`(홍북읍 신경리)**, `OO읍/면`, `OO리`도 법정동으로 인정. 과거 `동\|읍\|면`만 허용해 수원 구도심·읍면지역 법정동이 전량 버려졌다. **② 읍/면만 온 경우 리까지 보강**(더 구체적인 값일 때만 교체) + `result.리` 백필. **③ 지번주소 최종 방어선**: `장곡동 344`처럼 지번의 동/리는 정의상 법정동이므로 API 없이 확정. **④ 건물명 전용 매칭 확장**: 행정동 컬럼 없어도 지자체만으로 시도, 건물 유형어 없는 자유 건물명(더존에이스빌·숲예찬)도 Kakao POI로 조회하되 **이름 포함관계 검증** 통과분만 채택. **⑤ `숫자+동/층/호` 뒤 한글이면 동호수 아님**(`(?![가-힣])` 가드) — `장안2동우체국`이 `장안`으로 잘려 매칭이 깨지고 상세주소가 오염되던 문제. **⑥ 건물명 컬럼 = 괄호 표시값**(예전엔 API `bdNm`만 담아 입력에서 뽑힌 건물명은 컬럼이 비었다). **⑦ 무손실(M-1)**: API 건물명 채택으로 버려지던 입력 문구(`뒷문으로 들어와서 계단 오른쪽`)를 특이사항으로 보존. 실측 법정동 채움률 **57.9% → 99.2%**(잔여는 타 지자체 소재·미준공으로 A-30이 올바르게 거부한 건) |
| A-33 | 특이사항 검증·정제 단일 규칙 (2026-07-22 · 절대 되돌리지 말 것) | 특이사항에는 **배송 도움 메모만** 남긴다. 구현은 `src/utils/noteSanitizer.js` **단일 SSOT**(앱·스크립트 공용 — 규칙이 두 벌로 갈라지는 것을 금지). **★삭제가 아니라 이동이 기본(무손실 M-1)**: ①`(본명:XXX)`→본명컬럼(중첩 괄호 `(본명:인순이(김병기))` 대응) ②**호수·층은 상세주소 컬럼에 먼저 채운 뒤에만** 특이사항에서 제거(형 지시 2026-07-22) ③법정동 단독→법정동컬럼, `내동, 성진그린타운` 형태는 법정동+건물명으로 분해 ④건물명 단독→건물명컬럼. **삭제는 딱 2가지만**: (a)완전 무의미(기호만·`0000`류 숫자반복·한글자·`[주소추정]` 시스템문구) (b)주소 문자열에 이미 통째로 존재. **판단이 애매하면 무조건 보존**. ★`계단위 201호 정면`처럼 호수가 *섞인* 메모를 통째로 상세주소로 보내면 안 된다(`isDetailOnly`), `대문 정면`이 '면'으로 끝난다고 법정동으로 보면 안 된다(실존 동 이름 일치 요구) — 둘 다 회귀 테스트로 고정: `scripts/note-sanitizer.test.mjs`. 일괄 정제 도구 `scripts/clean-special-notes.mjs`(dry-run 기본). **적용 지점 2곳(이중 방어)**: ①정제 단계 `App.jsx` 행 조립부 — 검증 후 **특이사항의 호수를 주소 상세부로 승격**해 주소를 재조립한다(`도로명, 호수 (법정동, 건물명)`). ★**다음 달 명단의 상세주소에 호수가 없어도 특이사항에 호수가 있으면 여기서 자동으로 채워진다**(형 질문 2026-07-22). ②저장 단계 `App.jsx` base_lists 저장부 — 정제를 거치지 않는 경로(직접 편집·이식·과거 데이터)의 마지막 방어선, `detailAddr`가 비면 승격값을 담는다 |
| A-34 | 이름칸 `홍길동(750315)` → 이름·생년월일 분리 (2026-07-22 · 용산구류 서식) | 일부 지자체는 수령자명 칸에 생년월일을 괄호로 붙여 보낸다. **A-1(5자 절단)보다 먼저** 분리하지 않으면 `홍길동(7`로 망가지고 생년월일이 통째로 유실된다. `splitNameBirth()` — 괄호 안이 **6자리(YYMMDD)·8자리(YYYYMMDD)** 숫자일 때만 분리하고, `최정호(박주령)` 같은 본명·별명 괄호는 건드리지 않는다. 생년월일 컬럼이 비어 있을 때만 채운다(기존값 우선). 구현: `App.jsx` 정제 루프 진입부 |
| UI-1 | 명단 표 직접수정 규칙 (2026-07-22 · 절대 되돌리지 말 것) | **① 입력값을 부모 상태에 두지 말 것** — 셀 입력값은 `components/CellInput.jsx`가 자체 `useState`로만 관리한다. 부모(BaseListManager·CloudListManager)에 `setEditValue` 하면 글자 하나마다 수천 행짜리 표 전체가 재계산돼 **타이핑이 버벅거린다**(형 지적). `onCommit`은 편집이 끝날 때(Enter·Tab·포커스 이탈) **단 한 번만** 호출한다. **② 진입은 더블클릭만** — 단일 클릭 진입은 스크롤 중 오진입으로 작업을 방해한다. **③ 칼럼 수정모드** — 헤더 ✏️(`ColEditToggle`) 를 누르면 그 칼럼 전 셀이 입력창이 되고 Enter/Tab으로 다음 행, Shift+Enter/Tab으로 이전 행으로 이동해 연속 입력한다(특이사항 대량 입력용). **④ 즉시 자동저장** — 셀에서 빠져나오는 순간 그 칸만 `setDoc(..., {merge:true})`로 저장하고, 실패하면 화면 값을 원복한 뒤 알린다(낙관적 갱신). 저장 버튼을 기다리지 않는다 |

### 1-3. 정제 주소 3분할 (도로명/상세/괄호)

`processAddress` 결과는 정제 주소를 3조각으로도 노출한다 — 콤마 기준 분할(표시 함수 `parseDisplayedAddress` 와 동일):

| result 필드 | 내용 |
|---|---|
| `result.도로명주소` | 콤마 앞 = 도로명주소(건물번호까지) |
| `result.상세주소` | 나머지(동호수 등, 괄호 제외) |
| `result.괄호정보` | `(법정동, 건물명)` 내부 텍스트(괄호 기호 제외) |

용도: 기본명단 3컬럼 저장·표시 + 도로명주소 기준 비교. 기존 레코드는 `address`에서 `parseDisplayedAddress`로 즉시 파생(호환).

### 1-4. 동명이인 안전 매칭 (S-1~S-6) — 절대규칙 (2026-07-10 동대문 김옥순 사고 재발방지)

> 사고: 같은 행정동 동명이인 2인 중 주민센터 수령 요청자의 주소가 다른 사람의 집주소로 덮어써짐
> (`repair-address-tampering.mjs` 6/24 실행). 전문: `동명이인_주소오염_재발방지_설계.md`

| # | 규칙 |
|---|---|
| S-1 | 주소·특이사항 등 **개인 데이터를 쓰는 매칭은 강키만**: `이름+생년월일` 또는 `이름+전화끝8`. 이름 단독·이름+행정동 약키로 쓰기 금지 |
| S-2 | 약키는 **양측(원본·레코드) 모두에서 그 이름이 유일**할 때만. 어느 한쪽이라도 동명이인이면 스킵+수동확인 리포트. 강키도 후보 2+건(가족 공용전화)이면 스킵 |
| S-3 | **무도로명 원본(주민센터·시설 등)도 원본맵에 등록** — 그 인물의 레코드는 자동수리 제외(담당자 판단). 주민센터 판정은 저장주소·원본 양쪽에 적용 |
| S-4 | **원본 1행 = 레코드 1건 소비**. 같은 원본이 두 레코드에 재사용되면 이상신호 → 스킵 |
| S-5 | **임의 폴백 금지**: 본번 일치 원본이 없으면 교체하지 않는다 (`origs[0]` 류 금지) |
| S-6 | **저하 모드 쓰기 차단**: 주소를 쓰는 스크립트는 시작 시 매칭서비스 URL+헬스체크 강제, 행별 `matchSource` 확인. 미충족 → 스킵/중단 |

구현: 매칭 코어 `scripts/doppelganger-guard.mjs`(순수함수) + 회귀 테스트 `doppelganger-guard.test.mjs`(사고 픽스처 포함). 새 스크립트·기능이 이름 기반 매칭을 쓸 때는 반드시 이 모듈을 사용한다. 절대 되돌리지 말 것.

### 1-5. 무손실 원칙 (M-1~M-6) — 절대규칙 (2026-07-16 안양시 동안구 포수증발 사고 재발방지)

> 사고: 원본 2789명·3351포 → 내보내기 결과 2780명·3341포 (**9명 10포 증발**).
> 원인 ① 완전중복 3명(임미숙·문찬경·김정순 — 원본 엑셀에 동일행 2줄)을 정제 후 **자동 dedup 삭제**.
> 원인 ② 주민센터 수령 확인필요 6명이 "정제완료만 보기" 화면필터로 **내보내기에서 누락**.
> 해결: V6.81~V6.82. 형 원칙 = **대상자·포수 누락은 절대 발생하면 안 되고, 임의 삭제 금지·담당자 확인**.

| # | 규칙 |
|---|---|
| M-1 | **대상자·포수 누락 절대 금지**. 파싱된 원본 전건 = 정제 결과 = 저장·내보내기 전건. 어떤 단계도 대상자를 임의 삭제·누락시키지 않는다 |
| M-2 | **완전중복 자동삭제 금지**. 정제 후 동일인(이름+생년월일끝6 또는 이름+휴대폰끝8) 중복이어도 삭제하지 않고 **전건 유지 + `_중복의심`·`_중복그룹` 플래그만** 부여. 담당자가 [중복확인] 시트/화면에서 확인·결정. 구현: `App.jsx` 정제 루프 직후(과거 자동 dedup 삭제 코드 자리). **절대 되돌리지 말 것** |
| M-3 | **내보내기 전건 원칙**. 전체명단·[확인필요]·[행정동요약]·[기사별] 모두 화면필터(`filteredData`) 무시하고 **`gridData` 전건** 기준(확인필요·중복 포함). 구현: `App.jsx` handleExport/handleExportErrors/handleExportDongSummary/handleExportByDriver + `_buildExportFileName` 포수도 전건 |
| M-4 | **다운로드 다중시트**. 기본 내보내기 = **[정제결과](전건) + [확인필요] + [중복확인]** 별도 시트. 원본(정제결과)엔 전건 포함, 확인분만 별도 시트로 표시. 구현: `excelWorker.js` 표준 내보내기(`errorRows`·`dupRows`) + `App.jsx` `handleExport`·`_exportResultRows` |
| M-5 | **정합성 가드**. 정제 완료 시 파싱 입력 건수 ≠ 정제 결과 건수면 **즉시 경고**(alert). 구현: `App.jsx` `pushHistory(results)` 직후 |
| M-6 | **명+포 병기**. 명단·정제 화면 건수 표시에 **포수 합계 항상 병기**(포수 확인 최우선). 빈 포수=1(규칙 C-4). 포수 필드: gridData·cloud_lists=`포수`, base_lists=`qty`(영문) |
| M-7 | **원본 소계 포수 대조** (2026-07-16 신설·정태면 사고 후속). 원본 명단이 명시한 소계 포수(예: 수급자 4785포+차상위 1190포=5975)와 정제 결과 포수가 다르면 **즉시 경고**. M-5(건수 가드)는 파싱 *이후* 건수만 봐서 파싱 단계 누락(정태면)을 못 잡던 사각을 보완 — 파싱 전 원본이 선언한 값과 대조하므로 어느 단계에서 빠지든 잡아낸다. 구현: `excelWorker.js parseSheet` 소계 추출(`declaredQty`·`declaredHead`·`declaredPpl`, 헤더 근처 "N포/N세대/N명") + `App.jsx` 정제 `pushHistory` 직후 대조. 절대 되돌리지 말 것 |
| M-8 | **업로드 직후 자동 이상감지** (2026-07-16 신설 · CM-0 선제방어). 파일 파싱 직후 ①핵심 컬럼(이름·주소·수량) 미인식 ②데이터 0건 ③원본 소계(`declaredHead`·`declaredQty`) 대비 파싱 건수·포수 급감을 감지해 **정제 전에 담당자에게 경고**하고 자동확정을 끈다(확인 모달 강제). M-5·M-7은 정제 *후* 대조, **M-8은 업로드 *단계*에서 선제 방어** — 이상 서식을 정제 시작 전에 잡는다. 구현: `App.jsx` 파싱 콜백 `uploadAnomalies`. 절대 되돌리지 말 것 |
| M-9 | **특이사항 빈값 덮어쓰기 차단** (2026-07-22 신설 · 동대문구 3,074건 소실 사고 재발방지 · 절대 되돌리지 말 것). 사고 경위: 월 명단은 기본명단 특이사항을 `◆내용`으로 이식받는데(D-4), 그 월 명단을 기본명단에 저장할 때 B-9가 `◆...`를 통째로 제거해 `note=''`가 되고, **그 빈값이 기존 기본명단 특이사항을 덮어썼다**. 저장을 반복할수록 특이사항이 사라지는 구조였고, 동대문구에서 3,074건이 공란이 됐다(기준명단 엑셀 대조로 발견). **수정: 새 note가 비어 있으면 기존 값을 그대로 둔다**(`updData()` — 신규 추가 경로와 인플라이트 병합 경로 모두 적용). 구현: `App.jsx` 기본명단 저장부. 복원 도구: `scripts/restore-notes-from-baseline.mjs` |
| M-10 | **전월 대비 주소 대량변동 게이트** (2026-07-22 신설 · 형 지시 · 절대 되돌리지 말 것). 명단 전체가 통째로 틀어진 경우 — **다른 달 파일 · 다른 지자체 파일 · 주소 칼럼 밀림** — 는 행정동별 경고(30%↑·20건↑)로는 안 잡힌다. 동마다 30% 미만이면 경고가 뜨지 않고 그대로 저장돼 버리기 때문. → **전월과 대조된 인원 기준 전체 변동률**로 한 번 더 막는다. **임계: 대조 30명 이상이면서 변동률 15% 이상 또는 변동 100건 이상 → `critical`**. `critical`이면 ①전월비교 모달 자동 표시 ②🔴 '명단 오류 의심' 배너 ③**담당자가 체크박스로 확인하기 전까지 월 명단 저장·기본명단 저장을 모두 차단**(`blockedByAddrAlert`). 새 비교 결과가 나오면 확인 상태(`addrAlertAck`)는 초기화된다. 변동 판정은 기존 `isRealAddrChange`(괄호·공백·호수 위치 등 **포맷 차이는 무시**, 도로명+번지·상세 숫자만 비교)를 그대로 쓴다 — 정제만 된 건을 이사로 오판하지 않는다. 구현: `App.jsx` 전월비교 계산부 + `blockedByAddrAlert` + `PrevMonthCompareModal` |

관련: 저장 사라짐(월 목록 캐시)도 같은 무손실 취지 — [[19. 코드 보호 원칙]] `getDocsFromServer` 항목 참조.

---

## 2. DB 저장 규칙 (기본명단 — `base_lists`)

### 2-1. 3순위 매칭 시스템 (절대 변경 금지)

```
1순위: 이름 + 생년월일         → liveByBirth    (전체 레코드)
2순위: 이름 + 휴대폰           → liveByPhone    (생년월일 없는 레코드만)
3순위: 이름 + 유선전화         → liveByLandline (생년월일·휴대폰 모두 없는 레코드만)
```

### 2-2. 세부 규칙

| # | 규칙 | 상세 |
|---|------|------|
| B-1 | 저장 제외 조건 | 생년월일 + 휴대폰 + 유선전화 모두 없으면 저장 불가 |
| B-2 | 동명이인 중복 차단 | `birthKeyedNames` Set — 생년월일 있는 이름은 2순위·3순위 신규 추가 금지 |
| B-3 | 인덱스 키 정규화 | `digitKey = v.replace(/[^\d]/g, '')` — 숫자만으로 비교 (포맷 무관) |
| B-4 | 저장 포맷 | `normPhone = v.replace(/[^0-9-]/g, '')` — 대시 유지 |
| B-5 | 인플라이트 중복 | `_isInFlight` 플래그 — 같은 배치 내 신규→신규 중복 방지 |
| B-6 | 배치 커밋 단위 | **499건씩** (`writeBatch` 500 제한) |
| B-7 | 캐시 우회 | `getDocsFromServer` — 서버 최신 데이터 직접 조회 |
| B-8 | 스키마 혼재 지원 | 구버전(한국어 키: 이름/휴대폰) + 신버전(영문 키: name/mobile) 동시 지원 |
| B-9 | note 저장 | 기존 ◆ 이식 내용 제거 후 저장: `.replace(/\s*◆[^◆]*/g, '').trim()` |
| B-10 | Payload 필드 | `name, birthKey, dong, address, mobile, landline, note, driver, seqNo, sms` + **3분할(`roadAddr, detailAddr, parenInfo`)** |
| B-11 | 감사 로그 | `audit_logs` 컬렉션에 모든 배치 저장 기록 (action, city, addCount, updateCount, adminEmail) |
| B-12 | 3분할 주소 저장 | 저장 시 `parseDisplayedAddress(주소)`로 도출: `roadAddr`(도로명)·`detailAddr`(상세)·`parenInfo`(법정동,건물명). BaseListManager 3컬럼 표시(저장값 없으면 `address`에서 파생). 도로명주소 기준 전월/기본명단 비교에 사용 |
| B-13 | 대량 import 스크립트 | `scripts/import-base-lists.mjs` (vite-node로 실엔진 재사용 + firebase-admin 서비스계정 쓰기). `--count`(커버리지)/dry-run(기본)/`--write`. `특이사항_기본명단/` 7파일→base_lists. 무전화+무생년월일 행은 B-1로 스킵 |
| B-14 | 정규 지자체명 (절대) | base_lists/cloud_lists/delivery_history **문서ID는 정규 지자체명 = 도(道) 포함 풀네임** (예 `충청남도 천안시 동남구`, `경기도 시흥시`). 비정규명(`천안시 동남구`)·URL인코딩·손상(`�`)명 금지 → **지자체 분리/중복 유발**. import 스크립트 city 키도 도 포함 필수 |
| B-15 | 동일인 dedup·최신우선 | 매칭 시 동일인 = **이름 + (생년월일·휴대폰끝8·유선끝8 중 하나라도 일치)**. 매칭되면 최신(`updatedAt` 최대) 자료로 덮어쓰기, 과거 중복 삭제. 복구 스크립트 `scripts/repair-base-lists.mjs` (`--step A` 지자체통합 / `--step B` dedup, dry-run 기본·`--write` 실행) |
| B-16 | cloud_lists 손상정리 | cloud_lists도 손상(`�`)·URL인코딩 지자체 문서 발생 가능(주소만 있는 garbage 레코드). `scripts/repair-cloud-lists.mjs` — 완전레코드(이름+연락처) 없는 손상문서만 recursiveDelete(이중 안전검사), 정규 문서 보존. dry-run 기본·`--write` 실행 |

---

## 2-3. 이번달 명단 주소 정제 기능 (`CloudListManager`)

| # | 규칙 | 상세 |
|---|------|------|
| CL-1 | 주소 정제 버튼 | 이번달 명단 보기 상태에서 **주소 정제** 버튼 클릭 → 저장된 레코드 전체에 `processAddress` 적용 (asyncPool 10, progress 표시) |
| CL-2 | 변경 감지 | 정제 전 주소 ≠ 정제 후 주소인 레코드만 `addrChanges` 목록으로 추적 |
| CL-3 | 저번달 비교 | `delivery_history/{city}/months/{YYYY-MM-1}/records` 에서 name+birthKey 또는 name+phone(끝7자리) 매칭 → `prevAddr` 필드로 비교 표시 |
| CL-4 | 변경 유형 자동 감지 | **기본명단(base_lists) 도로명주소 비교 우선**(없으면 저번달). `오류`(확인필요) > 도로명 변경 시 **법정동↔법정동 규칙**: 새 주소 법정동 == 기본명단 법정동 → `이사`(같은 동 내), 다르면 `주민센터배송`(다른 동·관할 밖) > 도로명 동일 → `정제`. **법정동끼리 비교(행정동≠법정동 과탐 방지)** — 행정동 비교 금지. base_lists 매칭키: 이름+생년월일 또는 이름+휴대폰끝7. 모달에 정제/이사/주민센터/오류 집계 + 유형 수동변경 |
| CL-5 | 주소변경확인 버튼 | 변경 건수를 배지로 표시. 클릭 시 모달 오픈 |
| CL-6 | 모달 탭 구성 | **행정동 요약** (기본): 동별 정제/이사/오류 건수 + 이상 여부 / **전체 목록**: 기존주소·정제주소·저번달주소·유형·처리 |
| CL-7 | 행정동 이상 경고 | 저번달 대비 주소 변경 10건 이상인 행정동 → 빨간 `⚠ {동} 이상 N건` 배지 + 모달 내 "행정동 주소이상" 표시 |
| CL-8 | 담당자 처리 | 개별 원복(단일 변경 취소) / 재적용 / 유형 수동 변경(정제·이사·오류) / 전체 원복 가능 |
| CL-9 | 저장 방식 | 정제 결과는 `dirtyRecords`에 반영 → 기존 **변경사항 저장** 버튼으로 Firestore에 확정 커밋 |
| CL-10 | 정제 대상 필드 | 주소(`주소`), 확인필요/확인사유 — 이름·행정동·전화번호 등 나머지 필드 불변 |

---

## 3. 월별 클라우드 명단 저장 규칙 (`cloud_lists`)

| # | 규칙 | 상세 |
|---|------|------|
| C-1 | 저장 경로 | `cloud_lists/{city}/months/{YYYY-MM}/records` |
| C-2 | 중복 저장 방지 | 동월 데이터 존재 시 confirm 후 전체 삭제 → 재저장 |
| C-3 | 메타 문서 | city, monthId, totalCount, 수급자Count, 차상위Count, uploadedAt, uploadedBy |
| C-4 | 포수 기본값 | 빈값이면 1로 처리 |
| C-5 | 저장 대상 | `_에러` 없는 정상 데이터만 저장 |

---

## 4. 신규 데이터 이식 및 업데이트 규칙

| # | 규칙 | 상세 |
|---|------|------|
| D-1 | baseMap 키 생성 | `이름_생년월일` 또는 `이름_전화번호(숫자만)` |
| D-2 | 전체 이식 | `importFields === null` 이면 특이사항·기사·배송순번 전부 이식 |
| D-3 | 선택 이식 | `importFields` 가 Set이면 해당 필드만 이식 (note, driver, seqNo) |
| D-4 | ◆ 표시 | 기본명단 이식 특이사항에 `◆` 접두사 추가, 기존 `[기본]` 접두사 제거 |
| D-5 | 우선순위 | Excel 원본값 우선 — 빈값일 때만 기본명단에서 이식 |
| D-6 | 상세주소 이식 (2026-07-22 · 형 지시) | 기본명단 이식 대상에 **상세주소(`detailAddr`)** 추가 — 이번 달 명단 주소에 **호수·층이 없을 때만** 기본명단에 쌓인 값을 가져와 채운다. 명단 원본에 값이 있으면 **절대 덮어쓰지 않는다**(무손실 M-1). 상세주소 우선순위: **①명단 원본 → ②특이사항에서 승격한 호수(A-33) → ③기본명단 이식**. 선택 UI: `DbImportModal` IMPORT_FIELDS(기본 체크됨) · 전체이식(`importFields === null`)이면 자동 포함. 구현: `App.jsx` 정제 행 조립부 `importedDetail` |
| D-7 | 단건 실시간 업데이트 | 이름+생년월일 → 실패하면 이름+휴대폰 순서로 조회 후 upsert |

---

## 5. 컬럼 매핑 규칙

| 내부 키 | 감지 키워드 | 예외 |
|---------|-----------|------|
| `이름` | 이름, 성명, 대상자, 수령자명 | — |
| `주소` | 주소 | — |
| `수량(포수)` | 포수, 수량, 신청수량, 신청 수량, 10kg, 포, 구입량 | **'가구'·'세대' 포함 칼럼은 무조건 포수 매칭 금지(부분일치 전면 차단)** — `HOUSEHOLD_EXCL = ['가구','세대','인원','구성원','동거인','식구','명수']`. '가구'·'세대'만으로 가구원·가구수·가구당·세대원·세대수·세대당 등 인원 계열을 모두 차단한다 |
| `연락처` | 휴대, 연락, 전화, 유선, 핸드폰, 핸드, 모바일, 휴폰 | — |
| `행정동` | 행정동, 읍면동, 동명, 관할구역 | — |
| `비고` | 특이사항, 비고, 메모 | — |

추가 규칙:
- **CM-0 서식 견고성 원칙 (메타 · 최우선 · 무손실 M-1 연동)**: 정부양곡·복지 명단은 지자체·연도마다 서식이 제각각이다 — 병합 헤더, 유령 후행열, 유령 범위(`XFC` 등 16000+열), 특이 이름(면/읍으로 끝나는 사람), 0열부터 비어 시작, 수십 MB 비대 파일 등. **어떤 이상 서식이 들어와도 대상자·포수 누락 0, 헤더·컬럼 밀림 0**을 보장한다. 새 서식으로 밀림·누락이 발견되면 반드시 다음 사이클을 거친다: ①원본 파일 정밀 분석(SheetJS로 `!ref`·병합셀·헤더행·데이터 열 대조, 원본 소계 vs 정제 결과) → ②원인 코드 특정 → ③최소 diff 수정 → ④**하위 `CM-*` 규칙으로 박제** → ⑤Red-Green 검증(수정 전 재현 → 수정 후 해결). **CM-\* 규칙은 사고가 생길 때마다 계속 늘어난다**(형 방침: 규칙은 계속 보강·강화). 이 원칙과 하위 CM 규칙은 절대 되돌리지 말 것.
- 헤더 행 자동 탐지: 상위 40행 중 키워드 최다 매칭 행 선택 (바로 아래 행이 데이터처럼 보이면 가점)
- 병합 헤더: 최대 2행 서브헤더 통합 (데이터행 판단되면 중단)
- **CM-병합1 서브헤더 라벨 보호** (2026-07-16 중원구 차상위 컬럼밀림 사고 재발방지): 주소 3분할(구/동/번지/층/호수/상세) 등 **헤더 서브라벨은 글자수와 무관하게 데이터로 오판하지 않는다**(`HEADER_LABEL_RE`). 사고: 같은 서식에서 `번지, 층, 호수(상세)`가 공백 1칸 차이로 13자가 되어 '긴 한글=데이터(length>12)'로 오판 → 주소 서브헤더(동/번지) 통합 실패 → 헤더가 `col_4`·`col_5` 빈칸으로 남아 **차상위 시트만 컬럼 밀림**. 구현: `excelWorker.js parseSheet`. 절대 되돌리지 말 것
- **CM-병합2 유령 후행 빈열 제거**: 시트 범위(`!ref`)가 실제 데이터보다 넓게 잡힌 경우(과거 입력 흔적으로 M열까지 범위 잔존 등) **데이터가 전혀 없는 후행 열은 colCount에서 잘라낸다** — 불필요한 `col_N`이 헤더·컬럼 매핑을 밀리게 하지 않도록. 구현: `excelWorker.js parseSheet` colCount 계산 직후. 절대 되돌리지 말 것
- **CM-이름1 면/읍 이름 보호** (2026-07-16 정태면 누락 사고 재발방지 · 무손실 M-1): 본문 행 필터에서 이름이 `면/읍`으로 끝나도(예: **정태면**·OO읍) **전화·포수·주소 중 하나라도 개인 데이터가 있으면 사람으로 보존**한다. 개인 데이터가 전혀 없을 때만 통계표의 행정구역 단독 행(갈산면·광천읍 등)으로 간주해 제외. 구현: `excelWorker.js parseSheet` 본문 필터 `/^[가-힣]{1,6}(면|읍)$/`. 절대 되돌리지 말 것
- **CM-범위1 유령 시트범위 정정** (2026-07-16 차상위 양곡 서식 XFC 밀림 사고 재발방지): 시트 `!ref`가 실제 데이터보다 과도하게 넓으면(예: `A1:XFC892`처럼 16000+열 — 과거 서식/편집 흔적으로 파일이 34MB로 비대) **파싱 전 실제 셀 기준으로 범위를 재계산**한다. 이 정정이 없으면 `sheet_to_json`이 오작동해 데이터를 못 읽거나(`blankrows:false`에서 0행) 컬럼이 밀린다. **★시작 열도 실제 데이터 시작 열(`minC`)부터** 잡는다 — `A`열 등 **빈 선행열을 포함하면 헤더는 정리되고 데이터는 안 돼 구분부터 전체가 한 칸씩 밀린다**(A열 빈칸 밀림 사고). 구현: `excelWorker.js fixSheetRange` — `XLSX.read` 직후 모든 시트에 적용(열 200 초과일 때만, 정상 파일 성능 보호), 시작 좌표 `{r:0, c:minC}`. 절대 되돌리지 말 것
- 시트 유형: `수급/의료/생계` → 기초수급자 / `차상위` → 차상위 / 나머지 → 기초수급자
- 혼합 시트: `구분/유형/계층/자격` 컬럼 데이터 값으로 자동 감지

### 5-1. 형식(내용) 기반 자동 매칭 규칙 (헤더 키워드와 병행 — 매칭 100% 목표)

헤더 키워드로 애매하면 **데이터 샘플(상위 N행)의 값 형식**으로 판정한다.

| 내부 키 | 데이터 형식(내용) 시그니처 | 판정·예외 |
|---------|----------------------------|-----------|
| `이름` | 2~5자 한글 이름, 숫자·기호 거의 없음 | 헤더 키워드 우선 |
| `행정동`(읍면동) | `OO동/읍/면` 짧은 행정구역명 다수 | 주소와 구분(도로명·번지·호 없음) |
| `주소` | 도로명/지번 포함 긴 텍스트(로·대로·길·번지·동·호) | 가장 긴 텍스트 칼럼 |
| `휴대폰` | 010/011/016~019 + 11자리 형식이 **다수**인 칼럼 | **휴대폰 형식 아니면 매칭 불가**(지역번호뿐이면 유선으로) |
| `유선전화` | 지역번호(02/0XX) 형식 연락처 | 휴대폰 칼럼과 분리 |
| `포수` | 1·2·3 등 소수 정수, 헤더 `포수/수량/신청수량/10kg/포/구입량` | **'가구'·'세대' 포함 칼럼 절대 금지(부분일치 차단)** |
| `문자수신` | `Y/N/O/X`(○/×/예/아니오 포함) 값만 | — |
| `생년월일` | 날짜/달력 데이터(YYYYMMDD·YY.MM.DD·엑셀 날짜) | — |
| `구분` | 의료/생계/수급/기초/차상위 값 | **`기초수급자`·`차상위` 2가지로만 정규화** |
| `특이사항` | 배송 도움 메모(부재·경비실·문앞·호출·계단 등) | 비고/특이/메모 |

판정 순서: ① 헤더 키워드 → ② 데이터 샘플 형식 다수결 → ③ 충돌 시 **형식 우선**(연락처는 휴대/유선 형식으로 분리). 각 칼럼 **매칭 신뢰도 점수화** → 100% 아니면 쉬운정제에서 사용자 1클릭 확인.

### 5-2. 지자체 자동 감지 + 허가 지자체 대조

- 파일명/시트명/요약행 + **주소 칼럼의 시·군·구 토큰**에서 지자체 후보 추출.
- **사용자 허가 지자체(`userCities`)와 대조** → 일치하는 **정확한 지자체명** 확정.
- 후보 불명확 시: 명단 주소의 **최빈 시·군·구** → `userCities` 매칭. 그래도 불명확하면 사용자에게 확인(CityMonthPickerModal).

---

## 6. 정렬 규칙

- **기본 정렬 (4단계 우선순위)**: 행정동(읍면동) → 리(里) → 주소(도로명) → 이름 (모두 오름차순, 한국어 numeric). 리 없으면 해당 단계 빈값으로 자연 통과
- **컬럼 클릭 정렬**: 해당 컬럼 단일 정렬 (클릭 반복 시 asc ↔ desc 토글)

---

## 7. 특이사항 조합 규칙

조합 순서 (공백으로 join, 빈값 제외):

```
1. 주소 엔진 결과  (건물명 분리, 본명 초과, 특수문자 뒤 내용)
2. 전화번호 레이블 (레이블(번호) 형식 — 예: 딸(010-1234-5678))
3. 원본 비고 컬럼 값
4. 기본명단 이식   (◆이식내용)
```

---

## 8. 전화번호 처리 규칙

| 규칙 | 내용 |
|------|------|
| 번호+텍스트 분리 | `extractPhoneNote` — 9자리 미만이면 전화번호 아님 |
| 구분자 | 공백, /, \|, 쉼표, 세미콜론, 콜론, 괄호, 대시+텍스트 |
| 포맷 | 11자리→010-XXXX-XXXX, 10자리(02)→02-XXXX-XXXX, 9자리(02)→02-XXX-XXXX |
| 모바일 판별 | 010/011/016/017/018/019 시작 |
| 레이블 형식 | `레이블(번호)` — 예) `딸(010-1234-5678) 형님번호(010-0000-0000)` |
| 내부 중복 제거 | `parsePhoneNumbers` 에서 formatPhone 공용 함수 사용 (별도 정의 금지) |

---

## 9. 생년월일 정규화 규칙

| 입력 | 변환 결과 |
|------|----------|
| 8자리 숫자 (19750315) | 앞 2자리 버리고 `75.03.15` |
| 6자리 숫자 (750315) | `75.03.15` |
| `YY-MM-DD` / `YY/MM/DD` | `-` → `.` 치환 |
| 비정형 | 원문 반환 |

---

## 10. 문자수신 파싱 규칙

| 결과 | 패턴 |
|------|------|
| **N (거부 우선)** | 거부/거절/불가/불허/미동의/미수신/미신청/부동의/비동의/수신안/안받/안함/미함/무응답/해지/중지 |
| N | N, n, X, x, ×, 0, F, f, 아니오, 불, 없음, 부, 무, 미, 안 |
| Y | Y, y, O, o, ○, ㅇ, 1, T, t, 예, 여, 유, 함, 동의, 수신, 가능, 허용, 신청, 승낙, 찬성, yes, true (+ 동의/수신/가능/허용/신청/승낙/찬성 포함 문구) |
| N | 패턴 미매칭 기본값 (내부 공백 제거 후 판정) |

---

## 11. 중복 분석 규칙 (부가서비스)

| 유형 | 기준 | 보존 행 |
|------|------|---------|
| 강한 중복 | 이름+생년월일+전화 모두 일치 | 마지막 행 |
| 약한 중복 | 행정동+이름+전화 일치 | 특이사항 있는 행 우선, 없으면 마지막 행 |
| 특이사항 없음 | 중복 아니어도 특이사항 공란 | 삭제 추천 대상 |

---

## 12. 내보내기 규칙 (파일명 + 무손실 다중시트)

### 파일명

```
{지자체명}-{X}월-기초{N},차상위{M},전체{T}-{MMDD}{HHMMSS}.xlsx
[확인필요]{위 형식}.xlsx
[행정동요약]{위 형식}.xlsx
```

- 파일명 포수(N/M/T)는 **전건(`gridData`) 기준** — 화면필터와 무관하게 전체 포수 표기(M-3).

### 무손실 다중시트 (M-3·M-4 · 절대규칙)

- 기본 내보내기 파일 = **[정제결과](전건) + [확인필요] + [중복확인]** 시트.
- 모든 내보내기는 `filteredData`(화면필터)가 아니라 **`gridData` 전건** 기준 → 대상자·포수 누락 0.
- 확인필요·중복의심은 담당자 확인용 별도 시트로 표시하되 [정제결과]엔 **전건 그대로 포함**(누락 아님).
- 구현: `excelWorker.js` 표준 내보내기(`finalRows`+`errorRows`+`dupRows`) + `App.jsx` `handleExport`·`_exportResultRows`. 상세는 [[1-5. 무손실 원칙]] 참조.

---

## 13. Undo 규칙

- 최대 10단계 이전 상태 복원
- gridData 모든 변경은 반드시 `pushHistory` 경유

---

## 14. 인증·권한 규칙

| 규칙 | 내용 |
|------|------|
| 관리자 이메일 | `ttong627@gmail.com` |
| 관리자 강제 보정 | admin 이메일이면 role=admin, tier=sapphire 강제 설정 (onAuthStateChanged 시 자동 복구) |
| 일반 유저 강등 | 비admin이 role=admin인 경우 role=user, tier=basic으로 강등 |

---

## 15. 인트로 화면 규칙

- **표시 조건**: 신규 회원가입 + 지자체·성명 등록 완료 직후 1회만
- **이후 금지**: 같은 계정 재로그인 시 표시 안 함
- **구현**: `ProfileSetupModal` `onClose` 에서 `isNewUser === true` 였을 때만 `setShowIntro(true)`

---

## 16. 기사 배분 규칙 (루트맵)

### 제1원칙 (절대 원칙)
> **기사 구역은 완전히 분리된다. 지리적 겹침 절대 불가.**
> capacity 비례 포수 배분 · 이동거리 최소화 · 전건 배정 보장을 목표로 한다.

---

### R-0. 배타적 구역 (모든 규칙의 전제)

기사 구역은 완전히 분리되는 것을 목표로 한다. 단, **혼재(겹침)가 남아도 최종 저장은 가능**하다 — 담당자 확인(confirm) 후 진행한다(저장은 항상 가능해야 함).
구현: `handleSaveSession(isFinal=true)` 진입 시 `overlapCount > 0`이면 차단하지 않고 `window.confirm` 경고 후 진행.

### R-A. 전건 배정 보장

970건 전원 배정 완료. 미배정 레코드 → 유효부담 가장 적은 기사에게 강제 배정.
구현: `handleAutoSplit` 후처리 마지막 단계.

### R-B. 좌표 없으면 자동배정 잠금

좌표 미수신 건수 > 0이면 "자동 N등분" 버튼 비활성화.
좌표 매칭 실행 후 여전히 실패한 건 → 같은 동 최다배정 기사에게 자동 귀속.
구현: `hasRunGeocoding` state, `handleFetchMissingCoords` 완료 후 `true`.

### R-C. capacity 비례 포수 배분

기사별 목표 유효부담 = 전체유효부담 × (capacity / capacity 합계).
예) 3명 85%/100%/70% → 합계 255% → 373포/439포/307포.
구현: `kMeansCluster` `targetLoads` 계산 (유효부담 기준).

### R-D. 절대 경계 (장벽 기반 구역 분리)

핀을 장벽 올바른 쪽에 배치하면 보로노이/K-means가 자연히 경계를 따름.

| 장벽 유형 | 분류 |
|-----------|------|
| 고속도로 · 자동차전용도로 | 절대 경계 |
| 철도 · 지하철 지상구간 · 기찻길 | 절대 경계 |
| 하천 · 강 (교량 1개 이하) | 절대 경계 |
| 왕복 4차선 이상 대로 | 권장 경계 |

### R-E. 동일 주소 동일 기사

같은 주소의 복수 수급자는 반드시 같은 기사 배정.
구현: `handleAutoSplit` 후처리 — 주소별 최다배정 기사로 통일.

### R-F. 겹침 감지 → 해소 권장 (저장은 강제 아님)

혼재(겹침) = **아파트 단지 내로만 한정** — 같은 단지(또는 분할 동)가 여러 기사로 갈라진 경우만 카운트. 도로/동일주소 단위·권역 고립 "섬"은 제외. 0건으로 해소 권장하되, **남아 있어도 확인 후 최종 저장 가능**(R-0 참조).
구현: `getMixedRouteUnitIssues`(apt 단위만), `overlapCount`, `handleResolveOverlap`.

### R-G. 이전달 구역 우선 승계

기사명 동일하면 이전달 좌표·기사·순번 자동 이식.
구현: `handleLoadPrevMonth` 버튼.

### R-H. 유효부담 환산

실제 배송 노동량을 반영한 가중 포수 계산.

| 조건 | 환산 |
|------|------|
| 20포↑ + 임대키워드(LH·SH·임대 등) | × 0.3 |
| 빌라·연립·다세대·단독 + 층수 | × (1 + min(층,5) × 0.1) |
| 문앞·거동불편·직접전달 등 | × 1.5 |
| 전화필수·골목·경비실 등 | × 1.2 |
| 기타 | × 1.0 |

구현: `getEffectiveLoad()` 함수.

### R-I. 아파트 단지 동일 기사 + 대형단지 분할

기본: 같은 단지명 전체 → 자동배정 후 최다배정 기사로 통일.
예외: 50포↑ 임대 단지는 담당자가 동(棟) 단위로 선택적 분할 가능.

구현:
- `handleAutoSplit` 후처리 — 단지별 최다배정 기사로 통일
- 좌측 패널 "대형단지" 섹션 — 50포↑ 단지 목록 + 기사 드롭다운 + [분할] 버튼
- `handleAptDriverChange` — 단지 전체 일괄 기사 변경
- `openAptSplitModal` / `applyAptSplit` — 동 단위 분할 모달

### R-J. 외곽 고립 배송지 묶음

주변 300m 이내 동일기사 레코드 < 3건인 고립 레코드:
1순위 → 해당 방향으로 구역이 이어지는 기사
2순위 → capacity 여유 있는 기사 (목표의 80% 미만)
3순위 → 포수 가장 적은 기사
같은 방향 외곽 2건↑ → 반드시 1명에게 묶음.

### R-K. 배송 밀집도 점수

기사별 배송순번 인접 레코드 간 평균 이동거리.
200m 미만 → 초록 / 200~400m → 노랑 / 400m↑ → 빨강.
구현: 기사 카드 하단 표시.

### R-L. 행정동 원자성

1행정동 = 1기사 원칙. 포수 과다 시에만 2기사 허용.
보로노이/K-means가 자연히 처리. 결과 확인 후 수동 조정.

### R-M. 아파트/단독 비율 균형

한 기사에게 아파트만 또는 단독만 몰리지 않도록 구성.
자동 배정 결과 확인 후 필요 시 수동 조정.

### R-N. 배송 연속성 점수

순번 인접 레코드 간 300m↑ 이동 횟수(역주행 횟수). 적을수록 좋음.
구현: 기사 카드 하단 표시.

---

### 배분 알고리즘 적용 순서

```
1. buildUnits() — 아파트/동일주소/좌표 기준 배송단위 묶기 (R-E·R-I 전제)
   ※ 절대 원칙: 새 알고리즘의 입력 단위는 항상 buildUnits() 출력이어야 한다.
      개별 레코드를 직접 배정하면 R-E·R-I·도로측면 규칙이 모두 깨진다.
2. 전략 선택 (autoSplitStrategy)
   - pca (기본값): PCA 주축 투영 → 1D 정렬 → DP 연속 분할
   - hilbert (실험): 힐베르트 공간충전곡선 인덱스 정렬 → DP 연속 분할
   - bestOfPcaHilbert (실험): pca·hilbert 둘 다 계산 → qualityScore 높은 쪽 채택
   - seedVoronoi (예정): N-씨앗 보로노이 + 연속성 강제 (향후 구현)
3. 좌표 없는 레코드 → 같은 행정동 최다배정 기사 자동 귀속
4. 미배정 전건 → 좌표 가까운 기사 배정 → fallback: 유효부담 최소 기사 강제 배정 (R-A)
```

구현 위치: `routeWorker.js computeAutoSplit()`

### autoSplitStrategy 허용값 및 기본값

| 값 | 설명 | 기본값 |
|----|------|--------|
| `pca` | PCA 주축 투영 정렬 (현재 운영 방식) | ✅ 기본값 |
| `hilbert` | 힐베르트 공간충전곡선 정렬 (실험) | — |
| `bestOfPcaHilbert` | pca·hilbert 비교 후 qualityScore 높은 쪽 자동 선택 | — |
| `seedVoronoi` | N-씨앗 보로노이 (향후 구현) | — |

허용값 외 입력 → `pca` 로 자동 fallback.

### qualityScore 공식 (0–100점, 높을수록 좋음)

```
qualityScore = 100
  - 25 × (splitUnitCount    / max(1, totalMandatoryUnits))  // 배송단위 분리 — 최우선 결함
  - 20 × (islandCount       / max(1, totalDrivers))         // 구역 연속성
  - 20 × (mixedUnitCount    / max(1, totalUnits))           // 섬 혼재 단위
  - 15 × (maxAbsDiffPct     / 100)                          // 부담 편차
  - 10 × (roadSideMixCount  / max(1, totalRoadUnits))       // 도로측면 순수성
  - 10 × (forcedAssignCount / max(1, totalRecords))         // 강제배정 비율

clamp(0, 100)

splitUnitCount  : buildUnits()의 mandatory 단위가 2개 이상 기사로 분리된 수
mixedUnitCount  : 인접 기사 구역에 고립된 배송단위 수 (splitUnitCount와 별개)
islandCount     : 구역이 연속되지 않는(섬 포함) 기사 수
roadSideMixCount: 같은 도로 홀수/짝수 측면이 다른 기사에 배정된 도로 수
forcedAssignCount: 좌표·행정동 fallback으로 강제 배정된 레코드 수
```

---

## 17. 배송순번 독립 규칙

배송순번은 자동 기사배정의 일부가 아니라, 기사 배정이 끝난 뒤 별도 실행되는 독립 단계다.
자동 N등분은 `_driverId` 배정까지만 책임지고, 배송순번은 `RouteMapModal` 의 순번 엔진이 기사별로 다시 계산한다.

### DS-1. 실행 전제

| 규칙 | 내용 |
|------|------|
| DS-1-1 | 배송순번은 기사 배정 후 실행한다. `_driverId` 가 없는 레코드는 자동 순번 대상이 아니다 |
| DS-1-2 | 배송순번은 기사별로 독립 계산한다. 기사 A의 1번과 기사 B의 1번은 서로 충돌이 아니다 |
| DS-1-3 | 브러시 보정 직후 순번을 실행하면 `pendingPaintRef` 의 임시 배정을 먼저 반영한 뒤 순번을 계산한다 |
| DS-1-4 | 기존 순번이 있어도 일반 순번은 덮어쓸 수 있다. 관리자 AI 순번은 덮어쓰기 전 확인창을 띄운다 |

### DS-2. 일반 자동 배송순번 로직

구현 위치: `RouteMapModal.handleAutoSequence()`

```text
1. pendingPaintRef 임시 배정을 records 에 먼저 반영
2. 기사별로 `_driverId === driver.id` 인 레코드만 추출
3. 도로명 또는 아파트 정보가 하나라도 있으면 roadAwareTSP(driverRecs) 실행
4. 도로명/아파트 정보가 없고 좌표만 있으면 nearestNeighborTSP(coordRecs) fallback
5. 계산 결과를 해당 기사 안에서 1부터 순차 부여
6. analyzeSequenceQuality() 실행 후 순번 분석 패널 표시
```

### DS-3. roadAwareTSP 기준

| 단계 | 기준 |
|------|------|
| DS-3-1 | 도로명·아파트·동일주소를 배송순번 단위로 묶는다 |
| DS-3-2 | 같은 도로 안에서는 건물번호 흐름을 우선한다 |
| DS-3-3 | 시작 도로는 가장 북쪽 도로 그룹을 우선한다 |
| DS-3-4 | 다음 도로 그룹은 최근접 이웃 방식으로 선택한다 |
| DS-3-5 | 2-opt 로 교차 동선을 줄인다 |
| DS-3-6 | 각 도로 그룹은 이전 도착점에서 더 가까운 진입 방향(정방향/역방향)을 선택한다 |
| DS-3-7 | 좌표 없는 레코드는 거리 최적화에 직접 쓰지 않고 뒤쪽 확인 대상으로 붙인다 |

### DS-4. nearestNeighborTSP fallback 기준

| 단계 | 기준 |
|------|------|
| DS-4-1 | 도로명/아파트 정보가 부족한 기사 명단은 좌표 기반 최근접 이웃 순회로 계산한다 |
| DS-4-2 | 시작점은 가장 북쪽 좌표를 우선한다 |
| DS-4-3 | 매 단계에서 현재 지점과 가장 가까운 미방문 좌표를 다음 순번으로 선택한다 |
| DS-4-4 | 2-opt 교차 제거로 총 이동거리를 줄인다 |
| DS-4-5 | 좌표가 없는 레코드는 좌표 기반 fallback 순번 대상에서 제외하고 분석 경고로 남긴다 |

### DS-5. 수동 순번 편집

| 기능 | 규칙 |
|------|------|
| 클릭 순번 | 목록 행을 클릭하는 순서대로 현재 기사 안에서 1→2→3 순번을 부여한다 |
| 드래그 순번 | 같은 기사 안에서만 드래그 재정렬을 허용하고, 재정렬 후 1부터 다시 번호를 매긴다 |
| 직접 입력 | 목록의 배송순번 입력칸에서 수동 수정할 수 있다 |
| 기사 간 이동 | 순번 드래그는 기사 간 이동을 허용하지 않는다. 기사 변경은 배정 기능으로 처리한다 |
| 전체 초기화 | 배송순번 전체 초기화는 모든 `배송순번` 을 빈값으로 만든다 |

### DS-6. 관리자 AI 순번

| 규칙 | 내용 |
|------|------|
| DS-6-1 | 관리자 전용 기능이다 |
| DS-6-2 | 기사 배정이 1건 이상 있어야 실행한다 |
| DS-6-3 | 기존 배송순번이 있으면 덮어쓰기 확인창을 띄운다 |
| DS-6-4 | Kakao REST 키가 있으면 차량 경로 후보를 활용하고, 없으면 로컬 거리 계산으로 fallback 한다 |
| DS-6-5 | 실행 후 예상 정확도, 확인 필요 건수, Kakao/local 경로 계산 비율을 표시한다 |

### DS-7. 순번 품질 분석

구현 위치: `RouteMapModal.analyzeSequenceQuality()`

| 분석 항목 | 기준 |
|----------|------|
| 점프 | 인접 배송순번 간 300m 이상 이동이며, 같은 아파트/같은 도로측면이면 경고를 완화한다 |
| 도보 후보 | 인접 배송순번 간 120m 이하 이동 |
| 좌표 없음 | 순번은 있으나 좌표가 없어 거리 검증이 불가능한 레코드 |
| 도로 정보 없음 | 도로명 파싱이 불가능하고 아파트 후보도 아닌 레코드 |
| 도로 재방문 | 이미 지나간 도로가 순번 뒤쪽에서 다시 등장하는 경우 |
| 평균 이동거리 | 기사별 인접 순번 간 평균 이동거리 |
| 최대 이동거리 | 기사별 인접 순번 간 최대 이동거리 |
| 예상 정확도 | 점프·좌표없음·도로없음·도로재방문은 감점, 도보 후보는 소폭 가점 |

품질 기준:

```text
평균 이동거리 200m 미만  → 좋음
평균 이동거리 200~400m → 주의
평균 이동거리 400m 이상 → 위험
인접 순번 300m 이상 점프 → 확인 필요
```

### DS-8. 저장·이식·내보내기

| 구분 | 규칙 |
|------|------|
| 기본명단 | `seqNo` 로 저장하고, 전체 이식 또는 `seqNo` 선택 이식 때만 가져온다 |
| 월별 명단 | `cloud_lists/{city}/months/{YYYY-MM}/records` 에 `배송순번` 으로 저장한다 |
| delivery_history | 전월 비교·복원용으로 `seqNo` 를 함께 저장한다 |
| 이전달 승계 | 기사명이 같으면 이전달 기사·좌표·배송순번을 복원할 수 있다 |
| 기사 요청 반영 | 기사 공유 링크의 순번 요청은 담당자 유선 확인 후 해당 기사 배송순번만 공식 반영한다 |
| 내보내기 | 배송표는 `배송순번` 오름차순으로 정렬하고, 순번 없는 행은 원래 순서를 최대한 유지한다 |

### DS-9. 기사별 출발점 (Driver Start Point)

구현 위치: `RouteMapModal` 기사 카드 UI + `handleGeocodeStartAddr`

| 규칙 | 내용 |
|------|------|
| DS-9-1 | 기사 카드에 `출발지` 주소 입력란을 표시한다 |
| DS-9-2 | 기본 출발지: `경기도 수원시 장안구 정자천로188번길 39` (모달 마운트 시 자동 적용) |
| DS-9-3 | Kakao REST API (`/v2/local/search/address.json`)로 입력 주소를 지오코딩하여 `startLat`, `startLng` 저장 |
| DS-9-4 | `roadAwareTSP`, `nearestNeighborTSP` 모두 `startPoint` 파라미터를 받아 가장 가까운 배송단위부터 시작한다 |
| DS-9-5 | `startAddr`, `startLat`, `startLng` 는 세션 저장·delivery_history·공유 링크 등 **외부 출력에 포함하지 않는다** (보안) |
| DS-9-6 | 모달 마운트 시 모든 기사의 `startAddr` 가 있고 `startLat` 없으면 자동 지오코딩 실행 |

### DS-12. 아파트·빌라 동→층 정렬

구현 위치: `RouteMapModal.sortSequenceUnitRecords()` + `parseInferredFloor()`

층수 파싱 기준 (`parseInferredFloor`):

| 입력 예시 | 결과 | 비고 |
| --- | --- | --- |
| `503호` | 5층 | 3자리 → 첫 자리 |
| `1503호` | 15층 | 4자리 → 앞 두 자리 |
| `101호` | 1층 | 3자리 → 첫 자리 |
| `1001호` | 10층 | 4자리 → 앞 두 자리 |
| `B101호` (동 없음) | 지하 1층 (-1) | B + 첫 자리 = 지하 층 |
| `지하101호` | 지하 1층 (-1) | 지하 prefix |
| `B동 101호` | 1층 | B동 = 건물동명, 지하 오인 금지 |
| `가101호` | skip (null) | 가·나·다... + 숫자 = 건물동 표기 |
| `상가 203호` | skip (null) | 상가 키워드 |
| `나동 503호` | 5층 | 동 표기 제거 후 503호 → 5층 |

정렬 우선순위: 동(棟) → 추론 층 오름차순 → 호수 (층 추론 실패 시 기존 `parseFloorHo` fallback)

### DS-14. 총거리·예상시간 표시

구현 위치: `RouteMapModal.analyzeSequenceQuality()` + 순번 분석 카드 UI

| 규칙 | 내용 |
|------|------|
| DS-14-1 | 기사별 인접 순번 간 haversine 거리 합산 → `totalDistKm` (소수 1자리, m→km) |
| DS-14-2 | `SEQUENCE_ESTIMATED_SPEED_KMH = 10` 상수 (도심 배송 속도, 정차·엘리베이터 포함) — 모듈 상단에 분리 |
| DS-14-3 | `estimatedMinutes = round((totalDistKm / SEQUENCE_ESTIMATED_SPEED_KMH) × 60)` |
| DS-14-4 | 순번 분석 카드 하단에 `총 {N}km · 약 {M}분 · {count}건` 표시 |
| DS-14-5 | `SEQUENCE_ESTIMATED_SPEED_KMH` 는 절대 하드코딩 금지 — 상수만 수정하여 전체 반영 |
| DS-15 | **좌표 오류 거리기반 검증** (2026-07-23 신설·형 지시 · 절대 되돌리지 말 것). 배송순번 교차·먼거리 왕복의 진짜 원인은 순번 알고리즘이 아니라 **좌표 오류**다(실측: 시흥 안금순 좌표 190km 밖 → 기사 구역 430km로 부풀림 · 오류 379건 정리 후 이성우 430→58km, 평균구역폭 31→11km). 엔진 `src/engine/coordValidator.js` `detectCoordOutliers`: 지자체 좌표 **중앙값** 중심에서 임계 반경(기본 25km) 벗어난 좌표를 API 없이 검출(중앙값이라 오류 좌표에 안 흔들림). 정리 도구 `scripts/fix-coord-outliers.mjs`(재지오코딩=nexus와 동일 Kakao 지자체접두어+`isInRegion` 관할필터, 실패 시 좌표제거+플래그, 백업·audit). 회귀 `scripts/coord-validator.test.mjs`. **순번 알고리즘(roadAwareTSP)은 이미 최적이라 변경 금지** — 3자 비교(nexus vs yyplus 도로명/좌표)로 현행이 되돌아가기 0·최단임을 증명 |
| DS-16 | **수량 기반 일자 분할** (2026-07-23 신설·형 지시). 수량 많으면 지역(동)별로 묶어 여러 날로 나눈다. 엔진 `src/engine/deliveryDaySplit.js` `splitByDay(records, {maxLoadPerDay|numDays, getLoad, depot})`: **①하루 최대 물량 자동분할 ②날짜 개수 직접지정** 둘 다. **행정동=권역 단위(같은 동 절대 안 쪼갬)**, 동을 지리적 최근접 체인으로 정렬 후 배분 → 가까운 동끼리 같은 날(동선 최소). 물량은 `getEffectiveLoad`(체감물량) 주입. numDays 모드는 정확히 N일 보장(마지막 날 초과 허용). 결과 `배송일차`(1..N) 저장, 각 날 안의 방문순번은 [순번] 버튼(roadAwareTSP)이 담당. UI: RouteMapModal 상단 [일자분할] 패널. 회귀 `scripts/delivery-day-split.test.mjs` 13 PASS |

---

## 18. 루트맵 UI 조작 규칙 (RouteMapModal)

### 자동 배정 버튼

"자동 N등분" 단일 버튼. `autoSplitStrategy` 기준 자동 기사배정 + R-A~R-I 후처리를 일괄 실행한다.
좌표 미수신 > 0이고 좌표매칭 미실행 시 버튼 잠금 (R-B).

### 기사 핀 (거점 배치)

- 기사 카드의 [핀 꽂기] 버튼 클릭 → 지도 클릭 1회로 거점 위치 확정 (이후 클릭 무시)
- 핀 = 기사 구역의 좌우/상하 순서를 정하는 기준
- 핀 전체 있으면 핀 주축 순서대로 연속 권역을 분할하고, 없으면 좌표 분포 주축으로 연속 권역 균형 분할
- 핀 제거: [×] 버튼 클릭

### 연속 권역 균형 기준

```text
targetLoad = 전체유효부담 × (capacity / 전체capacity)

아파트 도로명주소 그룹, 대로/로 좌우 측면, 같은좌표 묶음을 배정 단위로 만든 뒤
지도 좌표의 PCA 주축 또는 선택된 autoSplitStrategy 기준으로 정렬하여 각 기사 targetLoad에 가까운 연속 구간을 배정한다.
가까운 점을 하나씩 따라가는 TSP식 순회 경로는 자동배정 경계 산정에 사용하지 않는다.
TSP식 순회는 밀집 지역에서 구역을 뱀처럼 왕복시켜 색이 섞일 수 있으므로 배송순번 계산에만 사용한다.
```

`getEffectiveLoad()` 가중치(임대 0.3×, 계단 층수 보정, 문앞 1.5× 등) 그대로 반영.

### 페인트 브러시 모드 (2차 수동 보정)

- 기사 카드의 [브러시] 버튼 클릭 → 해당 기사 색상 페인트 모드 활성
- 지도 위에서 마우스 드래그 → 브러시 반경 내 모든 핀을 선택 기사로 일괄 재배정
- 반경 슬라이더로 브러시 크기 조절
- [브러시 종료] 버튼 또는 다른 기사 선택 시 모드 해제

### 대형단지 패널 (좌측 패널 하단)

50포↑ 아파트 단지 목록. 기사 드롭다운으로 단지 전체 일괄 변경.
[분할] 버튼 → 동 단위 분할 모달.

### 배정 관리

- **배정 전체 초기화**: 모든 `_driverId → null`. Undo 없으므로 신중히 사용
- **핀 우클릭**: 개별 핀 미배정 전환
- **최종저장**: 겹침 > 0건이면 확인(confirm) 후 저장 진행 (R-0 — 차단 아님)

---

## 19. 코드 보호 원칙

- 기존 작동 기능 임의 변경 금지
- 배치 499 제한 절대 초과 금지
- 3순위 매칭 시스템 구조 변경 시 전체 인덱스 재검토 필수
- `getDocsFromServer` → `getDocs` 로 교체 금지 (캐시 오염 위험). **CloudListManager 월 목록 조회(`fetchMonths`·카드 열기)도 반드시 `getDocsFromServer`** — `getDocs`면 저장 직후 새 월/건수가 캐시 miss로 안 보임("저장했는데 사라짐")
- **완전중복 자동삭제 코드 부활 금지** (M-2) — 정제 후 dedup은 삭제가 아니라 `_중복의심` 표시만. 대상자 누락 절대 금지
- **내보내기 소스 `gridData`→`filteredData` 되돌리기 금지** (M-3) — 화면필터로 대상자·포수 누락 발생
- `pushHistory` 경유 없이 `setGridData` 직접 호출 금지 (Undo 파괴)
- `SEQUENCE_ESTIMATED_SPEED_KMH` 상수 하드코딩 금지 — 모듈 상단 상수만 수정
- `parseInferredFloor` 로직 변경 시 DS-12 전체 테스트 케이스 재검토 필수
- `CityMonthPickerModal` 컴포넌트 수정 시 `nexus_city_prefs_v2` 키 구조 유지
- 파일 업로드 후 Step2 직접 이동 금지 — 반드시 `pendingSetup` → `showCityPicker` → `handleCityMonthConfirm` 경로 경유
- 작업 완료 후 항상 `npm run build && firebase deploy --only hosting` 실행

---

## 20. 업로드 워크플로우 (CityMonthPickerModal)

### 흐름

```text
파일 업로드 (Web Worker 파싱 완료)
  → setPendingSetup({ sheetsData, initialSel, detectedCity, detectedMonth })
  → setShowCityPicker(true)          ← Step2 직접 이동 금지
  ↓
CityMonthPickerModal 표시
  → 사용자가 지자체 · 적용 월 확인/수정
  → onConfirm(city, month) 호출
  ↓
handleCityMonthConfirm(city, month)
  → setFileInfo({ city, month })
  → setWorksheets(pendingSetup.sheetsData)
  → setMapDefs(pendingSetup.initialSel)
  → setShowCityPicker(false)
  → setPendingSetup(null)
  → setStep(2)
```

### CityMonthPickerModal Props

| Prop | 타입 | 설명 |
| --- | --- | --- |
| `userId` | string | Firebase Auth uid — 저장 키 분리용 |
| `detectedCity` | string | Web Worker 파싱 결과 지자체명 (없으면 빈 문자열) |
| `detectedMonth` | string | 파일 내 감지된 월 (없으면 빈 문자열) |
| `userCities` | string[] | 현재 로그인 유저의 허용 지자체 목록 |
| `isAdmin` | bool | 관리자이면 자유 입력(datalist), 일반이면 select |
| `onConfirm` | func | `(city, month) => void` |
| `onCancel` | func | `() => void` |

### 지자체 감지 우선순위

```text
1. 파일명 / 시트 내용에서 지자체명 자동 감지 (Web Worker `detectedCity`)
2. 감지 실패 → 저장된 마지막 사용 지자체 (`nexus_city_prefs_v2`)
3. 저장 없음 → `userCities[0]` 첫 번째 허용 지자체
```

### 월 감지 우선순위

```text
1. 파일 내 감지된 월 (`detectedMonth` → toYYYYMM 변환)
2. 해당 지자체 저장 이력 (`nexus_city_prefs_v2[userId][city].month`)
3. 현재 월 (new Date() 기준 YYYY-MM)
```

---

## 21. 설정 자동저장 (localStorage)

### nexus_city_prefs_v2 (지자체별 월 저장)

저장 키: `nexus_city_prefs_v2`

구조:

```json
{
  "{userId}": {
    "{지자체명}": {
      "month": "YYYY-MM",
      "lastUsed": 1716800000000
    }
  }
}
```

| 규칙 | 내용 |
| --- | --- |
| CP-1 | `onConfirm` 시 자동 저장 — 담당자가 확인 버튼을 누를 때만 저장 |
| CP-2 | `userId` 최상위 키 — 동일 기기 다계정 혼용 방지 |
| CP-3 | 지자체 변경 시 해당 지자체 저장 월로 자동 교체 (useEffect on city) |
| CP-4 | `lastUsed` 타임스탬프 — 모달에서 "최근 작업: N월 N일" 표시용 |
| CP-5 | 저장 실패 시 예외 무시 (try-catch) — localStorage 접근 불가 환경 대응 |

### nexus_col_map_v1_{city} (컬럼 매핑 자동저장)

저장 키: `nexus_col_map_v1_{지자체명}`

구조:

```json
{
  "이름": 2,
  "주소": 3,
  "수량(포수)": 5
}
```

| 규칙 | 내용 |
| --- | --- |
| CM-1 | Step3 컬럼 매핑 확인 시 자동 저장 |
| CM-2 | 같은 지자체 다음 업로드 시 저장된 매핑 자동 적용 |
| CM-3 | 저장 키에 지자체명 포함 — 지자체마다 다른 엑셀 포맷 지원 |
| CM-4 | 키 형식: `nexus_col_map_v1_${city}` (city는 공백 포함 원문 그대로) |

---

## 22. 버전 관리·업데이트 내역 자동화 (배포 표준 절차)

> 버전 번호·빌드시간·CHANGELOG·사용자 팝업이 하나로 맞물려 자동 동작한다. 손으로 `version.js`를 직접 수정하지 말 것.

### 배포 단위 작업이 끝나면 반드시 release 실행

```bash
# 형식: node scripts/bump-version.cjs [minor|patch|major] "변경항목1" "변경항목2" ...
node scripts/bump-version.cjs minor "쉬운정제 자동저장 추가" "이번달 명단 0건 표시 해결"
# 또는 npm 별칭(인자 앞 -- 필요)
npm run release -- minor "항목1" "항목2"
```

| 규칙 | 내용 |
| --- | --- |
| VER-1 | `bump-version.cjs` 한 번 실행으로 ① `package.json`/`lock` semver ② `version.js`의 `APP_VERSION`·`APP_BUILD`·`APP_BUILD_TIME`(KST) ③ `CHANGELOG` 맨 앞 새 버전 블록 — 전부 자동 갱신 |
| VER-2 | bump 종류 기본 `minor`(빠른 버전업, 표기 `V{major}.{minor}` 2자리). minor가 100 도달 시 major 롤오버. 큰 변경만 `major`, 핫픽스만 `patch` |
| VER-3 | CHANGELOG 항목은 **형(사용자)이 이해하는 한국어 기능 설명**으로 작성(기술용어·파일명 최소). 인자로 넘긴 항목이 그대로 사용자 팝업에 노출됨 |
| VER-4 | CHANGELOG는 **append-only** — 과거 버전 항목 절대 수정·삭제 금지. 같은 버전 블록 중복 삽입은 스크립트가 자동 차단 |
| VER-5 | 변경항목 인자를 생략하면 버전/빌드시간만 갱신되고 CHANGELOG는 보존(긴급 빌드용). 정상 배포는 **항상 항목을 넘긴다** |
| VER-6 | 사용자는 새 버전 첫 접속 시 `WhatsNewModal` 자동 팝업으로 변경내역 확인(기기별 `localStorage` 키 `nexus_whatsnew_seen_v1`). 헤더 버전 배지 클릭 시 전체 내역 재열람 |
| VER-7 | release 직후 `npm run build && npm run deploy`(= `firebase deploy --only hosting --account ttong627@gmail.com`). 운영 배포는 형 확인 후 |
| VER-8 | `WhatsNewModal`/자동표시 useEffect 수정 시 `nexus_whatsnew_seen_v1` 키 구조 유지(인트로·웰컴투어와 표시 순서 충돌 방지 로직 보존) |
