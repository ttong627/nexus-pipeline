# NEXUS PIPELINE V5.0 — 프로젝트 룰 (운영 전체 규칙)

이 파일은 프로젝트의 모든 운영 규칙을 정의합니다.
코드 수정 시 이 규칙들이 깨지지 않도록 반드시 준수해야 합니다.

---

## 기술 스택

- React 19.2 + Vite 8
- Firebase 12 (Firestore, Auth, Hosting)
- Tailwind CSS 4
- XLSX (SheetJS) — Web Worker에서 실행
- 도로명주소 API: business.juso.go.kr (3개 키 로테이션)
- 배포: `npm run build && firebase deploy --only hosting`

---

## 1. 주소 정제 규칙

| # | 규칙 | 상세 |
|---|------|------|
| A-1 | 이름 5자 초과 | 5자로 자르고 `(본명:XXX)` 를 특이사항에 추가 |
| A-2 | 오타 사전 적용 | Firestore `typo_dict` 우선 치환 (AI 자가학습). Enter키로 재정제 시 오타→정답 자동 등록 |
| A-3 | 유니코드 정규화 | NFC + 제어문자/NBSP/탭/개행 제거, 따옴표 제거 |
| A-4 | 미닫힌 괄호 제거 | `\([^)]*$` 패턴 삭제 |
| A-5 | 지역 접두어 제거 | 시/도/구/동 토큰 — 도로명·지번 만나면 제거 중단 |
| A-6 | 통·반 제거 | `제X통 제X반` 패턴 제거 |
| A-7 | 주민센터 검색어 생성 5단계 | ①행정동 컬럼 > ②분리형(XX동 주민센터) > ③부착형(청량리주민센터→청량리동) > ④텍스트에서 동·읍·면 추출 > ⑤cityLabel 마지막 레벨 |
| A-8 | 건물명 전용 Fallback | 도로명·지번 없고 건물 유형어 있으면 `adminDong + 건물명` 으로 재검색 |
| A-9 | 특수문자 분리 | `* / ☆ ★ \| ~ # § ※ ** => ->` 이후 내용 → 특이사항으로 이동 (Firestore `special_chars` 자동 확장) |
| A-10 | 동호 형식 정규화 + 4자리 패딩 | `XXX동 YYY호` → `XXX-YYY호`. 호수 4자리 미만 시 공백으로 우측 패딩 (`101- 203호`, `101-  21호`) |
| A-11 | 최종 주소 형식 | `도로명주소, 동호수 (동명, 건물명)` 또는 `도로명주소, (동명, 건물명) 기타설명`. `\d+호` 포함(isUnitDetail=true)이면 쉼표 뒤, 없으면 `()` 뒤에 위치. 괄호 안에 동명·건물명 `,` 구분, trailing 쉼표 제거 |
| A-12 | 확인필요 플래그 3종 | 주소 공란 / 도로명 미발견+API 실패 / 변환 후 주소 3자 미만 |
| A-13 | API 캐시 + 지역 컨텍스트 검색 | 메모리(최대 1000건) + IndexedDB 2단계 캐시, dedup (pendingRequests Map). cityLabel에서 시·군·구 토큰(districtTok) 추출 → `"동대문구 왕산로 72"` 형태로 전국 동명 오매칭 방지 |
| A-14 | 병렬 처리 | asyncPool(20) — 500건 청크, 50건마다 메인스레드 양보 (화면 프리징 제로) |
| A-15 | 구분자 점 치환 | 도로명 번호 뒤 `숫자. ` → `숫자, ` (예: `테헤란로 123. 456호` → `테헤란로 123, 456호`) |
| A-16 | 번지 표기 제거 | `123번지` → `123` (지번주소 정규화) |
| A-17 | 층 표기 변환 비활성화 | `3F→3층`, `B1→지하1층` 모두 적용 안 함 — 아파트 B동·F동 등 건물동 명칭과 혼동 오탐 발생 |
| A-18 | 제(第) 접두어 제거 | `제101동` → `101동`, `제205호` → `205호` |
| A-19 | 동호 붙여쓰기 분리 | `101동205호` → `101동 205호` (A-10에서 대시로 변환) |
| A-20 | 리(里) 지번 인식 | `신남리 123-5` 형태의 지번주소에서 `리`를 isJibun 패턴에 포함 |
| A-21 | 동사무소/읍사무소/면사무소 → 주민센터 | 주소 및 특이사항 입력값에서 구식 명칭을 `주민센터`로 정규화. DB 전체 일괄 변경 가능 (관리자 패널 → 운영현황 → DB 마이그레이션) |
| A-22 | 특이사항 주민센터 → 주소 앞에 붙이기 | `inputNote`에 `주민센터` 포함 시 해당 주민센터 주소를 API로 조회하여 `result.주소` 앞에 `{주민센터 도로명주소}, {기존 주소}` 형태로 추가 (중복 방지 내장) |
| A-23 | 번길/가길 도로명 공백 없음 | `사가정로 2길` → `사가정로2길`, `XX로 14번길` → `XX로14번길`. 로/대로/길 + 공백 + 숫자+한글길 계열은 붙여쓰기 (역방향 패턴: `\s+(\d+[가-힣]*길)` 제거) |
| A-24 | 행정동 자동 보완 (emdNm fallback) | API 결과에서 dongPart 추출 실패 시: ①adminDong 컬럼 → ②JUSO API `emdNm`(읍면동명) 순서로 fallback. `adminDong`도 없을 때 `(동명)` 미표시 방지 |
| A-25 | 같은 도로명 `()` **건물명** 통일 | 배치 정제 완료 후 `unifyParenContent()` 후처리 — 공백 제거 후 도로명+번호(`황물로7길17`) 기준 그룹화 → **건물명만** 최빈값으로 통일. **동명은 각 레코드 그대로 보존** (`답십리2동`·`답십리동` 다른 행정동 덮어쓰기 금지). 괄호 없는 레코드에 건물명 추가 시 `, (건물명)` 쉼표 필수 |
| A-26 | `()` 내 동명 토큰 필터 | `buildingName` 구성 시 한글 시작 + 동/읍/면 접미어 토큰 제거 (`답십리1동`, `용두동` 등). 숫자 시작 건물 동호(`101동`)는 유지. 입력 주소 괄호 안 동명이 API 결과 `dongPart`와 달라도 중복 동명 표시 방지 |
| A-27 | 행정동 번호 접미어 제거 | 도로명주소 `()` 내 동명은 **법정동(번호 없음)** 표기. `답십리2동` → `답십리동`, `청량리3동` → `청량리동`. `행정동` 컬럼 값은 변경 안 함. `A-25 unifyParenContent`에서도 동일 정규화 적용 |
| A-28 | 중첩 괄호 잔여 `)` 제거 | 입력에 `삼화에코빌(6차)` 같은 중첩 괄호가 포함된 경우, non-greedy `/\(.*?\)/g` 추출 후 바깥 `)` 가 text에 잔류하여 `103- 501호 ) (장안동)` 형태의 오출력 발생. **괄호 추출 직후 text 내 남은 `)` 를 모두 제거** (`text = text.replace(/\)/g, '')`). 구현: `src/engine/addressEngine.js` 괄호 추출 다음 줄. 절대 되돌리지 말 것 |

---

## 2. DB 저장 규칙 (기본명단 — `base_lists`)

### 2-1. 3순위 매칭 시스템 (절대 변경 금지)

```
1순위: 이름 + 생년월일         → liveByBirth    (전체 레코드)
2순위: 이름 + 휴대폰           → liveByPhone    (생년월일 없는 레코드만)
3순위: 이름 + 유선전화         → liveByLandline (생년월일·휴대폰 모두 없는 레코드만)
```

### 2-2. 세부 규칙

| # | 규칙 | 상세 |
|---|------|------|
| B-1 | 저장 제외 조건 | 생년월일 + 휴대폰 + 유선전화 모두 없으면 저장 불가 |
| B-2 | 동명이인 중복 차단 | `birthKeyedNames` Set — 생년월일 있는 이름은 2순위·3순위 신규 추가 금지 |
| B-3 | 인덱스 키 정규화 | `digitKey = v.replace(/[^\d]/g, '')` — 숫자만으로 비교 (포맷 무관) |
| B-4 | 저장 포맷 | `normPhone = v.replace(/[^0-9-]/g, '')` — 대시 유지 |
| B-5 | 인플라이트 중복 | `_isInFlight` 플래그 — 같은 배치 내 신규→신규 중복 방지 |
| B-6 | 배치 커밋 단위 | **499건씩** (`writeBatch` 500 제한) |
| B-7 | 캐시 우회 | `getDocsFromServer` — 서버 최신 데이터 직접 조회 |
| B-8 | 스키마 혼재 지원 | 구버전(한국어 키: 이름/휴대폰) + 신버전(영문 키: name/mobile) 동시 지원 |
| B-9 | note 저장 | 기존 ◆ 이식 내용 제거 후 저장: `.replace(/\s*◆[^◆]*/g, '').trim()` |
| B-10 | Payload 필드 | `name, birthKey, dong, address, mobile, landline, note, driver, seqNo, sms` |
| B-11 | 감사 로그 | `audit_logs` 컬렉션에 모든 배치 저장 기록 (action, city, addCount, updateCount, adminEmail) |

---

## 2-3. 이번달 명단 주소 정제 기능 (`CloudListManager`)

| # | 규칙 | 상세 |
|---|------|------|
| CL-1 | 주소 정제 버튼 | 이번달 명단 보기 상태에서 **주소 정제** 버튼 클릭 → 저장된 레코드 전체에 `processAddress` 적용 (asyncPool 10, progress 표시) |
| CL-2 | 변경 감지 | 정제 전 주소 ≠ 정제 후 주소인 레코드만 `addrChanges` 목록으로 추적 |
| CL-3 | 저번달 비교 | `delivery_history/{city}/months/{YYYY-MM-1}/records` 에서 name+birthKey 또는 name+phone(끝7자리) 매칭 → `prevAddr` 필드로 비교 표시 |
| CL-4 | 변경 유형 자동 감지 | `오류` (확인필요=true) > `이사` (저번달·신규 주소의 **도로명+번호** 불일치 — 공백·괄호·쉼표 무시 유사값 비교) > `정제` (도로명 동일, 포맷/동호수만 변경) |
| CL-5 | 주소변경확인 버튼 | 변경 건수를 배지로 표시. 클릭 시 모달 오픈 |
| CL-6 | 모달 탭 구성 | **행정동 요약** (기본): 동별 정제/이사/오류 건수 + 이상 여부 / **전체 목록**: 기존주소·정제주소·저번달주소·유형·처리 |
| CL-7 | 행정동 이상 경고 | 저번달 대비 주소 변경 10건 이상인 행정동 → 빨간 `⚠ {동} 이상 N건` 배지 + 모달 내 "행정동 주소이상" 표시 |
| CL-8 | 담당자 처리 | 개별 원복(단일 변경 취소) / 재적용 / 유형 수동 변경(정제·이사·오류) / 전체 원복 가능 |
| CL-9 | 저장 방식 | 정제 결과는 `dirtyRecords`에 반영 → 기존 **변경사항 저장** 버튼으로 Firestore에 확정 커밋 |
| CL-10 | 정제 대상 필드 | 주소(`주소`), 확인필요/확인사유 — 이름·행정동·전화번호 등 나머지 필드 불변 |

---

## 3. 월별 클라우드 명단 저장 규칙 (`cloud_lists`)

| # | 규칙 | 상세 |
|---|------|------|
| C-1 | 저장 경로 | `cloud_lists/{city}/months/{YYYY-MM}/records` |
| C-2 | 중복 저장 방지 | 동월 데이터 존재 시 confirm 후 전체 삭제 → 재저장 |
| C-3 | 메타 문서 | city, monthId, totalCount, 수급자Count, 차상위Count, uploadedAt, uploadedBy |
| C-4 | 포수 기본값 | 빈값이면 1로 처리 |
| C-5 | 저장 대상 | `_에러` 없는 정상 데이터만 저장 |

---

## 4. 신규 데이터 이식 및 업데이트 규칙

| # | 규칙 | 상세 |
|---|------|------|
| D-1 | baseMap 키 생성 | `이름_생년월일` 또는 `이름_전화번호(숫자만)` |
| D-2 | 전체 이식 | `importFields === null` 이면 특이사항·기사·배송순번 전부 이식 |
| D-3 | 선택 이식 | `importFields` 가 Set이면 해당 필드만 이식 (note, driver, seqNo) |
| D-4 | ◆ 표시 | 기본명단 이식 특이사항에 `◆` 접두사 추가, 기존 `[기본]` 접두사 제거 |
| D-5 | 우선순위 | Excel 원본값 우선 — 빈값일 때만 기본명단에서 이식 |
| D-6 | 단건 실시간 업데이트 | 이름+생년월일 → 실패하면 이름+휴대폰 순서로 조회 후 upsert |

---

## 5. 컬럼 매핑 규칙

| 내부 키 | 감지 키워드 | 예외 |
|---------|-----------|------|
| `이름` | 이름, 성명, 대상자, 수령자명 | — |
| `주소` | 주소 | — |
| `수량(포수)` | 포수, 수량, 구입량, 포, 신청수량, 신청 수량 | **가구원** 포함 컬럼 제외 |
| `연락처` | 휴대, 연락, 전화, 유선, 핸드폰, 핸드, 모바일, 휴폰 | — |
| `행정동` | 행정동, 읍면동, 동명, 관할구역 | — |
| `비고` | 특이사항, 비고, 메모 | — |

추가 규칙:
- 헤더 행 자동 탐지: 상위 20행 중 키워드 최다 매칭 행 선택
- 병합 헤더: 최대 2행 서브헤더 통합 (데이터행 판단되면 중단)
- 시트 유형: `수급/의료/생계` → 기초수급자 / `차상위` → 차상위 / 나머지 → 기초수급자
- 혼합 시트: `구분/유형/계층/자격` 컬럼 데이터 값으로 자동 감지

### 5-1. 형식(내용) 기반 자동 매칭 규칙 (헤더 키워드와 병행 — 매칭 100% 목표)

헤더 키워드로 애매하면 **데이터 샘플(상위 N행)의 값 형식**으로 판정한다.

| 내부 키 | 데이터 형식(내용) 시그니처 | 판정·예외 |
|---------|----------------------------|-----------|
| `이름` | 2~5자 한글 이름, 숫자·기호 거의 없음 | 헤더 키워드 우선 |
| `행정동`(읍면동) | `OO동/읍/면` 짧은 행정구역명 다수 | 주소와 구분(도로명·번지·호 없음) |
| `주소` | 도로명/지번 포함 긴 텍스트(로·대로·길·번지·동·호) | 가장 긴 텍스트 칼럼 |
| `휴대폰` | 010/011/016~019 + 11자리 형식이 **다수**인 칼럼 | **휴대폰 형식 아니면 매칭 불가**(지역번호뿐이면 유선으로) |
| `유선전화` | 지역번호(02/0XX) 형식 연락처 | 휴대폰 칼럼과 분리 |
| `포수` | 1·2·3 등 소수 정수, 헤더 `포수/수량/10kg` | **가구원수 절대 금지** |
| `문자수신` | `Y/N/O/X`(○/×/예/아니오 포함) 값만 | — |
| `생년월일` | 날짜/달력 데이터(YYYYMMDD·YY.MM.DD·엑셀 날짜) | — |
| `구분` | 의료/생계/수급/기초/차상위 값 | **`기초수급자`·`차상위` 2가지로만 정규화** |
| `특이사항` | 배송 도움 메모(부재·경비실·문앞·호출·계단 등) | 비고/특이/메모 |

판정 순서: ① 헤더 키워드 → ② 데이터 샘플 형식 다수결 → ③ 충돌 시 **형식 우선**(연락처는 휴대/유선 형식으로 분리). 각 칼럼 **매칭 신뢰도 점수화** → 100% 아니면 쉬운정제에서 사용자 1클릭 확인.

### 5-2. 지자체 자동 감지 + 허가 지자체 대조

- 파일명/시트명/요약행 + **주소 칼럼의 시·군·구 토큰**에서 지자체 후보 추출.
- **사용자 허가 지자체(`userCities`)와 대조** → 일치하는 **정확한 지자체명** 확정.
- 후보 불명확 시: 명단 주소의 **최빈 시·군·구** → `userCities` 매칭. 그래도 불명확하면 사용자에게 확인(CityMonthPickerModal).

---

## 6. 정렬 규칙

- **기본 정렬 (4단계 우선순위)**: 행정동(읍면동) → 리(里) → 주소(도로명) → 이름 (모두 오름차순, 한국어 numeric). 리 없으면 해당 단계 빈값으로 자연 통과
- **컬럼 클릭 정렬**: 해당 컬럼 단일 정렬 (클릭 반복 시 asc ↔ desc 토글)

---

## 7. 특이사항 조합 규칙

조합 순서 (공백으로 join, 빈값 제외):

```
1. 주소 엔진 결과  (건물명 분리, 본명 초과, 특수문자 뒤 내용)
2. 전화번호 레이블 (레이블(번호) 형식 — 예: 딸(010-1234-5678))
3. 원본 비고 컬럼 값
4. 기본명단 이식   (◆이식내용)
```

---

## 8. 전화번호 처리 규칙

| 규칙 | 내용 |
|------|------|
| 번호+텍스트 분리 | `extractPhoneNote` — 9자리 미만이면 전화번호 아님 |
| 구분자 | 공백, /, \|, 쉼표, 세미콜론, 콜론, 괄호, 대시+텍스트 |
| 포맷 | 11자리→010-XXXX-XXXX, 10자리(02)→02-XXXX-XXXX, 9자리(02)→02-XXX-XXXX |
| 모바일 판별 | 010/011/016/017/018/019 시작 |
| 레이블 형식 | `레이블(번호)` — 예) `딸(010-1234-5678) 형님번호(010-0000-0000)` |
| 내부 중복 제거 | `parsePhoneNumbers` 에서 formatPhone 공용 함수 사용 (별도 정의 금지) |

---

## 9. 생년월일 정규화 규칙

| 입력 | 변환 결과 |
|------|----------|
| 8자리 숫자 (19750315) | 앞 2자리 버리고 `75.03.15` |
| 6자리 숫자 (750315) | `75.03.15` |
| `YY-MM-DD` / `YY/MM/DD` | `-` → `.` 치환 |
| 비정형 | 원문 반환 |

---

## 10. 문자수신 파싱 규칙

| 결과 | 패턴 |
|------|------|
| **N (거부 우선)** | 거부/거절/불가/불허/미동의/미수신/수신불가 |
| N | N, n, X, x, ×, 0, 아니오, 불, 없음 |
| Y | Y, y, O, o, ○, ㅇ, 1, 예, 동의, 수신, 가능, 허용, yes, YES |
| N | 패턴 미매칭 기본값 |

---

## 11. 중복 분석 규칙 (부가서비스)

| 유형 | 기준 | 보존 행 |
|------|------|---------|
| 강한 중복 | 이름+생년월일+전화 모두 일치 | 마지막 행 |
| 약한 중복 | 행정동+이름+전화 일치 | 특이사항 있는 행 우선, 없으면 마지막 행 |
| 특이사항 없음 | 중복 아니어도 특이사항 공란 | 삭제 추천 대상 |

---

## 12. 내보내기 파일명 규칙

```
{지자체명}-{X}월-기초{N},차상위{M},전체{T}-{MMDD}{HHMMSS}.xlsx
[확인필요]{위 형식}.xlsx
[행정동요약]{위 형식}.xlsx
```

---

## 13. Undo 규칙

- 최대 10단계 이전 상태 복원
- gridData 모든 변경은 반드시 `pushHistory` 경유

---

## 14. 인증·권한 규칙

| 규칙 | 내용 |
|------|------|
| 관리자 이메일 | `ttong627@gmail.com` |
| 관리자 강제 보정 | admin 이메일이면 role=admin, tier=sapphire 강제 설정 (onAuthStateChanged 시 자동 복구) |
| 일반 유저 강등 | 비admin이 role=admin인 경우 role=user, tier=basic으로 강등 |

---

## 15. 인트로 화면 규칙

- **표시 조건**: 신규 회원가입 + 지자체·성명 등록 완료 직후 1회만
- **이후 금지**: 같은 계정 재로그인 시 표시 안 함
- **구현**: `ProfileSetupModal` `onClose` 에서 `isNewUser === true` 였을 때만 `setShowIntro(true)`

---

## 16. 기사 배분 규칙 (루트맵)

### 제1원칙 (절대 원칙)
> **기사 구역은 완전히 분리된다. 지리적 겹침 절대 불가.**
> capacity 비례 포수 배분 · 이동거리 최소화 · 전건 배정 보장을 목표로 한다.

---

### R-0. 배타적 구역 (모든 규칙의 전제)

기사 구역은 완전히 분리된다. 동선 겹침 > 0이면 최종저장 불가.
구현: `handleSaveSession(isFinal=true)` 진입 시 `overlapCount > 0` 차단.

### R-A. 전건 배정 보장

970건 전원 배정 완료. 미배정 레코드 → 유효부담 가장 적은 기사에게 강제 배정.
구현: `handleAutoSplit` 후처리 마지막 단계.

### R-B. 좌표 없으면 자동배정 잠금

좌표 미수신 건수 > 0이면 "자동 N등분" 버튼 비활성화.
좌표 매칭 실행 후 여전히 실패한 건 → 같은 동 최다배정 기사에게 자동 귀속.
구현: `hasRunGeocoding` state, `handleFetchMissingCoords` 완료 후 `true`.

### R-C. capacity 비례 포수 배분

기사별 목표 유효부담 = 전체유효부담 × (capacity / capacity 합계).
예) 3명 85%/100%/70% → 합계 255% → 373포/439포/307포.
구현: `kMeansCluster` `targetLoads` 계산 (유효부담 기준).

### R-D. 절대 경계 (장벽 기반 구역 분리)

핀을 장벽 올바른 쪽에 배치하면 보로노이/K-means가 자연히 경계를 따름.

| 장벽 유형 | 분류 |
|-----------|------|
| 고속도로 · 자동차전용도로 | 절대 경계 |
| 철도 · 지하철 지상구간 · 기찻길 | 절대 경계 |
| 하천 · 강 (교량 1개 이하) | 절대 경계 |
| 왕복 4차선 이상 대로 | 권장 경계 |

### R-E. 동일 주소 동일 기사

같은 주소의 복수 수급자는 반드시 같은 기사 배정.
구현: `handleAutoSplit` 후처리 — 주소별 최다배정 기사로 통일.

### R-F. 겹침 감지 → 해소 후 저장

동선 겹침(150m 이내 다른 기사 레코드) 카운트 표시. 최종저장 전 0건 필수.
구현: `overlapCount`, `handleResolveOverlap`.

### R-G. 이전달 구역 우선 승계

기사명 동일하면 이전달 좌표·기사·순번 자동 이식.
구현: `handleLoadPrevMonth` 버튼.

### R-H. 유효부담 환산

실제 배송 노동량을 반영한 가중 포수 계산.

| 조건 | 환산 |
|------|------|
| 20포↑ + 임대키워드(LH·SH·임대 등) | × 0.3 |
| 빌라·연립·다세대·단독 + 층수 | × (1 + min(층,5) × 0.1) |
| 문앞·거동불편·직접전달 등 | × 1.5 |
| 전화필수·골목·경비실 등 | × 1.2 |
| 기타 | × 1.0 |

구현: `getEffectiveLoad()` 함수.

### R-I. 아파트 단지 동일 기사 + 대형단지 분할

기본: 같은 단지명 전체 → 자동배정 후 최다배정 기사로 통일.
예외: 50포↑ 임대 단지는 담당자가 동(棟) 단위로 선택적 분할 가능.

구현:
- `handleAutoSplit` 후처리 — 단지별 최다배정 기사로 통일
- 좌측 패널 "대형단지" 섹션 — 50포↑ 단지 목록 + 기사 드롭다운 + [분할] 버튼
- `handleAptDriverChange` — 단지 전체 일괄 기사 변경
- `openAptSplitModal` / `applyAptSplit` — 동 단위 분할 모달

### R-J. 외곽 고립 배송지 묶음

주변 300m 이내 동일기사 레코드 < 3건인 고립 레코드:
1순위 → 해당 방향으로 구역이 이어지는 기사
2순위 → capacity 여유 있는 기사 (목표의 80% 미만)
3순위 → 포수 가장 적은 기사
같은 방향 외곽 2건↑ → 반드시 1명에게 묶음.

### R-K. 배송 밀집도 점수

기사별 배송순번 인접 레코드 간 평균 이동거리.
200m 미만 → 초록 / 200~400m → 노랑 / 400m↑ → 빨강.
구현: 기사 카드 하단 표시.

### R-L. 행정동 원자성

1행정동 = 1기사 원칙. 포수 과다 시에만 2기사 허용.
보로노이/K-means가 자연히 처리. 결과 확인 후 수동 조정.

### R-M. 아파트/단독 비율 균형

한 기사에게 아파트만 또는 단독만 몰리지 않도록 구성.
자동 배정 결과 확인 후 필요 시 수동 조정.

### R-N. 배송 연속성 점수

순번 인접 레코드 간 300m↑ 이동 횟수(역주행 횟수). 적을수록 좋음.
구현: 기사 카드 하단 표시.

---

### 배분 알고리즘 적용 순서

```
1. buildUnits() — 아파트/동일주소/좌표 기준 배송단위 묶기 (R-E·R-I 전제)
   ※ 절대 원칙: 새 알고리즘의 입력 단위는 항상 buildUnits() 출력이어야 한다.
      개별 레코드를 직접 배정하면 R-E·R-I·도로측면 규칙이 모두 깨진다.
2. 전략 선택 (autoSplitStrategy)
   - pca (기본값): PCA 주축 투영 → 1D 정렬 → DP 연속 분할
   - hilbert (실험): 힐베르트 공간충전곡선 인덱스 정렬 → DP 연속 분할
   - bestOfPcaHilbert (실험): pca·hilbert 둘 다 계산 → qualityScore 높은 쪽 채택
   - seedVoronoi (예정): N-씨앗 보로노이 + 연속성 강제 (향후 구현)
3. 좌표 없는 레코드 → 같은 행정동 최다배정 기사 자동 귀속
4. 미배정 전건 → 좌표 가까운 기사 배정 → fallback: 유효부담 최소 기사 강제 배정 (R-A)
```

구현 위치: `routeWorker.js computeAutoSplit()`

### autoSplitStrategy 허용값 및 기본값

| 값 | 설명 | 기본값 |
|----|------|--------|
| `pca` | PCA 주축 투영 정렬 (현재 운영 방식) | ✅ 기본값 |
| `hilbert` | 힐베르트 공간충전곡선 정렬 (실험) | — |
| `bestOfPcaHilbert` | pca·hilbert 비교 후 qualityScore 높은 쪽 자동 선택 | — |
| `seedVoronoi` | N-씨앗 보로노이 (향후 구현) | — |

허용값 외 입력 → `pca` 로 자동 fallback.

### qualityScore 공식 (0–100점, 높을수록 좋음)

```
qualityScore = 100
  - 25 × (splitUnitCount    / max(1, totalMandatoryUnits))  // 배송단위 분리 — 최우선 결함
  - 20 × (islandCount       / max(1, totalDrivers))         // 구역 연속성
  - 20 × (mixedUnitCount    / max(1, totalUnits))           // 섬 혼재 단위
  - 15 × (maxAbsDiffPct     / 100)                          // 부담 편차
  - 10 × (roadSideMixCount  / max(1, totalRoadUnits))       // 도로측면 순수성
  - 10 × (forcedAssignCount / max(1, totalRecords))         // 강제배정 비율

clamp(0, 100)

splitUnitCount  : buildUnits()의 mandatory 단위가 2개 이상 기사로 분리된 수
mixedUnitCount  : 인접 기사 구역에 고립된 배송단위 수 (splitUnitCount와 별개)
islandCount     : 구역이 연속되지 않는(섬 포함) 기사 수
roadSideMixCount: 같은 도로 홀수/짝수 측면이 다른 기사에 배정된 도로 수
forcedAssignCount: 좌표·행정동 fallback으로 강제 배정된 레코드 수
```

---

## 17. 배송순번 독립 규칙

배송순번은 자동 기사배정의 일부가 아니라, 기사 배정이 끝난 뒤 별도 실행되는 독립 단계다.
자동 N등분은 `_driverId` 배정까지만 책임지고, 배송순번은 `RouteMapModal` 의 순번 엔진이 기사별로 다시 계산한다.

### DS-1. 실행 전제

| 규칙 | 내용 |
|------|------|
| DS-1-1 | 배송순번은 기사 배정 후 실행한다. `_driverId` 가 없는 레코드는 자동 순번 대상이 아니다 |
| DS-1-2 | 배송순번은 기사별로 독립 계산한다. 기사 A의 1번과 기사 B의 1번은 서로 충돌이 아니다 |
| DS-1-3 | 브러시 보정 직후 순번을 실행하면 `pendingPaintRef` 의 임시 배정을 먼저 반영한 뒤 순번을 계산한다 |
| DS-1-4 | 기존 순번이 있어도 일반 순번은 덮어쓸 수 있다. 관리자 AI 순번은 덮어쓰기 전 확인창을 띄운다 |

### DS-2. 일반 자동 배송순번 로직

구현 위치: `RouteMapModal.handleAutoSequence()`

```text
1. pendingPaintRef 임시 배정을 records 에 먼저 반영
2. 기사별로 `_driverId === driver.id` 인 레코드만 추출
3. 도로명 또는 아파트 정보가 하나라도 있으면 roadAwareTSP(driverRecs) 실행
4. 도로명/아파트 정보가 없고 좌표만 있으면 nearestNeighborTSP(coordRecs) fallback
5. 계산 결과를 해당 기사 안에서 1부터 순차 부여
6. analyzeSequenceQuality() 실행 후 순번 분석 패널 표시
```

### DS-3. roadAwareTSP 기준

| 단계 | 기준 |
|------|------|
| DS-3-1 | 도로명·아파트·동일주소를 배송순번 단위로 묶는다 |
| DS-3-2 | 같은 도로 안에서는 건물번호 흐름을 우선한다 |
| DS-3-3 | 시작 도로는 가장 북쪽 도로 그룹을 우선한다 |
| DS-3-4 | 다음 도로 그룹은 최근접 이웃 방식으로 선택한다 |
| DS-3-5 | 2-opt 로 교차 동선을 줄인다 |
| DS-3-6 | 각 도로 그룹은 이전 도착점에서 더 가까운 진입 방향(정방향/역방향)을 선택한다 |
| DS-3-7 | 좌표 없는 레코드는 거리 최적화에 직접 쓰지 않고 뒤쪽 확인 대상으로 붙인다 |

### DS-4. nearestNeighborTSP fallback 기준

| 단계 | 기준 |
|------|------|
| DS-4-1 | 도로명/아파트 정보가 부족한 기사 명단은 좌표 기반 최근접 이웃 순회로 계산한다 |
| DS-4-2 | 시작점은 가장 북쪽 좌표를 우선한다 |
| DS-4-3 | 매 단계에서 현재 지점과 가장 가까운 미방문 좌표를 다음 순번으로 선택한다 |
| DS-4-4 | 2-opt 교차 제거로 총 이동거리를 줄인다 |
| DS-4-5 | 좌표가 없는 레코드는 좌표 기반 fallback 순번 대상에서 제외하고 분석 경고로 남긴다 |

### DS-5. 수동 순번 편집

| 기능 | 규칙 |
|------|------|
| 클릭 순번 | 목록 행을 클릭하는 순서대로 현재 기사 안에서 1→2→3 순번을 부여한다 |
| 드래그 순번 | 같은 기사 안에서만 드래그 재정렬을 허용하고, 재정렬 후 1부터 다시 번호를 매긴다 |
| 직접 입력 | 목록의 배송순번 입력칸에서 수동 수정할 수 있다 |
| 기사 간 이동 | 순번 드래그는 기사 간 이동을 허용하지 않는다. 기사 변경은 배정 기능으로 처리한다 |
| 전체 초기화 | 배송순번 전체 초기화는 모든 `배송순번` 을 빈값으로 만든다 |

### DS-6. 관리자 AI 순번

| 규칙 | 내용 |
|------|------|
| DS-6-1 | 관리자 전용 기능이다 |
| DS-6-2 | 기사 배정이 1건 이상 있어야 실행한다 |
| DS-6-3 | 기존 배송순번이 있으면 덮어쓰기 확인창을 띄운다 |
| DS-6-4 | Kakao REST 키가 있으면 차량 경로 후보를 활용하고, 없으면 로컬 거리 계산으로 fallback 한다 |
| DS-6-5 | 실행 후 예상 정확도, 확인 필요 건수, Kakao/local 경로 계산 비율을 표시한다 |

### DS-7. 순번 품질 분석

구현 위치: `RouteMapModal.analyzeSequenceQuality()`

| 분석 항목 | 기준 |
|----------|------|
| 점프 | 인접 배송순번 간 300m 이상 이동이며, 같은 아파트/같은 도로측면이면 경고를 완화한다 |
| 도보 후보 | 인접 배송순번 간 120m 이하 이동 |
| 좌표 없음 | 순번은 있으나 좌표가 없어 거리 검증이 불가능한 레코드 |
| 도로 정보 없음 | 도로명 파싱이 불가능하고 아파트 후보도 아닌 레코드 |
| 도로 재방문 | 이미 지나간 도로가 순번 뒤쪽에서 다시 등장하는 경우 |
| 평균 이동거리 | 기사별 인접 순번 간 평균 이동거리 |
| 최대 이동거리 | 기사별 인접 순번 간 최대 이동거리 |
| 예상 정확도 | 점프·좌표없음·도로없음·도로재방문은 감점, 도보 후보는 소폭 가점 |

품질 기준:

```text
평균 이동거리 200m 미만  → 좋음
평균 이동거리 200~400m → 주의
평균 이동거리 400m 이상 → 위험
인접 순번 300m 이상 점프 → 확인 필요
```

### DS-8. 저장·이식·내보내기

| 구분 | 규칙 |
|------|------|
| 기본명단 | `seqNo` 로 저장하고, 전체 이식 또는 `seqNo` 선택 이식 때만 가져온다 |
| 월별 명단 | `cloud_lists/{city}/months/{YYYY-MM}/records` 에 `배송순번` 으로 저장한다 |
| delivery_history | 전월 비교·복원용으로 `seqNo` 를 함께 저장한다 |
| 이전달 승계 | 기사명이 같으면 이전달 기사·좌표·배송순번을 복원할 수 있다 |
| 기사 요청 반영 | 기사 공유 링크의 순번 요청은 담당자 유선 확인 후 해당 기사 배송순번만 공식 반영한다 |
| 내보내기 | 배송표는 `배송순번` 오름차순으로 정렬하고, 순번 없는 행은 원래 순서를 최대한 유지한다 |

### DS-9. 기사별 출발점 (Driver Start Point)

구현 위치: `RouteMapModal` 기사 카드 UI + `handleGeocodeStartAddr`

| 규칙 | 내용 |
|------|------|
| DS-9-1 | 기사 카드에 `출발지` 주소 입력란을 표시한다 |
| DS-9-2 | 기본 출발지: `경기도 수원시 장안구 정자천로188번길 39` (모달 마운트 시 자동 적용) |
| DS-9-3 | Kakao REST API (`/v2/local/search/address.json`)로 입력 주소를 지오코딩하여 `startLat`, `startLng` 저장 |
| DS-9-4 | `roadAwareTSP`, `nearestNeighborTSP` 모두 `startPoint` 파라미터를 받아 가장 가까운 배송단위부터 시작한다 |
| DS-9-5 | `startAddr`, `startLat`, `startLng` 는 세션 저장·delivery_history·공유 링크 등 **외부 출력에 포함하지 않는다** (보안) |
| DS-9-6 | 모달 마운트 시 모든 기사의 `startAddr` 가 있고 `startLat` 없으면 자동 지오코딩 실행 |

### DS-12. 아파트·빌라 동→층 정렬

구현 위치: `RouteMapModal.sortSequenceUnitRecords()` + `parseInferredFloor()`

층수 파싱 기준 (`parseInferredFloor`):

| 입력 예시 | 결과 | 비고 |
| --- | --- | --- |
| `503호` | 5층 | 3자리 → 첫 자리 |
| `1503호` | 15층 | 4자리 → 앞 두 자리 |
| `101호` | 1층 | 3자리 → 첫 자리 |
| `1001호` | 10층 | 4자리 → 앞 두 자리 |
| `B101호` (동 없음) | 지하 1층 (-1) | B + 첫 자리 = 지하 층 |
| `지하101호` | 지하 1층 (-1) | 지하 prefix |
| `B동 101호` | 1층 | B동 = 건물동명, 지하 오인 금지 |
| `가101호` | skip (null) | 가·나·다... + 숫자 = 건물동 표기 |
| `상가 203호` | skip (null) | 상가 키워드 |
| `나동 503호` | 5층 | 동 표기 제거 후 503호 → 5층 |

정렬 우선순위: 동(棟) → 추론 층 오름차순 → 호수 (층 추론 실패 시 기존 `parseFloorHo` fallback)

### DS-14. 총거리·예상시간 표시

구현 위치: `RouteMapModal.analyzeSequenceQuality()` + 순번 분석 카드 UI

| 규칙 | 내용 |
|------|------|
| DS-14-1 | 기사별 인접 순번 간 haversine 거리 합산 → `totalDistKm` (소수 1자리, m→km) |
| DS-14-2 | `SEQUENCE_ESTIMATED_SPEED_KMH = 10` 상수 (도심 배송 속도, 정차·엘리베이터 포함) — 모듈 상단에 분리 |
| DS-14-3 | `estimatedMinutes = round((totalDistKm / SEQUENCE_ESTIMATED_SPEED_KMH) × 60)` |
| DS-14-4 | 순번 분석 카드 하단에 `총 {N}km · 약 {M}분 · {count}건` 표시 |
| DS-14-5 | `SEQUENCE_ESTIMATED_SPEED_KMH` 는 절대 하드코딩 금지 — 상수만 수정하여 전체 반영 |

---

## 18. 루트맵 UI 조작 규칙 (RouteMapModal)

### 자동 배정 버튼

"자동 N등분" 단일 버튼. `autoSplitStrategy` 기준 자동 기사배정 + R-A~R-I 후처리를 일괄 실행한다.
좌표 미수신 > 0이고 좌표매칭 미실행 시 버튼 잠금 (R-B).

### 기사 핀 (거점 배치)

- 기사 카드의 [핀 꽂기] 버튼 클릭 → 지도 클릭 1회로 거점 위치 확정 (이후 클릭 무시)
- 핀 = 기사 구역의 좌우/상하 순서를 정하는 기준
- 핀 전체 있으면 핀 주축 순서대로 연속 권역을 분할하고, 없으면 좌표 분포 주축으로 연속 권역 균형 분할
- 핀 제거: [×] 버튼 클릭

### 연속 권역 균형 기준

```text
targetLoad = 전체유효부담 × (capacity / 전체capacity)

아파트 도로명주소 그룹, 대로/로 좌우 측면, 같은좌표 묶음을 배정 단위로 만든 뒤
지도 좌표의 PCA 주축 또는 선택된 autoSplitStrategy 기준으로 정렬하여 각 기사 targetLoad에 가까운 연속 구간을 배정한다.
가까운 점을 하나씩 따라가는 TSP식 순회 경로는 자동배정 경계 산정에 사용하지 않는다.
TSP식 순회는 밀집 지역에서 구역을 뱀처럼 왕복시켜 색이 섞일 수 있으므로 배송순번 계산에만 사용한다.
```

`getEffectiveLoad()` 가중치(임대 0.3×, 계단 층수 보정, 문앞 1.5× 등) 그대로 반영.

### 페인트 브러시 모드 (2차 수동 보정)

- 기사 카드의 [브러시] 버튼 클릭 → 해당 기사 색상 페인트 모드 활성
- 지도 위에서 마우스 드래그 → 브러시 반경 내 모든 핀을 선택 기사로 일괄 재배정
- 반경 슬라이더로 브러시 크기 조절
- [브러시 종료] 버튼 또는 다른 기사 선택 시 모드 해제

### 대형단지 패널 (좌측 패널 하단)

50포↑ 아파트 단지 목록. 기사 드롭다운으로 단지 전체 일괄 변경.
[분할] 버튼 → 동 단위 분할 모달.

### 배정 관리

- **배정 전체 초기화**: 모든 `_driverId → null`. Undo 없으므로 신중히 사용
- **핀 우클릭**: 개별 핀 미배정 전환
- **최종저장 잠금**: 겹침 > 0건이면 차단 (R-0)

---

## 19. 코드 보호 원칙

- 기존 작동 기능 임의 변경 금지
- 배치 499 제한 절대 초과 금지
- 3순위 매칭 시스템 구조 변경 시 전체 인덱스 재검토 필수
- `getDocsFromServer` → `getDocs` 로 교체 금지 (캐시 오염 위험)
- `pushHistory` 경유 없이 `setGridData` 직접 호출 금지 (Undo 파괴)
- `SEQUENCE_ESTIMATED_SPEED_KMH` 상수 하드코딩 금지 — 모듈 상단 상수만 수정
- `parseInferredFloor` 로직 변경 시 DS-12 전체 테스트 케이스 재검토 필수
- `CityMonthPickerModal` 컴포넌트 수정 시 `nexus_city_prefs_v2` 키 구조 유지
- 파일 업로드 후 Step2 직접 이동 금지 — 반드시 `pendingSetup` → `showCityPicker` → `handleCityMonthConfirm` 경로 경유
- 작업 완료 후 항상 `npm run build && firebase deploy --only hosting` 실행

---

## 20. 업로드 워크플로우 (CityMonthPickerModal)

### 흐름

```text
파일 업로드 (Web Worker 파싱 완료)
  → setPendingSetup({ sheetsData, initialSel, detectedCity, detectedMonth })
  → setShowCityPicker(true)          ← Step2 직접 이동 금지
  ↓
CityMonthPickerModal 표시
  → 사용자가 지자체 · 적용 월 확인/수정
  → onConfirm(city, month) 호출
  ↓
handleCityMonthConfirm(city, month)
  → setFileInfo({ city, month })
  → setWorksheets(pendingSetup.sheetsData)
  → setMapDefs(pendingSetup.initialSel)
  → setShowCityPicker(false)
  → setPendingSetup(null)
  → setStep(2)
```

### CityMonthPickerModal Props

| Prop | 타입 | 설명 |
| --- | --- | --- |
| `userId` | string | Firebase Auth uid — 저장 키 분리용 |
| `detectedCity` | string | Web Worker 파싱 결과 지자체명 (없으면 빈 문자열) |
| `detectedMonth` | string | 파일 내 감지된 월 (없으면 빈 문자열) |
| `userCities` | string[] | 현재 로그인 유저의 허용 지자체 목록 |
| `isAdmin` | bool | 관리자이면 자유 입력(datalist), 일반이면 select |
| `onConfirm` | func | `(city, month) => void` |
| `onCancel` | func | `() => void` |

### 지자체 감지 우선순위

```text
1. 파일명 / 시트 내용에서 지자체명 자동 감지 (Web Worker `detectedCity`)
2. 감지 실패 → 저장된 마지막 사용 지자체 (`nexus_city_prefs_v2`)
3. 저장 없음 → `userCities[0]` 첫 번째 허용 지자체
```

### 월 감지 우선순위

```text
1. 파일 내 감지된 월 (`detectedMonth` → toYYYYMM 변환)
2. 해당 지자체 저장 이력 (`nexus_city_prefs_v2[userId][city].month`)
3. 현재 월 (new Date() 기준 YYYY-MM)
```

---

## 21. 설정 자동저장 (localStorage)

### nexus_city_prefs_v2 (지자체별 월 저장)

저장 키: `nexus_city_prefs_v2`

구조:

```json
{
  "{userId}": {
    "{지자체명}": {
      "month": "YYYY-MM",
      "lastUsed": 1716800000000
    }
  }
}
```

| 규칙 | 내용 |
| --- | --- |
| CP-1 | `onConfirm` 시 자동 저장 — 담당자가 확인 버튼을 누를 때만 저장 |
| CP-2 | `userId` 최상위 키 — 동일 기기 다계정 혼용 방지 |
| CP-3 | 지자체 변경 시 해당 지자체 저장 월로 자동 교체 (useEffect on city) |
| CP-4 | `lastUsed` 타임스탬프 — 모달에서 "최근 작업: N월 N일" 표시용 |
| CP-5 | 저장 실패 시 예외 무시 (try-catch) — localStorage 접근 불가 환경 대응 |

### nexus_col_map_v1_{city} (컬럼 매핑 자동저장)

저장 키: `nexus_col_map_v1_{지자체명}`

구조:

```json
{
  "이름": 2,
  "주소": 3,
  "수량(포수)": 5
}
```

| 규칙 | 내용 |
| --- | --- |
| CM-1 | Step3 컬럼 매핑 확인 시 자동 저장 |
| CM-2 | 같은 지자체 다음 업로드 시 저장된 매핑 자동 적용 |
| CM-3 | 저장 키에 지자체명 포함 — 지자체마다 다른 엑셀 포맷 지원 |
| CM-4 | 키 형식: `nexus_col_map_v1_${city}` (city는 공백 포함 원문 그대로) |
